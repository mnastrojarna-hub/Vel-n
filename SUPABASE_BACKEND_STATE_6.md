# SUPABASE BACKEND STATE — MotoGo24 (Část 6: Changelog)
> **Soubory:** 1/6 (Tabulky) | 2/6 (Sloupce) | 3/6 (RPC funkce) | 4/6 (Triggery) | 5/6 (RLS, Realtime, Edge, Storage, Secrets) | **6/6 (Changelog)**

---

## CHANGELOG

| Datum | Změna |
|-------|-------|
| 2026-04-25 | **NEW: Real-time refresh kalendářů na webu (pending = bíle viditelné ostatním):** Frontend-only, BEZ SQL změn. 1) `motogo-web-php/js/pages-rezervace-calendar.js` — `MG._rezLoadCalendar` rozdělen na `MG._rezFetchBookings` (sdílený fetch + map) a render. Přidán `MG._rezStartLiveRefresh()` s 30s `setInterval`, který tichounce přefetchuje booked map přes `get_moto_booked_dates` RPC a re-renderuje kalendář pouze pokud se data změnila (diff přes JSON.stringify). Stop helper se sám zavolá, když `#rez-calendar` zmizí z DOM. 2) `motogo-web-php/pages/katalog-detail.php` — inline JS detail kalendáře refaktor: `buildBookedDays(bookings)` extrahováno + `startLiveRefresh()` se spouští po `buildCalendar()` se stejnou 30s frekvencí. 3) `motogo-web-php/css/pages.css` — sjednocena bílá pro nepotvrzené dny: `.cal-day.unconfirmed{background:#fff;color:#333;border:2px solid #ccc}` (bylo `transparent`) + `.cicon.unconfirmed{background:#fff;border:2px solid #ccc}`. **Flow:** zákazník vyklikne dny → projde formulář → `create_web_booking` vytvoří `pending` booking → ostatní uživatelé do 30 s vidí dny bíle (status='unconfirmed' v JS pro pending+createdAt<4h). Po 4 h pg_cron `auto_cancel_expired_pending()` (každé 2 min, app=10min/web=4h) buď pending→cancelled (dny zase volné) nebo pokud zákazník mezitím zaplatí, `confirm_payment` → reserved/active (dny šedě obsazené). Anon uživatelé Realtime subscribe na `bookings` nedostanou kvůli RLS — proto polling. Bez backend změn — `get_moto_booked_dates` RPC vrací status+created_at už od dříve. |
| 2026-04-25 | **NEW: Web rezervace — auto-výpočet trasy přistavení/vrácení + ukládání času vrácení mimo provozovnu:** 1) **Frontend (`motogo-web-php/js/`)**: Odstraněny checkboxy „✅ Potvrzuji adresu přistavení/vrácení" — stačí vyplněná adresa. Po výběru adresy z Mapy.cz autocomplete / map pickeru / fallback geocode (na change/blur) se přes Mapy.cz Routing API spočítá nejrychlejší trasa (`routeType=car_fast`) od pobočky **Mezná 9, 393 01 Mezná** k zadané adrese. Cena přistavení/vrácení = **1 000 Kč + 40 Kč × km** (každý směr zvlášť). Rozpis km, doba jízdy a cena se zobrazí přímo v panelu přistavení/vrácení. Při „Vrátit motorku na stejné adrese" se distance/fee propaguje. Při manuálně zadaných adresách bez autocomplete se na change-event spustí fallback geocode + výpočet. Default časy: pickup 09:00, return 19:00 (předvyplněno, lze měnit). Helpery: `MG._mapyRouting`, `MG._ensureBranchCoords`, `MG._calcRouteFor`, `MG._calcDeliveryFee`, `MG._renderRouteInfo`. 2) **SQL — rozšíření `create_web_booking` (spuštěno ručně mimo gitu):** přidán parametr `p_return_time text DEFAULT NULL` na konec signatury (31 parametrů celkem) + sloupec `return_time` v `INSERT INTO bookings (...)` s hodnotou `p_return_time`. Frontend (`pages-rezervace-steps.js`) posílá `p_return_time` POUZE při vrácení mimo provozovnu — buď `rez-return-other.checked` (vlastní adresa vrácení), nebo `rez-delivery + rez-return-same-as-delivery` (vrácení na adresu přistavení). Při vrácení v půjčovně Mezná zůstává `bookings.return_time = NULL`. 3) Beze změny v Flutter app a Velínu (Flutter má vlastní edit screen s `pickup_time/return_time` z 2026-04-22). |
| 2026-04-25 | **FIX: Druhá kolize `create_web_booking` (PGRST203) + UX redesign step 1 + Flutter app insert gear sizes:** 1) Po deploy 30-parametrové verze v DB zůstala vedle ní stále **stará 26-param** verze (vznikla z 2026-04-11 migrace, nebyla dropnuta při dnešním nasazení). PostgREST opět hlásil `Could not choose the best candidate function`. **SQL (mimo gitu — spuštěno ručně):** `DROP FUNCTION public.create_web_booking(uuid, timestamptz, timestamptz, text, text, text, text, text, text, text, text, time, text, text, jsonb, numeric, text, text, uuid, text, text, text, text, text, text, text);` (signatura bez 4 passenger size parametrů). Po dropnutí zůstala v DB jediná verze 30-param. 2) **UX redesign step 1** (`pages-rezervace.js` + `pages.css`): Velikostní chips se nyní zobrazují **vždy** přímo v gear-cards (žádný collapse animation). Rider gear card je **předzaškrtnutá a otevřená by default** (výbava je v ceně). Každá ze 4 karet má pod hlavičkou vlastní hint („Vyberte své velikosti..." pro řidiče zelený, „Zaškrtněte výše + vyberte velikosti..." pro placené extras šedý → zelený po zaškrtnutí). Toggle „Mám vlastní výbavu" přepíná pouze rider card do `disabled` stavu (panel + hint schovaný). 3) **Flutter app fix** (`payment_screen.dart`): Přidán parser, který před `.from('bookings').insert()` extrahuje gear sizes z `BookingDraft` (řidič 5 polí) + ze stringu v `SelectedExtra.size` extras `extra-spolujezdec` (formát „Helma: M, Rukavice: L, Bunda: M, Kalhoty: M") + `extra-boty-spolu` (boty spolujezdce) + `extra-boty-ridic` (fallback boty řidič). Insert dict rozšířen o všech 10 size sloupců (5 řidič + 5 spolujezdec). Předtím se tyto údaje v app sbíraly, ale do DB nezapsaly → Velín v detailu ukazoval prázdnou sekci Výbava pro app rezervace. Bez DB změn. |
| 2026-04-25 | **NEW: Redesign rezervačního formuláře (web) + dotažení passenger gear sizes do RPC:** 1) **Frontend (`motogo-web-php`)**: `pages-rezervace.js` přepsán na premium UI — 4 sekce s číslovanými kroky a zaoblenými kartami, krásný time-card s rychlými chips (08/09/10/12/14/16/18 h), lokační karty pro vyzvednutí + přistavení (s tooltipy), 4 gear-cards (výbava řidiče zdarma, výbava spolujezdce +690 Kč, boty řidič/spolujezdce +290 Kč), každá s rozbalovacím panelem a velikostními chips (XS-XXL pro výbavu, 36-46 pro boty). Stav v `MG._rez.sizes.{rider,passenger}`, persistence v sessionStorage. Při zaškrtnutí přistavení se automaticky otevře rider gear panel. „Mám vlastní výbavu" toggle vyčistí rider sizes. Pole poznámka schované jako hidden input (zůstává prázdné — velikosti jdou strukturovaně přes RPC). CSS `pages.css` + 130 řádků nové sekce „RESERVATION FORM v2 — premium UI" (gear-grid, size-chips, rez-time-card, rez-loc-card). 2) **SQL — rozšíření `create_web_booking` (spuštěno ručně mimo gitu):** `DROP FUNCTION public.create_web_booking(uuid, timestamptz, timestamptz, text, text, text, text, text, text, text, text, time, text, text, jsonb, numeric, text, text, uuid, text, text, text, text, text, text, text);` + nová verze 30-parametrová s 4 novými parametry `p_passenger_helmet_size, p_passenger_jacket_size, p_passenger_gloves_size, p_passenger_boots_size`. INSERT do `bookings` rozšířen o sloupce `passenger_helmet_size, passenger_jacket_size, passenger_gloves_size, passenger_boots_size` (kalhoty u spolujezdce nejsou v základní výbavě). 3) **Kompatibilita:** Flutter app NEPOUŽÍVÁ `create_web_booking` RPC — dělá přímý `.from('bookings').insert(...)` v `payment_screen.dart:151-178`, takže SQL migrace ji nemění. **Známý bug v Flutter app** (existoval už dřív, dnes nezpůsoben): app sbírá gear sizes do `BookingDraft` modelu, ale při insertu je zahazuje — pole zůstávají v DB null pro app rezervace. K opravě v samostatném tasku. 4) **Velín**: `BookingSummary.jsx:88-112` už zobrazuje všech 10 velikostních polí (řidič 5 + spolujezdec 5) + kompletní rozpis (pickup_time, return_time, pickup/return_address, extras, discount_amount, delivery_fee, deposit, insurance_type) — beze změny, vše už hotové z 2026-04-11/22. |
| 2026-04-25 | **FIX: Web rezervace — `create_web_booking` PGRST203 (ambiguous function):** V DB byly současně dvě overloady `create_web_booking` — stará 20-parametrová (z 2026-03-29) a nová 25-parametrová s driver gear sizes (z 2026-04-11). Frontend (`pages-rezervace-steps.js:109`) posílá 19 named parametrů → obě varianty byly validní candidate funkce → PostgREST hlásí `Could not choose the best candidate function`. **SQL (mimo gitu — spuštěno ručně):** `DROP FUNCTION public.create_web_booking(uuid, timestamptz, timestamptz, text, text, text, text, text, text, text, text, time, text, text, jsonb, numeric, text, text, uuid, text, text);` — odstraněna stará 20-param overload (s `p_password` jako posledním parametrem, bez gear sizes). V DB zůstala jediná verze (26 parametrů: 20 původních + p_password + 5 driver gear sizes). **Reconciliation se state soubory:** opraven popis v `SUPABASE_BACKEND_STATE_3.md` — popsané rozšíření o `p_booking_source` (changelog 2026-04-12) a o passenger gear sizes (changelog 2026-04-11) byly **pouze v migračních souborech, fakticky se na DB nedeploynuly**. Reálná funkce nemá ani `p_booking_source` (booking_source je v INSERT hardcoded `'web'`), ani 5 passenger size parametrů. Bez kódových změn ve frontend/backend. |
| 2026-04-24 | **FIX: motogo-web-php — stránka `/jak-pujcit/postup` zobrazovala 12 kroků se špatnými ikonami (CMS override v `app_settings`):** Defaults v `pages/jak-pujcit-postup.php` jsou správné (8 kroků s `gfx/ico-step1.svg`..`ico-step8.svg`: Vyber motorku → Počet jezdců → Rezervace online → Výbava v ceně → Potvrzení a platba → Převzetí motorky → Užij si jízdu → Vrácení motorky), ale `siteContent()` deep-merguje DB obsah přes defaults a `steps` je číselný list, takže se celý přepisoval 12-krokovým paskvilem z DB. **SQL (mimo gitu — spuštěno ručně, 1 řádek ovlivněn):** `DELETE FROM app_settings WHERE key = 'site.jak_pujcit_postup';` — override odstraněn, použijí se PHP defaults. Bez schema změn, bez PHP změn. |
| 2026-04-24 | **NEW: motogo-web-php — UI/UX sjednocení s Flutter/Velín theme + rozšířený katalog filtr + blog fallback + reviews + availability badge (bez DB změn v kódu, čeká na SQL migraci níže):** 1) **CSS theme sjednoceno** s Flutter (`motogo-app-flutter/lib/core/theme.dart`) a Velínem (`velin/tailwind.config.js`): dark `#0b0b0b/#1a1a1a/#111` → `#1a2e22` (tmavě zelená), light body `#f5f5f5` → `#dff0ec` (mint gray-green), border `#e0e0e0` → `#d4e8e0`, text secondary `#555/#666` → `#4a6b5a`, muted `#999` → `#8aab99`. Font `Jost` → `Montserrat` (matching Flutter + Velín). Rezervační JS (pages-rezervace*.js) a `.gv-*` voucher form styles **záměrně nezměněny** (rezervace krok 1/2/platba dle pokynu beze změny). 2) **Rozšířený katalog filtr** `pages/katalog.php`: fulltext search (`?q=`), kategorie, ŘP skupina, výkon kW min (11/35/70/100), cena max Kč/den (1000/1500/2000/3000), ABS checkbox, 2 osoby radio, řazení (cena/výkon asc/desc). Form-based GET, funguje i bez JS. Nové třídy `.katalog-filters`, `.filter-field`, `.filter-check`. 3) **Detail motorky + karty** (`pages/katalog-detail.php`, `components.php`): real-time availability check přes `get_moto_booked_dates` RPC → "Dostupné dnes" zelený badge / "Dnes obsazeno" žlutý warning; branch info "📍 Místo vyzvednutí: <název> · <adresa>" pod H1; na kartě v katalogu malý badge "Dostupné" vpravo nahoře + pobočka + ABS feature. 4) **Blog fallback** `pages/blog_fallback.php` (nový): funkce `getBlogFallbackPosts()` vrací 5 plnohodnotných článků (Top motorkářské trasy Vysočina, Jak si půjčit motorku na víkend, Tipy pro bezpečnou jízdu, Nové motorky 2026, Dárkový poukaz nápady) s HTML body, tagy, obrázky. Použije se když `cms_pages` v DB prázdná; také v sitemap.xml. `blog-detail.php` umí také single view fallback po slug. 5) **Reviews sekce na homepage** (`pages/home.php`): nový helper `SupabaseClient::fetchPublicReviews($limit)` čte `reviews` tabulku (rating, comment, author_name) s cache. Sekce se vykreslí pouze pokud data existují — karty s hvězdičkami, italic komentář, autor. CSS `.review-card`. 6) `components.php`: `renderBlogCard()` podporuje relativní cesty `/gfx/...` pro fallback obrázky. **SQL migrace pro cms_pages (pošle se mimo git dle CLAUDE.md pravidel — uvedená v chatu):** `ALTER TABLE cms_pages ADD COLUMN IF NOT EXISTS excerpt TEXT, ADD COLUMN IF NOT EXISTS image_url TEXT, ADD COLUMN IF NOT EXISTS images TEXT[], ADD COLUMN IF NOT EXISTS tags TEXT[], ADD COLUMN IF NOT EXISTS published BOOLEAN DEFAULT true;` + seed 5 blog článků. Bez dalších SQL změn. |
| 2026-04-23 | **NEW: motogo-web-php — kompletně CMS-driven (bez DB změn, jen nová konvence klíčů v `app_settings`):** 1) Nová metoda `SupabaseClient::siteContent($page, $defaults)` v `supabase.php` čte JSONB hodnotu z `app_settings.value` pro klíč `site.<page>` a rekurzivně ji mergeuje s `$defaults`. Asociativní pole se mergují hlouběji, číselné seznamy se přepisují celé. `fetchSetting()` nyní cacheuje i null hodnoty (méně dotazů). 2) Nová funkce `sanitizeHtml($html, $allowIframe=false)` v `components.php` — strip `<script>`, `<style>`, `<iframe>` (pokud není povolen), `on*=` event atributy, `javascript:`/`vbscript:`/`data:` v `href`/`src`. Aplikováno v `blog-detail.php` a `cms.php`. 3) Přepsáno 11 stránek na CMS-driven pattern s fallback defaults (identický obsah jako předtím, ale každý text editovatelný přes DB): `home.php`, `pujcovna.php`, `jak-pujcit.php`, `jak-pujcit-postup.php`, `jak-pujcit-cena.php`, `jak-pujcit-pristaveni.php`, `jak-pujcit-vyzvednuti.php`, `jak-pujcit-dokumenty.php`, `poukazy.php`, `kontakt.php`, `faq.php`. 4) Optimalizace: `layout.php` přidává `<link rel="preconnect">` na SUPABASE_URL a automaticky preloaduje hero banner (`/gfx/hero-banner.png`, `as=image`, `fetchpriority=high`) na homepage (LCP). Stránky mohou předat vlastní `preload` pole v meta. 5) `home.php` hero používá `<picture>` s `image/webp` source (fallback PNG) pro budoucí WebP verzi banneru. **Bez SQL změn, bez migrací.** Konvence klíčů v `app_settings.value`: `site.home`, `site.pujcovna`, `site.jak_pujcit`, `site.jak_pujcit_postup`, `site.jak_pujcit_cena`, `site.jak_pujcit_pristaveni`, `site.jak_pujcit_vyzvednuti`, `site.jak_pujcit_dokumenty`, `site.poukazy`, `site.kontakt`, `site.faq` — každý JSONB objekt se strukturou dle fallback defaults v odpovídajícím PHP souboru (sekce `seo`, `h1`, `intro`, `steps`, `faq`, `cta` apod.). |
| 2026-04-22 | **FIX: Fakturace při úpravě rezervace (ZF/DP/dobropis + platba kartou + rozepsané extras):** 1) **`generate-invoice` edge:** a) Nový parametr `source` (`'booking'` default / `'edit'`) — odstraní dedup blokaci, každá úprava dostane vlastní ZF+DP. b) **Splatnost ihned** (due_date = issue_date) — dříve +14 dní, nesmyslné u karty. c) Položkový rozpis příslušenství: každý booking_extras řádek zvlášť s velikostí (párování `helma/bunda/kalhoty/boty/rukavice × řidič/spolujezdec` s bookings `*_size` sloupci; multi-item „Výbava spolujezdce" rozepsán na komponenty). Dříve jediná sloučená položka. d) Platba kartou — načte `payment_methods` (brand+last4) přes `user_id` když `stripe_payment_intent_id` není NULL; platební údaje zobrazí „VISA **** 4242, Uhrazeno online přes Stripe" místo bankovních údajů. e) Edit flow: vytvoří jedinou rozdílovou položku z posledního záznamu `bookings.modification_history[].price_diff`, titul „ZÁLOHOVÁ FAKTURA — ÚPRAVA — prodloužení o N dní". f) Dobropis (credit_note) se nevystavuje přes tuto funkci — pro zkrácení se volá `process-refund` (Stripe refund → credit_note). 2) **`send-booking-email` `booking_modified`:** rozhoduje podle `price_difference` — `>0` nová rozdílová ZF+DP (source='edit'), `<0` přiloží existující dobropis z `process-refund`, `===0` pouze smlouva+VOP. Předmět: „Úprava rezervace č. X — doplatek Y Kč" / „vrácení platby". 3) **Velín `BookingModifyModal.jsx`:** při zkrácení (priceDiff < 0 && stripe_payment_intent_id && chargeCustomer) automaticky volá `process-refund` s `reason: 'shortening'` — Stripe refund + credit_note (DB-) + negativní účetní záznam. 4) **Velín `BookingSummary.jsx`:** Příslušenství v souhrnu i sekci Platba nyní ukazuje velikosti („Boty řidič (vel. 43)") — nový lokální helper `matchExtraSize()`. 5) **SQL (mimo gitu — spustit ručně):** `ALTER TABLE invoices DROP CONSTRAINT IF EXISTS invoices_type_check; ALTER TABLE invoices ADD CONSTRAINT invoices_type_check CHECK (type IN ('issued','received','final','proforma','shop_proforma','shop_final','advance','payment_receipt','credit_note'));` — formalizuje už používaný typ `credit_note`. Bez dalších DB schema změn. Affected: `generate-invoice/{index,template}.ts`, `send-booking-email/index.ts`, `BookingModifyModal.jsx`, `BookingSummary.jsx`. |
| 2026-04-22 | **FIX: Skenování OP/ŘP — per-side mapování polí (bez přepisování z druhé strany):** 1) **Flutter `document_provider.dart` — přepracovaný `saveOcrToProfile(result, docType, stepKey)`** — nový parametr `stepKey` (`id_front`/`id_back`/`passport_front`/`passport_back`/`dl_front`/`dl_back`) řídí, která pole se z které strany ukládají do `profiles`. Klíčové opravy: a) **ŘP zadní strana** už NEPŘEPISUJE `license_expiry` ani `license_verified_until` — ta jsou výhradně z přední strany. Zadní strana pouze kontroluje `license_number` (warning při neshodě) a dělá **průnik skupin** (redukce skupin z přední strany, pokud zadní vrací méně). b) **OP/pas přední strana** ukládá `id_number` + platnost do správného sloupce — pro OP `id_verified_until` (dříve se chybně ukládalo do `license_expiry`/`license_verified_until`), pro pas `passport_verified_until`. c) **OP/pas zadní strana** ukládá JEN `street`/`city`/`zip` (adresa trvalého bydliště). Neshoda čísla ŘP se vrací jako konkrétní uživatelská hláška místo generické chyby. 2) **`mindee-parser.ts`** — rozšířena extrakce polí: a) Address parser rozloží jednořádkovou českou adresu "Ulice 123/45, 130 00 Praha 3" na `street`/`city`/`zip` přes novou funkci `parseCzechAddress()`. Zkouší nejdřív strukturovaná pole (`street`, `city`, `postal_code`, …) a fallback na parsing. b) `expiry_date` hledá i varianty `date_of_expiry`, `valid_until`, `validity_end`, `end_of_validity`. `issue_date` hledá i `valid_from`, `date_of_issue`. 3) **`document_scanner_screen.dart`** — a) `_hasRequiredFieldsForStep` zmírněna: OP zadní povoluje úspěch i při jménu/MRZ (nejen adrese), ŘP zadní stačí skupiny. Tím už uživatel neuvízne, když Mindee adresu nerozpozná. b) Per-step guidance a error hlášky upraveny: OP přední tip přidává „číslo dokladu a platnost", OP zadní zjednodušen na „stačí adresa", ŘP zadní popisuje pouze potvrzení skupin. c) `saveOcrToProfile` volán se `stepKey: step.key`. d) Save warning (neshoda čísla ŘP apod.) se zobrazí přímo v overlay jako „⚠️ <text>" místo generického i18n klíče. 4) **Lokální `_parseCzechAddress()` v Flutter provideru** — stejná fallback logika jako v edge, pro případy kdy už Mindee vrátil jen `address` bez rozložení. 5) Bez DB změn. Affected: `document_provider.dart`, `document_scanner_screen.dart`, `mindee-parser.ts`. |
| 2026-04-22 | **FIX: Výbava spolujezdce — boty se ptaly 2× + velikosti při přistavení povinné:** 1) **Passenger gear dialog** (`booking_size_dialogs.dart` + dead-code duplikát v `router.dart`) — odebrána položka „Boty" z dialogu „Výbava spolujezdce". Boty spolujezdce se nyní ptáme JEN když uživatel zvolí samostatný extra „Boty spolujezdce" (`extra-boty-spolu`). Dialog také vyžaduje vyplnění VŠECH 4 zbývajících velikostí (Helma, Rukavice, Bunda, Kalhoty) před aktivací tlačítka POTVRDIT. 2) **Nová rezervace** (`booking_form_widget.dart`): přidán helper `_missingGearSizes()` + varovný banner + blokace tlačítka „POKRAČOVAT K PLATBĚ", pokud pickup_method = delivery (přistavení) a chybí velikosti. Požadavky: řidič helma/rukavice/bunda/kalhoty vždy; boty řidič jen při extra-boty-ridic; výbava spolujezdec při extra-spolujezdec; boty spolujezdec při extra-boty-spolu. 3) **Úprava rezervace** (`reservation_edit_screen.dart`): stejná validace + varovný banner + blokace tlačítka „Uložit změny" — `_missingGearSizes()` kontroluje driver sady + passenger sady podle `_selectedExtras`. Uložení také blokováno v `_save()` s toast zprávou. Bez DB změn. |
| 2026-04-22 | **FIX: Úprava rezervace v app — aktivace tlačítka Uložit při změně času + defaulty 09:00/19:00:** 1) **NEW: `bookings.return_time`** (TEXT DEFAULT NULL) — čas vrácení motorky HH:MM. Doplňuje existující `pickup_time`. 2) **Flutter app `reservation_edit_screen.dart`:** opraven `EditPriceCalc.hasChanges` — nově detekuje změnu `pickupTime` i `returnTime` oproti booking datům, takže tlačítko Uložit se aktivuje i při změně pouze časů (ve všech typech úprav: aktivní prodloužení, zkrácení, nadcházející + kombinace). Defaulty: 09:00 přistavení, 19:00 vrácení. Časy se načítají z DB při otevření edit screen a ukládají se do `bookings.pickup_time` / `bookings.return_time` při Uložit. 3) `Reservation` model rozšířen o `returnTime` field (čteno z `return_time` JSON). |
| 2026-04-21 | **NEW: Mapy.cz API integrace napříč všemi flow (frontend only, bez DB změn):** 1) Nový Vel&iacute;n lib modul `velin/src/lib/mapyCz.js` — centralizovaný klient pro Mapy.cz API (Suggest, Geocode, Rgeocode, Routing, tile URL, embed URL, Haversine fallback). API key: `whg1ilj203oYhmsqkBHVtUqpk-tYr0E-HFTx4lGdue0`. 2) Nová komponenta `velin/src/components/shared/MapyAddressAutocomplete.jsx` — reusable autocomplete nad Mapy.cz Suggest API (debounce 220ms, šipky, Escape, kliknutí). 3) Vel&iacute;n úpravy: `BookingMapPicker` (Leaflet + Mapy.cz tiles + rgeocode), `BookingDeliverySection` (autocomplete pro pickup + return při úpravě rezervace), `MotoMap` (Mapy.cz tiles, markery, odkazy), `SOSDetailInfoCards` (autocomplete pro Adresa servisu, MapCard s Mapy.cz tiles), `SOSIncidentCard` (Mapy.cz tiles v SOSMap, odkazy), `SOSTimeline`, `DetailTabSections`, `BookingSummary`, `BranchDetailModal` — všechny `https://maps.google.com/...` odkazy nahrazeny `mapyLinkUrl`. 4) Flutter app (motogo-app-flutter): nahrazen starý Mapy.cz API klíč `Ag9d2QJD0i8_fA07r6GDDaZ4qV9aZDGMhWn_HhQ_rFs` → `whg1ilj203oYhmsqkBHVtUqpk-tYr0E-HFTx4lGdue0` v 7 souborech (`address_autocomplete_field.dart`, `booking_address_widgets.dart`, `booking_ui_helpers.dart`, `map_launcher.dart`, `price_calculator.dart`, `widgets/map_picker.dart`, `widgets/address_picker.dart`). Nominatim zůstává pouze jako fallback. 5) Web PHP (motogo-web-php): `pages-rezervace-pricing.js` — nové helpery `MG._mapyRgeocode`, `MG._mapySuggest`, `MG._attachMapyAutocomplete` (autocomplete pro `rez-delivery-address` + `rez-return-address` s MutationObserver pro dynamicky vytvořené formuláře), web map picker používá Mapy.cz tile layer + rgeocode místo Nominatim/Carto. `kontakt.php` + `jak-pujcit-vyzvednuti.php` — Google Maps embed nahrazen `frame.mapy.cz` iframe. 6) **Všechny flow pokryté:** SOS incidenty (zobrazení polohy, autocomplete adresy servisu), úprava aktivní/nadcházející rezervace (autocomplete + map picker pro pickup + return), rezervovat (nová rezervace v app přes Flutter), web rezervace (obě adresní pole). |
| 2026-04-21 | **FIX: Sjednocení čísel rezervací napříč kanály:** Rezervační číslo se nyní všude formátuje jednotně jako `LAST 8 chars of booking UUID → toUpperCase()`. Vel&iacute;n i Flutter app tuto logiku měly už dříve (`id.slice(-8).toUpperCase()` / `id.substring(id.length - 8).toUpperCase()`), ale edge funkce používaly `slice(0, 8)` (prvních 8 znaků), což vedlo k rozdílu mezi ID v detailu rezervace a ID v e-mailu/smlouvě. Opraveno v: `send-booking-email` (booking_number + order_number + filenames), `send-cancellation-email`, `generate-document` (booking_id+booking_number pro {{booking_number}} placeholder ve smlouvách/VOP/protokolech), `process-payment/payment-flows` (Stripe produkt popis), `webhook-receiver/payment-confirmers` (attachment filenames + order_number fallback), `ai-moto-agent/booking-context`. Vel&iacute;n: sjednoceny i 4 zbývající místa na last-8 (`TemplatesTab`, `GeneratedTab`, `SOSDetailInfoCards.BookingCard`, `InvoiceCreateModal`). **NOVĚ:** ZF (zálohová faktura) / DP (doklad přijaté platby) / KF (konečná faktura) z `generate-invoice` nyní zobrazují v hlavičce i v průvodním emailu rezervaci `#XXXXXXXX` (přes nový `bookingNumber` parametr do `generateInvoiceHtml` + `generateEmailHtml`). |
| 2026-04-21 | **FIX: Door codes — validita ŘP vs. end_date + rozlišené withheld_reason + Mindee pipeline robustnost:** 1) **NOVÁ RPC `check_booking_docs_status(p_user_id, p_end_date)`** — centralizovaná kontrola dokladů pro konkrétní rezervaci. Vrací NULL pokud OK, jinak konkrétní důvod: `Chybí doklady (OP/pas/ŘP)`, `Chybí doklad totožnosti (OP/pas)`, `Chybí ŘP`, `Neúplné údaje ŘP (chybí číslo)`, `ŘP propadlý DD.MM.YYYY`. STABLE, SECURITY DEFINER. 2) **Přepracovány `auto_generate_door_codes`, `release_withheld_door_codes_for_user`, `regen_door_codes_on_moto_change`, `release_my_door_codes`** — všechny teď používají `check_booking_docs_status` proti `b.end_date::date`. Už nepošleme kódy k půjčce na motorku, když ŘP propadl před koncem rezervace. 3) **`release_withheld_door_codes_for_user`** teď neomezeně NEUVOLNÍ kódy s propadlým ŘP — místo toho jen aktualizuje `withheld_reason` (např. "Chybí doklady (OP/pas/ŘP)" → "ŘP propadlý 20.07.2023"), takže admin ve Velínu vidí přesný problém. 4) **Flutter app fixes** (commit `flutter/document_provider.dart`, `document_scanner_screen.dart`, `documents_screen.dart`): a) `uploadDocPhoto` vrací `DocUploadResult{markerPath, errorDetail}` místo null-tiché chyby, scanner se na selhání DB zápisu chová stejně jako na OCR failure (retry + contact fallback). b) `saveOcrToProfile` nastavuje `license_verified_at` POUZE když OCR extrahoval číslo ŘP, platnost nebo skupinu. Předtím stačilo že typ=DL → profil měl "ověřeno" i pro nečitelné sken. c) Scanner: per-step retry counter (`_attempts` Map), po 3. selhání stejného kroku zobrazí `_contactFallbackOverlay` s CTA na `tel:+420774256271` a `mailto:info@motogo24.cz?subject=Ověření dokladů — <krok>`. d) Per-step validace `_hasRequiredFieldsForStep` — ŘP musí mít číslo/platnost/skupinu, OP/pas přední musí mít jméno nebo číslo, OP/pas zadní musí mít adresu. e) Per-step konkrétní guidance („Nafoťte znovu PŘEDNÍ stranu OP, musí být vidět jméno, rodné číslo...") nahrazuje generický tip. 5) Použité konstanty v app: `_kSupportPhone`, `_kSupportEmail`, `_kMaxAttemptsPerStep=3`. |
| 2026-04-16 | **FIX: KF rozpis položek + email "booking_completed":** 1) `generate_final_invoice_on_complete()` rozepisuje položky konzistentně se ZF/DP: Pronájem (brutto = total_price + discount_amount − extras − delivery), Příslušenství, Přistavení, Sleva (záporná, s kódem), Odpočty všech zaplacených DP. Dříve KF zobrazoval jen jedinou položku s `NEW.total_price` — po modifikaci rezervace (prodloužení) se ztratila sleva a celková částka nesouhlasila s DP. 2) **NOVÝ trigger `trg_send_booking_completed_email`** na `invoices` (AFTER INSERT WHEN type='final') — volá edge funkci `send-booking-email` přes `pg_net` s `type='booking_completed'`. Zákazník tak dostane e-mail s poděkováním za využití služeb a KF v příloze (generuje autoGenerateAttachments). Dedup přes `message_log` (slugy `booking_completed`/`web_booking_completed`). SECURITY DEFINER, EXCEPTION safe. |
| 2026-04-14 | **UPD: Door codes — email do textu mailu + opravený FCM channel:** 1) `send-booking-email` načítá aktivní uvolněné kódy z `branch_door_codes` a vystavuje `{{door_code_moto}}`, `{{door_code_gear}}`, `{{door_codes_block}}` pro DB šablony i fallback HTML. 2) Nový email type `door_codes` s vlastní fallback šablonou + subject (`Přístupové kódy k pobočce — rezervace č. X`). 3) Helper RPC `send_door_codes_email(booking_id, user_id)` — posílá email přes pg_net s dedup přes message_log. Volá se z `auto_generate_door_codes`, `release_withheld_door_codes`, `regen_door_codes_on_moto_change`, `release_my_door_codes`. 4) `send-push` edge — opraven `channel_id` z `motogo_door_codes` na existující `motogo_notifications` (Flutter AndroidManifest default) — bez tohoto pushy na Androidu padaly do defaultního channelu bez zvuku/ikony. 5) Pokrytí kódů: detail rezervace ✓, in-app zprávy ✓, FCM push ✓, SMS ✓, WhatsApp ✓, email (text + PDF faktury) ✓. |
| 2026-04-14 | **NEW: Door codes — uvolnění po nahrání dokladů + přegenerování při změně motorky + FCM push:** 1) **Trigger funkce `release_withheld_door_codes()`** + trigger `trg_release_codes_on_doc_upload` (documents AFTER INSERT, type IN id_card/passport/drivers_license/id_photo/license_photo): při nahrání dokladů zkontroluje doklady (Mindee + fallback) a uvolní zadržené kódy pro všechny aktivní bookings uživatele, posílá `door_codes` admin_message + SMS/WA. 2) **Trigger funkce `regen_door_codes_on_moto_change()`** + trigger `trg_regen_codes_on_moto_change` (bookings AFTER UPDATE OF moto_id, status IN active/reserved): deaktivuje staré kódy a vygeneruje 2 nové pro novou motorku, notifikuje zákazníka. 3) **RPC `send_push_via_edge(user_id, title, body, data)`** + trigger `trg_push_on_admin_message` na admin_messages — posílá FCM push přes nově vytvořenou edge function `send-push`. 4) **NOVÁ Edge Function `send-push`** — FCM HTTP v1 API (Firebase service account JWT → OAuth2 → FCM), Android channel `motogo_door_codes`, APNS payload, auto-deaktivace invalid tokenů. Auth jen service_role. Vyžaduje secrets `FCM_PROJECT_ID` + `FCM_SERVICE_ACCOUNT_JSON` (base64). 5) **RPC `release_my_door_codes(p_booking_id)`** — pro authenticated zákazníka (GRANT EXECUTE TO authenticated): ověří doklady a uvolní kódy pro vlastní booking, vrací jsonb {success, released, error?}. Volá appka po platbě. 6) **Update `auto_generate_door_codes()`** — admin_message používá nyní type `door_codes` (místo `info`) → správná ikona + routing v appce. 7) `admin_messages.type` je TEXT bez CHECK constraintu — hodnota `door_codes` je jen přidaná do používaných hodnot. 8) DB nastavení `app.settings.supabase_url` + `app.settings.service_role_key` pro pg_net volání z triggerů. |
| 2026-04-12 | **FIX: Rezervace z app se ukládaly jako booking_source='web':** 1) `create_web_booking()` rozšířena o parametr `p_booking_source` (text, default 'web') — app předává 'app', web ponechává default. INSERT používá `COALESCE(p_booking_source, 'web')`. 2) **NOVÁ funkce `auto_cancel_expired_pending()`** — chyběla v DB, cron job ji volal bez efektu. Ruší pending+unpaid: app po 10 min, web po 4h. SECURITY DEFINER. 3) Flutter app: `_createDraftBooking()` předává `p_booking_source: 'app'`. 4) Flutter app: `displayStatus` skrývá stale pending+unpaid (>10 min) jako cancelled. 5) Velín `autoCancelStale()`: oprava filtru `.neq('web')` → `.eq('app')`. |
| 2026-04-11 | **NEW: Velikosti výbavy pro řidiče + spolujezdce v bookings:** 1) Nové sloupce v `bookings`: `pants_size`, `gloves_size` (řidič) + `passenger_helmet_size`, `passenger_jacket_size`, `passenger_pants_size`, `passenger_boots_size`, `passenger_gloves_size` (spolujezdec) — vše TEXT, nullable. 2) `create_web_booking()` rozšířena o parametry pro obě sady (řidič: p_helmet_size/jacket/pants/boots/gloves_size, spolujezdec: p_passenger_helmet/jacket/pants/boots/gloves_size). 3) Velín: BookingSummary v detailu rezervace zobrazuje dvě sekce — „Výbava — řidič" a „Výbava — spolujezdec", každá s 5 položkami (Helma, Bunda, Kalhoty, Boty, Rukavice). Sekce se zobrazí jen pokud je vyplněna alespoň jedna velikost. |
| 2026-03-30 | **NEW: SOS → URGENT servisní záznamy:** 1) Nový sloupec `maintenance_log.sos_incident_id` (UUID FK→sos_incidents ON DELETE SET NULL) — propojení servisního záznamu s SOS incidentem. 2) Index `idx_maintenance_log_sos_incident`. 3) Velín: při „Motorka do servisu" z SOS incidentu se automaticky vytváří URGENT maintenance_log s `is_urgent=true`, `sos_incident_id`, `km_at_service`. 4) Nový komponent `SOSServiceCard` v SOS detailu — rozšířený servisní formulář se stejným UX jako flotilový checklist: typ SOS události (těžká/lehká nehoda, porucha, krádež), standardní servisní checklist, datumy, popis. Editovatelný kdykoliv během řešení incidentu. |
| 2026-03-30 | **NEW: Dobropisy (credit notes) + automatické Stripe refundy:** 1) Nový typ faktury `credit_note` (prefix DB-YYYY-NNNN) pro dobropisy. 2) Nové sloupce `invoices.original_invoice_id` (FK→invoices) a `invoices.stripe_refund_id` (TEXT). 3) `process-refund` edge funkce rozšířena: po úspěšném Stripe refundu automaticky vytváří dobropis (credit_note invoice) + záporný účetní záznam (accounting_entry category=refund). 4) Velín BookingDetail: při zrušení zaplacené rezervace automaticky volá process-refund (Stripe) + generuje dobropis. Badge DOBROPIS u rezervace. 5) Velín BookingPaymentsTab + BookingDocumentsTab: dobropisy zobrazeny červeně s −částkou. 6) Finance: nový tab „Dobropisy" (CreditNotesTab) se statistikami, seznamem dobropisů a vrácených plateb. 7) InvoicesTab + FinancePrehledTab: podpora credit_note typu (červené zobrazení). 8) invoiceTemplate: červený design pro dobropisy s textem „DOBROPIS / Credit note" a „Celkem k vrácení". |
| 2026-03-30 | **FIX: Finanční události — auto-fill detailů ze Stripe:** webhook-receiver rozšířen o enrichment metadata při payment_intent.succeeded: supplier_name/ico/bank_account (firma), invoice_number/variable_symbol (z invoices tabulky), customer_name, booking_model/spz/dates, payment_method, due_date. Stejně pro charge.refunded — linkuje booking přes stripe_payment_intent_id. |
| 2026-03-30 | **FIX: Přístupové kódy — oprava doručení do app zpráv:** 1) `auto_generate_door_codes()` používala špatný sloupec `content` místo `message` při INSERT do `admin_messages` → kódy se v app zprávách zobrazovaly s prázdným textem (nebo INSERT selhal tiše). Opraveno na `message`. 2) Velín `BranchDoorCodes.jsx` resend: stejný bug `content`→`message`. 3) AI Copilot `tools-write-core.ts`: `content`→`message: content`. Flow: kódy se generují POUZE při aktivaci (status→active), doklady musí být ověřené. Budoucí rezervace: kódy přijdou v den začátku (cron 00:01), DP bez kódů je správné chování. |
| 2026-03-29 | **FIX+NEW: Web rezervace — Stripe amount mismatch + heslo + Google autofill + resume:** 1) **FIX ceníku:** `create_web_booking()` přepočítává cenu per-day (price_mon..price_sun) s inkluzivním počítáním dnů (start+end), shodně s frontendem. Předchozí verze používala `price_weekday × EXTRACT(DAY)` což dávalo jinou částku než web. 2) **Heslo v kroku 2:** Nové pole heslo + potvrzení hesla ve step 2. Heslo se uloží do `auth.users.encrypted_password` přes novou RPC `set_web_booking_password()`. Heslo slouží pro editaci rezervace i budoucí přihlášení do app. 3) **Stripe cancel_url:** Nyní vede na `?resume=BOOKING_ID` místo čisté `/rezervace` — po kliknutí zpět ze Stripe zůstane vše předvyplněné z DB. 4) **Google autofill:** Přidány `name` atributy ke všem input polím formuláře pro lepší podporu Google/browser autofill. 5) Nová RPC `set_web_booking_password(p_booking_id, p_password)` — SECURITY DEFINER. 6) `create_web_booking` rozšířen o `p_password` parametr. |
| 2026-03-29 | **NEW+FIX: create_web_booking — deduplikace zákazníků + kontrola ŘP:** 1) Kompletní reimplementace `create_web_booking()` RPC. 2) **Deduplikace:** hledá existujícího zákazníka podle emailu v auth.users — pokud existuje, přiřadí booking k němu a aktualizuje profil (adresa, telefon). Nového zákazníka vytvoří jen pokud email neexistuje. 3) **Kontrola ŘP skupiny:** ověřuje hierarchii A > A2 > A1 > AM, B = AM. Motorky s license_required='N' nevyžadují ŘP. 4) **Web formulář:** přidány skupiny AM a B do selectu, `p_license_group` se předává do RPC. 5) Opraveny názvy sloupců: `notes` (ne `note`), `price_weekday` (ne `price_per_day`), `pickup_address` pro doručení. |
| 2026-03-29 | **NEW: Auto-generování dárkových poukazů z webu (kompletní flow):** 1) Web formulář (`pages-poukazy.js`) rozšířen o předávání telefonu, adresy, isPrint, voucher_amount do process-payment. 2) `handleWebShopCheckout` ukládá customer_phone, shipping_address, oddělené položky (Dárkový poukaz + Tisk a poštovné), generuje ZF (proforma) + odesílá email se shrnutím objednávky. 3) `confirmShopPayment` po Stripe platbě: generuje DP (doklad přijaté platby), načítá vygenerované voucher kódy, odesílá email `voucher_purchased` s kódem + hodnotou + platností. 4) Trigger `trg_generate_shop_final_on_ship` opraven: přidán `payment_status` do OF clause → FV pro elektronické poukazy se generuje okamžitě (auto_process_voucher_order nastaví status='delivered'). Fyzické položky: FV až při odeslání z velínu. 5) Potvrzovací stránka (`pages-potvrzeni.js`) rozšířena o podporu shop objednávek — zobrazuje kódy poukazů, order_number, polling shop_orders. 6) Kompatibilita s app zachována — stávající triggery a flow bez změn. |
| 2026-03-27 | **NEW: QR kód + Mindee OCR + resume flow pro web rezervace:** 1) Step 2 (doklady): QR kód pro přesměrování na mobil (desktop only) — Apple Pay/Google Pay, sken dokladů, autonomní pobočka. 2) Mobilní web: Mindee OCR krok mezi doklady a Stripe bránou — fotoaparát → scan-document → auto-fill profilu + upload dokladů do storage. 3) Resume flow `?resume=BOOKING_ID` z QR kódu nebo abandoned emailu — nová RPC `get_web_booking_resume()` (SECURITY DEFINER). Mobil+docs → rovnou Mindee, desktop → step 2. 4) Abandoned email: QR kód + resume odkaz ve fallback šabloně, nová proměnná `{{resume_qr_url}}` pro DB šablony. 5) `send-booking-email` rozšířen o `resume_qr_url` variable + hardcoded abandoned fallback s CTA + QR. |
| 2026-03-28 | **NEW: registration_source v profiles:** Nový sloupec `profiles.registration_source` (TEXT DEFAULT NULL) — zdroj registrace: 'app' nebo 'web'. Velín: nový sloupec "Zdroj" v tabulce zákazníků + filtr v rozšířených filtrech. |
| 2026-03-28 | **FIX: process-payment — invalid enum value:** Edge funkce používala `.in('payment_status', ['pending', 'unpaid'])` ale enum `payment_status` nemá hodnotu `pending` (reálné hodnoty: unpaid, paid, refunded, partial_refund). Opraveno na `.eq('payment_status', 'unpaid')`. Opraven i backend state — enum `payment_status` aktualizován. |
| 2026-03-28 | **FIX: bookings.promo_code_id + jednodenní rezervace na webu:** 1) Přidány chybějící sloupce `bookings.promo_code_id` (UUID FK→promo_codes ON DELETE SET NULL) a `bookings.voucher_id` (UUID FK→vouchers ON DELETE SET NULL) — `create_web_booking()` RPC na ně odkazovala, ale sloupce neexistovaly. 2) Web kalendář: opravena jednodenní rezervace — klik na stejný den nyní nastaví start=end místo resetu výběru. |
| 2026-03-28 | **FIX: Delivery pricing + Stripe + address UI:** 1) Delivery fee: `1000 + km×2×20` (zpáteční cesta) místo `1000 + km×20`. 2) process-payment: `payment_method_types:['card']` místo `automatic_payment_methods` (fix Stripe 500 v appce), web shop `payment_status:'pending'`→`'unpaid'`, validace amount>0. 3) Opraveny dotazy s neexistujícím statusem `confirmed`. 4) `extras_catalog .single()`→`.maybeSingle()`. 5) Potvrzení adresy (checkbox) + výběr na mapě (fullscreen Leaflet) ve VŠECH adresních formulářích: rezervace pickup/return, úprava rezervace edit-pickup/edit-return, SOS replacement sos-repl, motogo-web, Velín BookingModifyModal. 6) `check_moto_availability()` opravena — přidán status `reserved` (nutné spustit SQL v Supabase). |
| 2026-03-27 | **FIX: create_web_booking — oprava extras parsing:** Opraven bug `record "v_extra" has no field "item"` — SQL funkce opravena na INSERT...SELECT s COALESCE pro oba formáty extras ({item,price} i {name,unit_price}). Frontend upraven na {name, unit_price}. |
| 2026-03-27 | **UPD: Katalog + Dárkové poukazy + Přistavení:** 1) Katalog: rozšířený filtr (kategorie + ŘP skupina), oprava "Podobné motorky" (filtr dle ŘP + kategorie, řazení dle relevance). 2) Dárkové poukazy: objednávkový formulář na /poukazy (libovolná částka, elektronický/tištěný, doručovací adresa, Stripe Checkout). 3) Přistavení: tlačítka předvyplní checkbox v rezervačním formuláři (?delivery=1). |
| 2026-03-27 | **REFACTOR: Email systém pro web rezervace:** 1) `send-booking-email` refaktorován — čte šablony z `email_templates` tabulky (editovatelné z Velína), fallback na hardcoded. Podpora `source` param (web/app) → hledá nejdřív `web_*` slug, pak generic. 2) `send-cancellation-email` refaktorován stejně. 3) Všechny email edge funkce: přidán `reply_to: info@motogo24.cz` pro odpovědi zákazníků. 4) Přidána vizitka (logo, QR, kontakty, IG, FB) do patičky všech emailů. 5) Kopie emailu automaticky na info@motogo24.cz. 6) Nové web-specifické šablony: `web_booking_reserved`, `web_booking_abandoned`, `web_booking_cancelled`, `web_booking_completed`, `web_voucher_purchased` — texty 1:1 dle požadavku klienta. 7) Aktualizovány app šablony (booking_cancelled, booking_completed, voucher_purchased). 8) Nová RPC `create_web_booking()` — vytvoří profil + booking pro webový formulář. 9) Nový trigger `trg_notify_web_booking_abandoned` — odešle abandoned email při auto-cancel web bookingu. 10) QR kód PNG přidán do motogo-web/gfx/. 11) Velín: aktualizovány sample proměnné v EmailTemplatesTab. |
| 2026-03-26 | **NEW: Konfigurovatelný AI agent pro zákaznickou appku:** 1) Nový klíč `ai_moto_agent_config` v `app_settings` (JSONB: persona_name, system_prompt, situations[], forbidden[], mustDo[], tone, max_tokens, enabled). 2) Velín: nový tab "App Agent" v AI Orchestrátoru s kompletním UI pro editaci promptu, pravidel a tónu. 3) Edge funkce `ai-moto-agent` dynamicky načítá konfiguraci z `app_settings` při každém požadavku. Fallback na hardcoded prompt pokud konfigurace neexistuje. |
| 2026-03-25 | **NEW: Biometrické přihlášení + souhlasy v profilu:** 1) Biometrika ukládá email+heslo (base64) do mg_bio_user, přístup jen přes otisk/Face ID. Auto-enable po login/registraci. Fallback: credential-based login když refresh_token expiruje. 2) Login předvyplní email z mg_saved_email. 3) Nové sloupce `profiles.consent_whatsapp` (BOOLEAN DEFAULT false) a `profiles.consent_contract` (BOOLEAN DEFAULT false). 4) Registrace: všechny souhlasy defaultně zaškrtnuté, ukládají se do DB (10 consent polí). 5) Booking form: odstraněny souhlasy VOP/GDPR (řídí se profilem). 6) Profil: Notifikace napojeny na DB (push/email/sms/wa/marketing). Soukromí a souhlasy — VOP, GDPR, data processing, smlouva, foto. 7) Velín: přidány consent_whatsapp + consent_contract. 8) Pobočky přesunuty pod Pomoc & Podpora. Web odkaz opraven na www.motogo24.cz. |
| 2026-03-25 | **FIX: Mindee OCR → door codes → notifikace:** 1) `auto_generate_door_codes()` opravena kontrola doc typů: nyní kontroluje `id_card/passport` + `drivers_license` (nové typy z Mindee), fallback na `id_photo/license_photo` (legacy) + `license_number`+`id_number` v profilu. 2) `trg_notify_door_codes()` opravena: neposílá SMS/WA pokud `sent_to_customer=false` (chybějící doklady). 3) Nový sloupec `profiles.id_number` (TEXT) pro číslo OP/pasu z OCR. 4) `verify_customer_docs()` RPC funkce přidána do migrací — cross-check OCR vs profil, kontrola platnosti ŘP vs datum rezervace, kontrola skupin. 5) Frontend: `apiUploadDocPhoto()` ukládá správné typy (`id_card`, `drivers_license`, `passport` místo `id_photo`/`license_photo`). 6) Frontend: `apiCheckLicenseForMoto()` nově kontroluje platnost ŘP proti datu konce rezervace. 7) Velín: `CustomerDocumentsTab` rozpoznává i legacy doc typy (`id_photo`, `license_photo`). 8) Velín: `DetailTab` zobrazuje přístupové kódy s dokladovým stavem (odesláno/zadrženo). |
| 2026-03-25 | **NEW: AI Training RPC funkce (kompletní):** `create_test_booking`, `update_test_booking_status`, `create_test_service_order`, `update_test_booking_fields` (universal field update), `create_test_sos_incident`, `update_test_sos_status`, `create_test_sos_timeline`, `create_test_maintenance_log`, `update_test_profile`, `cleanup_all_test_data` (maže i auth.users). Vše SECURITY DEFINER, bypass RLS. Triggery (KF, SMS, door codes) se spouští normálně. |
| 2026-03-24 | **NEW: AI Training Pipeline — is_test sloupce:** Přidán `is_test` (BOOLEAN DEFAULT false) do tabulek: `bookings`, `sos_incidents`, `service_orders`, `maintenance_log`, `promo_codes`. Přidán `is_test_account` (BOOLEAN DEFAULT false) do `profiles`. Partial indexy na všech is_test sloupcích (WHERE true). Účel: simulátor zákaznických procesů vytváří reálná data v DB, po tréninku agentů se smažou přes cleanup. |
| 2026-03-24 | **NEW: Dodací listy + Smlouvy + AI třídění dokladů:** 1) Nová tabulka `delivery_notes` (dl_number, supplier_name/ico, total_amount, items jsonb, AI matching: matched_invoice_id, match_method ai/manual, match_confidence, storage_path, extracted_data). 2) Nová tabulka `contracts` (contract_number, contract_type: rental/lease/service/insurance/employment/dpp/dpc/vacation_request/supply/nda/other, counterparty, amount, valid_from/until, status, employee_id FK→acc_employees). 3) `invoices.matched_delivery_note_id` (UUID FK→delivery_notes). 4) `financial_events.document_type` (TEXT: faktura_prijata/dodaci_list/zaloha/doklad_platby/smlouva/pracovni_smlouva/zadost_dovolena/objednavka/skladovy_doklad/pokladni_doklad/other). 5) Storage: tříděné složky v bucketu documents/ (faktury-prijate/, dodaci-listy/, smlouvy/, zamestnanecke/, objednavky/, sklad/, pokladna/, ostatni/). 6) RLS: admin ALL na obě nové tabulky. 7) Triggery: updated_at na obě tabulky |
| 2026-03-24 | **CLEANUP: Edge functions:** Smazáno 11 duplicitních/nepoužívaných edge funkcí z dashboardu (document-generator, generate_document, generate-html-document, redeploy, redeploy-invoice, bright-endpoint, cms-sync, prediction-engine, send-sos, upload-handler, inventory-check). Aktuální stav: 26 aktivních funkcí (22 v repo + 4 jen v dashboardu). **FIX: Chybějící cron joby:** Přidány expire-vouchers, cron-daily, auto-check-service-parts, auto-activate-reserved. Vytvořena chybějící RPC `auto_activate_reserved_bookings()`. Celkem 6 cron jobů |
| 2026-03-24 | **CLEANUP: DB triggery a funkce:** Smazány duplicitní triggery: `trg_auto_generate_door_codes` (nahrazen _insert+_update), `admin_users_updated_at`, `ai_conversations_updated_at` (duplicitní updated_at). Smazány staré overloady: `check_license_for_moto(uuid)` (nahrazen verzí s p_rental_end), `verify_customer_docs` 2 starší signatury (necháno jen nejnovější s p_rental_end). Smazány mrtvé funkce: `trigger_sos_auto_reply()` (no-op), `calc_booking_price()` (nahrazeno calc_booking_price_v2), `expire_vouchers_and_promos()` (nahrazeno expire_vouchers) |
| 2026-03-24 | **NEW: Ukládání platebních karet do Supabase:** 1) Nová tabulka `payment_methods` (user_id, stripe_payment_method_id UNIQUE, brand, last4, exp_month, exp_year, holder_name, is_default). RLS: admin ALL, customer CRUD own. 2) `manage-payment-methods` rozšířena o sync karet do DB po každé akci. 3) `webhook-receiver` rozšířen: při checkout.session.completed (setup i payment) automaticky syncuje karty do `payment_methods`. 4) Frontend: `apiFetchPaymentMethods()` čte primárně ze Supabase tabulky (rychlé, bez edge function), fallback na edge function. Mutace (delete, set_default) aktualizují i lokální tabulku. Po návratu ze Stripe setup retry 3× pro zachycení webhook sync. |
| 2026-03-24 | **NEW: GPS souřadnice v rezervacích:** bookings: přidány sloupce pickup_lat, pickup_lng, return_lat, return_lng (DOUBLE PRECISION, nullable) — GPS souřadnice míst přistavení a vrácení pro odkaz na mapu. |
| 2026-03-23 | **NEW: Přidání karty předem (Stripe Setup):** 1) Edge Function `manage-payment-methods` rozšířena o action `setup` — vytvoří Stripe Checkout Session v setup mode (bez platby), uloží kartu do Stripe Customer. Pokud customer neexistuje, automaticky ho vytvoří. 2) Frontend: `apiSetupNewCard()`, `addNewCard()` — profil „Platební metody" má tlačítko „Přidat novou kartu". 3) Shop checkout: odstraněna nefunkční Apple Pay option, přidán náhled uložené karty, jasný popis Stripe flow. 4) Reservation detail: dynamické zobrazení skutečné platební karty místo hardcoded mock `••••4242`. 5) Platební obrazovky: vylepšené texty o Stripe redirect flow. |
| 2026-03-23 | **NEW: Správa platebních metod (Stripe Customer):** 1) Nový sloupec `profiles.stripe_customer_id` (TEXT). 2) `process-payment` automaticky vytváří Stripe Customer a ukládá kartu (`setup_future_usage: off_session`). Uložená karta se předvyplní na Stripe Checkout. 3) **Nová Edge Function `manage-payment-methods`** — list/delete/set_default uložených karet ze Stripe. 4) Frontend: profil „Platební metody" zobrazuje reálné uložené karty ze Stripe (brand, last4, expiry, jméno držitele). Nastavení prioritní karty, odebrání. 5) Platební obrazovka: zobrazí náhled prioritní uložené karty. 6) Nový soubor `api-payment-methods.js`. |
| 2026-03-23 | **MAJOR: Stripe LIVE platební brána + refundy:** 1) `process-payment` přepnut z TEST na LIVE mode, pouze Checkout Session, locale=cs. 2) `webhook-receiver` — povinná signature verifikace, auto-generace dokumentů, ukládání stripe_payment_intent_id. 3) **Nová Edge Function `process-refund`** — Stripe refundy (částečné i plné), napojeno na storno + zkrácení. 4) Frontend: odstraněn fake kartový formulář, všechny platby jdou přes Stripe Checkout redirect. 5) Shop checkout přepojen na Stripe. 6) SOS payment přepojen na Stripe. 7) Nové sloupce: `bookings.stripe_payment_intent_id`, `bookings.stripe_session_id`, `shop_orders.stripe_payment_intent_id`, `shop_orders.stripe_session_id`. 8) `apiCancelBooking` automaticky volá Stripe refund dle storno podmínek. |
| 2026-03-22 | motorcycles: přidán sloupec tracking_unit (TEXT DEFAULT 'km', CHECK km/mh) — podpora motohodin jako alternativy ke km |
| 2026-03-22 | profiles: přidány sloupce is_blocked, blocked_at, blocked_reason, consent_gdpr, consent_vop, consent_email, consent_sms, consent_push, consent_data_processing, consent_photo |
| 2026-03-08 | Prvotní vytvoření ze 32 migrací + 6 edge funkcí |
| 2026-03-08 | Aktualizace: doplněny chybějící triggery, realtime tabulky, RLS detaily |
| 2026-03-08 23:30 | **MAJOR UPDATE:** Kompletní přepis dle reálného stavu Supabase DB. Doplněno: ENUM typy, 14 dalších edge funkcí, 3 storage buckety, branch-based RLS, dodatečné sloupce (bookings, motorcycles, profiles, sos_incidents), 19+ dalších DB funkcí, foreign keys, 11 secrets, cron jobs |
| 2026-03-09 00:15 | Přidána funkce `validate_voucher_code(p_code)` — validace dárkových poukazů ve slevovém kódu |
| 2026-03-09 | **FIX invoices:** Oprava sync triggeru (payment_receipt → správný doc type), přidán WITH CHECK na RLS policy, zajištění sloupců variable_symbol/source/order_id, customer INSERT policy |
| 2026-03-09 | **FIX shop payment:** Přidána RPC `confirm_shop_payment(p_order_id, p_method)` (SECURITY DEFINER) + UPDATE RLS policy na shop_orders pro zákazníka |
| 2026-03-09 | **FIX documents.type:** Sloupec `documents.type` změněn z `document_type` ENUM na TEXT. ENUM `document_type` zrušen. Trigger `sync_invoice_to_documents()` přepsán s TEXT mapováním |
| 2026-03-09 | **FIX generate_shop_invoice:** Opraven typ faktury z `'shop'` na `'shop_final'`. Přidán `source='shop'` a SECURITY DEFINER |
| 2026-03-09 | **NEW:** Přidán sloupec `bookings.modification_history` (jsonb, default '[]') |
| 2026-03-10 | **FIX SOS triggers:** Dropnut trigger `sos_auto_reply_on_create`. `trigger_sos_auto_reply()` přepsán na safe no-op |
| 2026-03-11 | **KOMPLETNÍ OPRAVA SOS v2:** DROP ALL triggers na sos_incidents + recreate. Klíčová oprava: `check_one_active_sos()` doplněn `WHEN OTHERS THEN RETURN NEW` |
| 2026-03-11 | **FIX license_group enum:** Přidána hodnota `N` (nevyžaduje ŘP) |
| 2026-03-11 | **NEW: Smluvní texty v document_templates:** Seed 3 šablon — VOP, Nájemní smlouva, Předávací protokol |
| 2026-03-11 | **FIX check_one_active_sos:** Změna logiky z per-user na per-booking |
| 2026-03-12 | **FIX confirm_payment:** Podmíněný přechod stavu dle start_date |
| 2026-03-12 | **NEW: Auto-cancel pending bookings:** `auto_cancel_expired_pending()` — app po 10 min, web po 4h |
| 2026-03-12 | **NEW: Header banner:** Nový klíč `header_banner` v `app_settings` |
| 2026-03-12 | **FIX confirm_payment v3 (robust):** Exception handling, fallback tiers |
| 2026-03-12 | **FIX header banner:** `.single()` → `.maybeSingle()` |
| 2026-03-12 | **FIX payment triggers (root cause):** `auto_accounting_on_booking_paid()` přepsán s EXCEPTION handling |
| 2026-03-13 | **NEW: Per-user overlap check:** `check_user_booking_overlap()` trigger |
| 2026-03-13 | **NEW: Auto-complete expired bookings:** `auto_complete_expired_bookings()` |
| 2026-03-13 | **FIX: Document templates .single()→array** |
| 2026-03-13 | **FIX: SOS flow + auto-activate reserved** |
| 2026-03-13 | **NEW: Auto voucher processing:** BEFORE UPDATE trigger `auto_process_voucher_order()` |
| 2026-03-14 | **FIX: Door codes in DP + email** |
| 2026-03-14 | **NEW: Autonomní pobočky:** branch_code, is_open, branch_accessories, branch_door_codes |
| 2026-03-14 | **FIX: Voucher trigger silent fail:** přidán `'voucher'` do CHECK constraint |
| 2026-03-15 | **REFACTOR: KF faktura triggerem:** `generate_final_invoice_on_complete()` |
| 2026-03-15 | **FIX: Robust SOS swap + KF + ZF/DP** |
| 2026-03-17 | **FIX: Mindee OCR document recognition:** Nová Edge Function `scan-document` |
| 2026-03-17 | **FIX: Nepřečtené zprávy:** Nové RPC `mark_thread_messages_read`, `get_unread_thread_message_count` |
| 2026-03-17 | **NEW: Produkty v e-shopu:** Tabulka `products`, Velín CRUD, dynamické načítání z DB |
| 2026-03-17 | **FIX: Přístupové kódy — automatický lifecycle:** `auto_generate_door_codes()`, `auto_deactivate_door_codes()` |
| 2026-03-18 | **SECURITY: Odstranění Service Role Key z frontendu** |
| 2026-03-18 | **NEW: Stripe integrace + Email retry:** `process-payment`, `webhook-receiver` |
| 2026-03-18 | **NEW: AI Copilot + AI Agent + SOS foto + Google recenze** |
| 2026-03-20 | **NEW: Kompletní účetnictví ve Velínu:** 8 nových DB tabulek (acc_employees, acc_payrolls, acc_vat_returns, acc_tax_returns, acc_short_term_assets, acc_long_term_assets, acc_depreciation_entries, acc_liabilities) |
| 2026-03-21 | **NEW: Claude Vision OCR + Flexi pull + Datová schránka:** `receive-invoice` přepsána (Claude Vision), `flexi-sync` rozšířena o PULL, nová tabulka `flexi_reports`, Edge Function `datova-schranka` |
| 2026-03-22 | **UPD: Servisní plány + Detail motorky:** 1) `motorcycles.purchase_mileage` (INTEGER) — km při zakoupení. 2) `maintenance_schedules.first_service_km` + `first_service_desc` — korekce 1. servisu. 3) HP odstraněno z UI, pouze kW. 4) Servisní intervaly počítají od purchase_mileage, 1. servis konfigurovatelný |
| 2026-03-22 | **NEW: Servisní díly + automatické objednávání:** 1) Nová tabulka `service_parts` (schedule_id, inventory_item_id, quantity, notes, UNIQUE constraint). 2) Nová RPC `auto_check_service_parts()` — denní kontrola dílů pro blížící se servisy (~30 kalendářních dní), dedup dle konkrétních dílů (nová PO jen pokud díl nemá otevřenou draft/sent objednávku), auto-vytvoření PO seskupených dle dodavatele. 3) Nová Edge Function `auto-check-service-parts` — volá RPC + rozesílá emaily dodavatelům přes send-order-email. 4) Nový cron job denně 06:00 UTC. 5) Velín: ServiceTab rozšířen o správu dílů per servisní plán (přidat/odebrat/upravit množství ze skladu) |
| 2026-03-21 | **NEW: Automatické objednávky ve Financích:** 1) Nová tabulka `auto_order_rules` (trigger_type: stock_low/interval/manual, threshold_quantity, interval_days, order_quantity, email_override, is_active). 2) Nový sloupec `purchase_orders.sent_at`. 3) Nová Edge Function `send-order-email` — branded HTML email s tabulkou položek, retry 3×, aktualizuje PO status→sent. 4) Velín: nový tab „Objednávky" ve Finance s pod-záložkami Objednávky (CRUD + odeslání emailu) a Automatická pravidla (stock_low/interval/manual triggery, CRUD, ruční spuštění). Jednorázová objednávka: zadá se jen email + položky → vše se zařídí automaticky |
| 2026-03-22 | **NEW: Správa zaměstnanců — kompletní modul:** 1) 4 nové DB tabulky: `emp_attendance` (docházka, UNIQUE employee+date), `emp_vacations` (dovolená, schvalování), `emp_shifts` (směny, branch_id FK), `emp_documents` (smlouvy, dodatky). 2) Nové sloupce `acc_employees`: phone, email, position, vacation_days_total, vacation_days_used. 3) RLS: Admin full access na všechny 4 tabulky. 4) Triggery: updated_at na všechny 4 tabulky. 5) Velín: nová hlavní stránka `/zamestnanci` s 6 záložkami (Přehled, Docházka kalendář, Dovolená, Směny, Dokumenty, Mzdy). Přesunuto ze záložky Finance→Účetnictví. |
| 2026-03-23 | **UPD: Servis — sjednocení formulářů + technik + náklady:** 1) `maintenance_log`: nové sloupce `technician_id` (UUID FK→acc_employees), `labor_hours` (NUMERIC), `extra_cost` (NUMERIC). 2) `acc_employees.hourly_rate` (NUMERIC DEFAULT 500). 3) Velín: oprava chyby `extra_note`/`planned_end` (neexistovaly v DB), sjednocení statusů (out_of_service = „Mimo provoz"), sjednocení datových polí (Servis od / Plánované dokončení / Skutečné dokončení), výběr technika ze zaměstnanců + externí, kalkulace nákladů (hodiny × sazba + extra), MH/km dle tracking_unit, tlačítko „Vrátit do servisu" v Správa modalu |
| 2026-03-23 | **REFACTOR: Sjednocení stavů motorek:** 1) `moto_status` ENUM: odstraněno `rented` (nepoužíváno) a `out_of_service` (migrováno na `unavailable`). Nové hodnoty: `active, maintenance, unavailable, retired`. 2) Nový sloupec `motorcycles.unavailable_reason` (TEXT). 3) `unavailable` bez data = auto max 28.2. následujícího roku. 4) `maintenance_log.status='pending'` = plánovaný servis, motorka zůstává active. Auto-přechod do maintenance v den servisu (cron). 5) `is_urgent` boolean pro urgentní servisy. |
| 2026-03-23 | **FIX: Slevové kódy — aplikace na celkovou cenu:** 1) `create_shop_order()` — procentuální sleva se nyní počítá z `(v_subtotal + v_shipping_cost)` místo jen `v_subtotal`. 2) Frontend: % sleva na booking se počítá z celkové ceny (pronájem + příslušenství + přistavení). 3) Blokace kombinace dvou procentuálních kódů. 4) Zobrazení „sleva X%" místo „sleva X Kč" pro procentuální kódy v aplikaci i Velínu. 5) Logování všech aplikovaných promo kódů do `promo_code_usage`. |
| 2026-03-21 | **NEW: Propojení poboček se skladem + dynamické typy příslušenství:** 1) Nová tabulka `accessory_types` (key, label, sizes[], is_consumable, sort_order, is_active) — nahrazuje hardcoded ACCESSORY_TYPES. 2) Rozšířen CHECK na `inventory.category` o 'prislusenstvi'. 3) Odstraněn CHECK na `branch_accessories.type`. 4) Velín: „Naplnit vše" zobrazuje dialog s přehledem skladu (co se doplní / co chybí). Přidání/úprava příslušenství kontroluje a strhává ze skladu. Spotřební zboží (kukly, ubrousky) se při snížení nevrací na sklad. „Správa typů" — přidání nových typů přímo v UI, auto-vytvoření skladových položek. |
