# SUPABASE BACKEND STATE — MotoGo24 (Část 3: RPC funkce)
> **Soubory:** 1/6 (Tabulky) | 2/6 (Sloupce) | **3/6 (RPC funkce)** | 4/6 (Triggery) | 5/6 (RLS, Realtime, Edge, Storage, Secrets) | 6/6 (Changelog)

---

## 4. RPC FUNKCE (callable z frontendu)

> **Živý snapshot 2026-06-24:** 174 funkcí celkem v `public` (RPC + trigger funkce dohromady; trigger funkce s `RETURNS trigger` jsou primárně dokumentované v STATE_4). Tato část dokumentuje RPC/helpery callable z frontendu/cronu.

### Existující v migracích
| Funkce | Popis |
|--------|-------|
| `is_admin()` | Vrací boolean — je aktuální user admin? |
| `admin_create_customer(p_full_name, p_email, p_phone, p_street, p_city, p_zip, p_country)` | **NEW 2026-08-16 (`20260816_admin_create_customer.sql`, APLIKUJE AUTO-DEPLOY po merge).** Ruční založení zákazníka z Velína (Zákazníci → „Nový zákazník"). Dřívější přímý `INSERT INTO profiles` s `crypto.randomUUID()` padal na FK `profiles_id_fkey` (profiles.id → auth.users.id). RPC založí SKUTEČNÝ auth účet vzorem `create_web_booking` (INSERT INTO auth.users, náhodné bcrypt heslo, `email_confirmed_at=now()` při zadaném emailu → zákazník si heslo nastaví přes recovery; bez emailu vznikne účet s email NULL), profil upsertne ON CONFLICT (id) DO UPDATE (trigger `handle_new_user` ho mezitím mohl založit), `registration_source='velin'`. Audit do `admin_audit_log` správnými sloupci (`new_data`, NE neexistující `details`) v izolovaném BEGIN/EXCEPTION. Guard `is_admin()`. Chyby: `forbidden`, `missing_name`, `email_exists` (+`user_id` existujícího). Vrací `{success, user_id}`. SECURITY DEFINER, GRANT authenticated. Volá Velín `Customers.jsx` → `AddCustomerModal`. **FIX 2026-08-16 (`20260816b_admin_create_customer_gen_salt_fix.sql`):** původní `SET search_path=public` na živé DB shodilo `crypt()`/`gen_salt()` (`function gen_salt(unknown) does not exist` — pgcrypto je na Supabase ve schématu `extensions`) → nově `SET search_path=public, extensions`; tělo 1:1. POZOR pro budoucí funkce: SECURITY DEFINER s pevným search_path MUSÍ obsahovat `extensions`, jinak pgcrypto funkce nenajde. |
| `swap_handoff_bookings(p_prev uuid, p_next uuid)` | **NEW 2026-07-21 (`20260720`) — rev.2 (`20260723_swap_handoff_robust.sql`).** Výměna motorky bez přerušení (Model A) — atomický switch dvou navazujících rezervací na obslužné pobočce. Guardy: `is_admin()`, stejný zákazník, B zaplacená (`paid`/`partial_refund`/`refund_pending`). Uzavře A (`completed`+`returned_at`+`actual_return_date`, jen z `active`/`reserved`, idempotentně), aktivuje B (`active`+`picked_up_at`, z `reserved`/`pending`/**`active`** — novou mohl doc-trigger `trg_activate_on_handover_protocol_doc` aktivovat už při podpisu protokolu PŘED RPC, i tak dopíše `continues_booking_id`; rev.3 `20260724`) a **`handover_protocol_filled_at` si nastaví PŘÍMO v aktivačním UPDATE** (COALESCE) → projde strážcem `trg_gate_obsluzna_activation` bez závislosti na samostatném frontendovém UPDATE + zapíše `continues_booking_id`. **Zrušen tvrdý guard `handover_protocol_missing`** (RPC volá admin až po podpisu protokolu v `SwapModal`; protokol si RPC dopíše). **Vrací diagnostiku** `{success, prev, next, prev_status, next_status, prev_completed, next_active}` → frontend ověří, že switch reálně přepnul stavy. Chyby `{success:false, error}`: `forbidden`,`not_found`,`different_customer`,`next_not_paid`. SECURITY DEFINER, GRANT authenticated. Volá Velín `SwapModal`. |
| `split_booking_moto_swap(p_booking_id uuid, p_new_moto_id uuid, p_swap_date date, p_swap_time text, p_dry_run bool)` | **NEW 2026-07-21 (`20260721_moto_swap_split.sql`) — rev.2 (NET model), rev.3 2026-07-31 (`20260731_moto_swap_first_last_day.sql` — povolen PRVNÍ den).** Výměna motorky uprostřed JEDNÉ rezervace = split na dvě (Model B). Ověří (owner/admin, status reserved/active, paid, swap_date ∈ [start..end] — **od rev.3 vč. prvního dne**, nová moto volná [swap_date..end] mimo tuto rez., ne již rozděleno). **Rev.3 větve pro swap_date=start (plná výměna, jen ≥CURRENT_DATE):** `reserved` → **REPLACE** in-place (`moto_id`+`pickup_time`+`total_price+=net` v TÉŽE rezervaci, žádný split; mail/doklad/app zprávu řeší `trg_booking_modified_email`); `active` (převzato dnes) → split s A.end=swap_date (stejnodenní předání, den účtován na B). Dry-run i commit vrací navíc `mode` ('replace'/'split'). **Rev.4 2026-07-31b (`20260731b_moto_swap_refund_cap.sql` — STROP VRATKY):** odebrané dny A se dobropisují max. do poměrné části SKUTEČNĚ zaplacené ceny pronájmu: `a_credit = LEAST(ceník(odebrané)−věrn.sleva, round(GREATEST(total_price−delivery_fee−extras_price,0) × ceník(odebrané)/ceník(celý termín)))`; `net = (B po slevě) − a_credit`. Promo/voucher/100% sleva → zákazník nikdy nedostane zpět víc, než zaplatil (100% promo → vratka 0, nová motorka za zbytek v plné ceně); plná cena → strop se nedotkne. Zkrácení A odečítá `a_credit`; dry-run pole `a_removed` = `a_credit`. **Rev.5 2026-07-31c (`20260731c_moto_swap_service_role.sql` — service_role commit):** `v_admin` je TRUE i pro JWT roli `service_role` (webhook-receiver po zaplacení doplatku volá COMMIT server-side z `_swap` metadat — pojistka proti tichému selhání klientského commitu, incident #EEC9CA33). **Cena = NET rozdíl** = `_moto_dayprice_sum(nová, swap..end) − _moto_dayprice_sum(stará, swap..end)`. **Dvoukrok:** `p_dry_run=true` → vrátí `{net, a_removed, b_total, a_new_end, b_start, b_end}` (nic nezapisuje); frontend vypořádá net stávající edit-platbou (doplatek/`process-refund`); `p_dry_run=false` (COMMIT, až po vypořádání) → zkrátí původní na [start..swap-1] (`total_price -= a_removed`, jen data — ZF/DP/KF NEŘEŠÍ), vytvoří DRUHOU rezervaci B [swap..end] jako **`reserved`/`paid`** (`continues_booking_id`, overlap výjimka), a **server-side rozešle B „reserved" mail + smlouvu** (`send-booking-email` přes pg_net → web i app stejně). Vrací `{success, new_booking_id, net, loyalty_percent, loyalty_level}`. **Věrnostní sleva:** pro `booking_source='app'` aplikuje % z AKTUÁLNÍHO ranku (`min(20,ceil((_loyalty_qualifying_count+1)/2))` → `loyalty_levels.discount_percent`) na cenu B i na net (`net = (B−B_disc)−(A_removed−A_disc)`); B dostane `loyalty_level/percent/discount_amount` (konzistentní s `validate_app_loyalty_discount` → neořízne). Web = bez slevy (app-only). Výbava se ve výměně neřeší (má vlastní sekci v úpravě rezervace, free-gear od ranku 3). Chyby: `not_found`,`forbidden`,`wrong_status`,`not_paid`,`invalid_new_moto`,`swap_date_out_of_range`,`already_split`,`new_moto_unavailable`. **POZOR: účetní rozpad (ZF/DP/KF, dobropisy) net/reallocation modelu NENÍ v RPC řešen — odloženo, dořešit před ostrým provozem.** SECURITY DEFINER, GRANT anon/authenticated. **Rev.6 2026-08-04 (`20260804_edit_money_fixes.sql`):** přidána ŘP kontrola VLASTNÍKA rezervace (OR-match `license_groups`, fallback `[license_required]`; `'N'` = bez kontroly) pro zákaznická volání — nový error `license_insufficient`; admin/service_role (Velín, webhook commit) beze změny. |
| `_moto_dayprice_sum(p_moto_id uuid, p_start date, p_end date)` | **NEW 2026-07-21.** Součet denní ceny motorky (`price_mon..price_sun` dle ISODOW) za rozsah [start..end] inkluzivně. Helper pro split (cena B + refund odebraných dnů A). STABLE. |
| `get_moto_booked_dates(p_moto_id)` **UPDATE 2026-07-21 (`20260720`)** | Blokace kalendáře. NOVĚ (LEFT JOIN LATERAL): má-li tentýž zákazník NAVAZUJÍCÍ rezervaci na jinou motorku (výměna; start ∈ [end_date, end_date+1]), blokuje se tato (stará) motorka do `GREATEST(end_date, next_start, CURRENT_DATE)` = **až do dne výměny, OKAMŽITĚ jakmile navazující rezervace existuje** (ne až v den výměny) — dokud není `returned_at`. Řeší, aby v mezidobí nikdo nezarezervoval nevrácenou motorku. Běžné rezervace beze změny (blok do `end_date`). Servis beze změny. **+ `auto_complete_expired_bookings` UPDATE:** nedokončuje starou motorku s ČEKAJÍCÍ navazující výměnou (dokončí ji až switch/protokol; fallback po skončení navazující rezervace), aby zůstala blokovaná i v den výměny do fyzického vrácení. |
| `correct_motorcycle_mileage(p_moto_id uuid, p_km integer, p_note text)` | **NEW 2026-06-28 (`20260628_handover_mileage_and_service_analytics.sql`).** Ruční korekce nájezdu motorky (Velín → Flotila → „Korigovat"). `is_admin()` guard; `UPDATE motorcycles SET mileage = GREATEST(p_km, COALESCE(purchase_mileage,0))` (smí i dolů kvůli překlepu, ale nikdy pod stav při pořízení); zápis do `admin_audit_log` (action `motorcycle_mileage_corrected`). Vrací `motorcycles` řádek. Chyby: `forbidden`, `invalid_km`, `moto_not_found`. SECURITY DEFINER, GRANT authenticated. |
| `update_moto_after_service()` | **UPDATE 2026-06-28 (`20260628_...`):** trigger fce (`maintenance_log_after_insert` + nově `maintenance_log_after_update`). Přidán bump odometru `motorcycles.mileage = GREATEST(mileage, NEW.km_at_service)` (nikdy nesnižuje); při `status='completed'` srovná `motorcycles.last_service_date` + `maintenance_schedules.last_service_km/last_service_date`. EXCEPTION-safe (chyba → `debug_log`). **POZOR:** funkce vznikla mimo migrace; před CREATE OR REPLACE nutno vydumpnout živé tělo (`pg_get_functiondef`) a začlenit případné extra chování. |
| `analytics_moto_rental_km()` | **NEW 2026-06-28.** Vrací řádek per předávací segment: `moto_id, booking_id, user_id, start_date, reading (mileage_start), next_reading (LEAD per moto dle start_date), km_driven (=next−reading, ≥0)`. Filtr `mileage_start>0` + `is_test=false`. Poslední otevřený segment má `next_reading=NULL` (nelze přiřadit najeto). Základ pro km analytiku. STABLE, GRANT authenticated. |
| `analytics_customer_km()` | **NEW 2026-06-28.** Per zákazník: `total_km, rentals_with_km, km_per_rental` (jen uzavřené segmenty `next_reading IS NOT NULL`). STABLE, GRANT authenticated. |
| `analytics_moto_km()` | **NEW 2026-06-28.** Per motorka: `current_km, purchase_km, total_driven (=current−purchase), rentals_with_km, km_per_rental, avg_km_per_calendar_day (od acquired_at), avg_km_per_rental_day`. STABLE, GRANT authenticated. Čte Velín Flotila detail (Ø na den) i Analýza → Nájezd km. |
| `analytics_service_plan(p_default_daily_km numeric DEFAULT 50)` | **NEW 2026-06-28.** Per motorka × typ servisu (olej/pneu/komplet z `motorcycles.*_interval_*`): `interval_km/days, current_km, last_service_km (z `maintenance_schedules`), km_since_last, km_remaining, due_soon (≤20 % intervalu), overdue (≤0), avg_daily_km, estimated_due_date` (kalendářní projekce z průměru). Bohaté umístění dne (sezóna, Út/St, zimní servis, STK) zůstává v JS `serviceScheduleUtils.js`. STABLE, GRANT authenticated. |
| `is_superadmin()` | Vrací boolean — je aktuální user superadmin? |
| `validate_promo_code(code)` | Validuje promo kód, vrací jsonb |
| `get_promo_code_type(p_code text)` | **NEW 2026-07-23 (`20260728_promo_codes_rls_lockdown.sql`, SECURITY FIX).** SECURITY DEFINER, `RETURNS text`, `SET search_path=public`. Vrací `type` (percent/fixed) kódu i pro neaktivní/vyčerpaný kód (na rozdíl od `validate_promo_code`, který pro neaktivní vrátí valid=false bez typu). Vrací JEN typ — žádnou hodnotu ani ciník, vyžaduje znát přesný `code` → neumožňuje enumeraci. GRANT EXECUTE anon+authenticated. Vzniklo protože po zrušení anon SELECT na `promo_codes` (viz STATE_5) potřebovala appka (`reservation_edit_screen._loadDiscountType`) náhradu za přímé čtení tabulky. |
| `set_promo_code_source()` | **NEW 2026-06-20.** Trigger fn (BEFORE INSERT na `promo_codes`, viz `trg_set_promo_code_source` ve STATE_4). Když `NEW.source IS NULL AND NEW.code ILIKE 'VRACENI-%'` → nastaví `NEW.source='vraceni'` (kódy z vrácení jsou pak ve Velíně filtrovatelné dle zdroje). Jen nastaví pole, výjimku nevyhazuje. LANGUAGE plpgsql. |
| `deactivate_exhausted_promo_code()` | **NEW 2026-08-20 (`20260820_deactivate_exhausted_promo_codes.sql`).** Trigger fn (BEFORE INSERT OR UPDATE OF used_count na `promo_codes`, viz `trg_deactivate_exhausted_promo` ve STATE_4). Když `NEW.max_uses IS NOT NULL AND NEW.used_count >= NEW.max_uses` → nastaví `NEW.active=false` (vyčerpaný kód přejde na neaktivní v okamžiku posledního uplatnění — used_count zvyšuje `increment_promo_used_count`). Jen nastaví pole, výjimku nevyhazuje. LANGUAGE plpgsql. |
| `use_promo_code(code, booking_id, base_amount)` | Použije promo kód atomicky |
| `create_shop_order(items, shipping, address, payment, promo)` | Vytvoří e-shop objednávku. **Procentuální sleva se počítá z celkové ceny (subtotal + shipping)**. **Login-only**, používá Flutter app. |
| `create_web_shop_order(items, customer_name, customer_email, customer_phone, shipping_method, shipping_address, payment_method, promo_code, notes)` | **Anon-friendly RPC pro web e-shop** (motogo24.cz). Items: `[{product_id uuid, size text|null, qty int}]` — cena z DB (ne z klienta), velikost validována proti `products.sizes[]`. **Per-size sklad (2026-05-05):** pro produkty s `sizes[]` kontroluje a dekrementuje `products.size_stock[size]` (ne pouze `stock_quantity`); produkty bez velikostí kontrolují/dekrementují `stock_quantity` jako dřív. `stock_quantity` se drží v sync (sum). Při nedostatku vrací `{error:'out_of_stock', size, available, requested}`. Doprava: `pickup`=0, `post`=99, `zasilkovna`=79 Kč. Vytvoří `shop_orders` + `shop_order_items` (s `product_id` FK + `size`). Vrací `{success, order_id}` nebo `{error, ...}`. SECURITY DEFINER, GRANT TO anon, authenticated. **NEPŘEPISUJE** stávající `create_shop_order` — Flutter dál používá login-only verzi. **FIX 2026-05-08 (`20260508_fix_create_web_shop_order_currency.sql`):** odstraněn neexistující sloupec `currency` z `INSERT INTO shop_orders` (column listu i `VALUES`) — Stripe si měnu řeší v line items, do DB se neukládá; stejný typ bugu byl 2026-05-07 opraven v `process-payment` edge fn. Současně `payment_status='unpaid'` → `'pending'` (CHECK constraint na `shop_orders.payment_status` povoluje jen `pending/paid/refunded/failed`, na rozdíl od `bookings.payment_status`). Reportováno: web rezervace krok 2 → doprodej (kukla) → „Pokračovat k platbě" hodilo alert „column 'currency' of relation 'shop_orders' does not exist". **FIX 2026-05-19 (`20260519_drop_legacy_create_web_shop_order.sql`):** stará p_-prefix verze RPC (s `p_shipping_address jsonb`) zůstávala v DB jako overload (`CREATE OR REPLACE` 20260508 ji nepřepsal kvůli odlišnému typu argumentu), web `js/checkout.js` ji v e-shop checkoutu volal a stejná chyba s `currency` se vracela. Migrace `DROP FUNCTION IF EXISTS create_web_shop_order(jsonb, text, text, text, text, jsonb, ...)` (všechny historické overloady s `jsonb` adresou) a recreate kanonické verze beze změny logiky. Současně z CASE pro shipping cost odstraněna větev `'zasilkovna'` — UI v `pages/checkout.php` ji už nenabízí, zůstává `pickup`=0 / `post`=99. `shop_orders.shipping_method` CHECK constraint zůstává `pickup/post/zasilkovna` kvůli historickým objednávkám. Frontend `js/checkout.js` updatován: non-prefix RPC params + `shipping_address` jako TEXT (joined string, ne objekt). |
| `cancel_booking_tracked(booking_id, reason)` | **UPDATE 2026-05-03:** Stornuje rezervaci s refund kalkulací (storno tabulka 7+/2-7/<2 dny = 100/50/0 %). Po INSERT do `booking_cancellations` zavolá `send-cancellation-email` přes `pg_net.http_post` — edge fn sama zařídí Stripe refund + dobropis credit_note + mail. Smlouva return jsonb beze změny `{success, refund_percent, refund_amount}`. SECURITY DEFINER **UPDATE 2026-08-04 (`20260804_edit_money_fixes.sql`):** hranice storna nově `>=168`/`>=48` h (dřív `>168`/`>48`) — server na přesné hranici sliboval o tier méně, než UI webu i appky (obě používají `>=`). |
| `cancel_pending_web_booking(p_booking_id)` | **NEW 2026-06-27:** Okamžité zrušení **rozpracované** web rezervace samotným zákazníkem (tlačítko „Zrušit nedokončenou rezervaci" na kroku 2 — `js/pages-rezervace-steps.js` → `MG._rezCancelBooking`). Zruší řádek POUZE když `status='pending' AND payment_status='unpaid'` (jinak `{success:false, error:'not_cancelable'}`/`'not_found'`) → `UPDATE bookings SET status='cancelled', updated_at=now()`. Bez refundu, bez mailu (rezervace nikdy nebyla potvrzená ani zaplacená) — okamžitě uvolní slot místo čekání na cron `auto_cancel_expired_pending()` (web=4h). Vrací `{success:true}`. SECURITY DEFINER, `SET search_path=public`, GRANT EXECUTE TO anon, authenticated. Frontend je odolný i bez RPC (při chybě stejně vyčistí session a přesměruje na `/rezervace`). |
| `set_booking_qr_payment(p_booking_id)` | **NEW 2026-07-05 (`20260705_qr_bank_payment.sql`):** Zákazník na WEBU zvolil „Platba QR / převodem" (krok 2). Přidělí rezervaci **číselný VS** (jednou, ze sekvence `booking_vs_seq`), nastaví `pay_channel='qr'`, `payment_method='bank_transfer'` a potlačí abandoned mail (`abandoned_email_sent_at=COALESCE(...,now())`). Guardy: jen `booking_source='web'` (jinak `{error:'not_web_booking'}`), ne `payment_status='paid'` (`already_paid`), neexistuje (`not_found`). Idempotentní (návrat/refresh nepřepíše existující VS). Vrací `{success,booking_id,vs,amount,due_hours:4}`. Rezervace zůstává pending/unpaid → potvrzení ruční ve Velíně (`confirm_payment`, `PaymentConfirmModal`), nezaplacené ruší cron `auto_cancel_expired_pending()` (web=4h). Volá edge fn `qr-payment` (service role), která pak vystaví ZF (`generate-invoice`) + rozešle maily. SECURITY DEFINER, `SET search_path=public`, GRANT EXECUTE TO anon, authenticated, service_role. |
| `set_booking_mod_surcharge(p_booking_id)` | **NEW 2026-07-27 (`20260727_booking_edit_surcharge_flow.sql`):** Přidělí číselný VS (sekvence `booking_vs_seq`) pro DOPLATEK za úpravu rezervace (`bookings.mod_surcharge_vs`). Guardy: `not_found`, `no_surcharge_pending` (`mod_surcharge_due` ≤ 0). Idempotentní (VS jen jednou). Vrací `{success, vs, amount}` (amount = mod_surcharge_due). Volá edge fn `qr-payment` v režimu surcharge. SECURITY DEFINER, `SET search_path=public`, GRANT authenticated, service_role. |
| `confirm_booking_surcharge(p_booking_id, p_method, p_vs, p_paid_date, p_transaction_ref)` | **NEW 2026-07-27 (`20260727_booking_edit_surcharge_flow.sql`):** Admin potvrdí přijetí DOPLATKU za úpravu (Velín → detail rezervace → „Potvrdit doplatek" → PaymentConfirmModal v režimu surcharge). Guard `is_admin()`; `no_surcharge_pending` když nic nedluží. Vynuluje `mod_surcharge_due`, nastaví `mod_surcharge_paid_at`, doplní `payment_reference`; pak (každý krok v izolovaném BEGIN/EXCEPTION — selhání nerollbackne potvrzení): (1) pošle ODLOŽENÝ `booking_modified` mail (payload z `mod_email_payload` uschovaný triggerem, fallback složí z řádku; `price_difference` přepíše na skutečně zaplacený doplatek → send-booking-email vygeneruje rozdílový DP source='edit' + smlouvu/VOP), (2) `dispatch_email_event('booking_updated')`, (3) in-app zpráva (`admin_messages` → FCM push). Nakonec vyčistí `mod_email_payload`. Vrací `{success, amount, vs}`. SECURITY DEFINER, `SET search_path=public`, GRANT authenticated. |
| `set_web_booking_chosen_method(p_booking_id, p_method)` | **NEW 2026-07-10 (`20260711_get_web_booking_resume_paid.sql`, flow platba PŘED doklady):** Uloží zvolenou platební metodu (`card`/`qr`) do `bookings.chosen_payment_method` (LEFT 20 zn.), jen `booking_source='web'`. Volá web `_rezSubmitPayment` při kliknutí na platbu — Velín pak pozná zvolenou metodu i u NEZAPLACENÝCH (QR má i `pay_channel`, karta jinak nešla poznat). EXCEPTION-safe. SECURITY DEFINER, `SET search_path=public`, GRANT anon/authenticated. **UPDATE 2026-07-17 (`20260717_web_payment_method_switch.sql`):** při `p_method='card'` navíc **vynuluje `pay_channel`** (`CASE WHEN p_method='card' THEN NULL ELSE pay_channel END`) + přidán guard `payment_status <> 'paid'`. Důvod: zákazník na platební bráně dá „Zpět" a přepne z QR na kartu — bez vynulování by po pozdější Stripe platbě naskočil trigger `trg_qr_payment_receipt` (pay_channel zůstal 'qr') a poslal duplicitní/chybný doklad. |
| `check_web_booking_user_overlap(p_email, p_start, p_end, p_exclude_booking_id)` | **NEW 2026-07-15 (`20260715_web_user_day_overlap_check.sql`):** Read-only anon check pro web — „jeden e-mail = max 1 rezervace na den". Najde usera dle `auth.users.email`, vrátí `{overlap:true, booking_id, start_date, end_date}`, pokud má nezrušenou rezervaci (`status IN pending/reserved/active/completed`) překrývající `[p_start,p_end]` (`daterange … '[]'` — sdílený den = kolize), jinak `{overlap:false}`. Vylučuje `p_exclude_booking_id` (vlastní draft). Volá web `_submitReservation` PŘED `create_web_booking` (alert `rez.alert.userDayOverlap`, blokuje). DB pojistka = trigger `check_user_booking_overlap`. STABLE SECURITY DEFINER, `SET search_path=public,auth`, GRANT anon/authenticated. |
| `set_web_booking_docs_completed(p_booking_id)` | **NEW 2026-07-12 (`20260712_web_docs_numbers_signal.sql`):** Signál „zákazník dokončil krok dokladů" (vyplnil povinná ČÍSLA). Nastaví `bookings.docs_completed_at=COALESCE(...,now())` + dopočte `docs_fill_seconds` (od confirmed_at/created_at, clamp 0–604800), jen `booking_source='web'`. Volá web `_rezFinishDocs` při „Hotovo". Na tento signál gate-uje reserved cron (`web_booking_reserved` až po vyplnění čísel; scan/kódy zvlášť). EXCEPTION-safe. SECURITY DEFINER, GRANT anon/authenticated. **UPDATE téže migrace:** `get_web_booking_resume` +`docs_optional`(child N ∨ delivery ∨ obslužná)/`pickup_method`/`docs_completed_at`; `send_missing_booking_reserved_emails` web-guard → `docs_completed_at IS NOT NULL`; `send_abandoned_booking_emails` Path C → `docs_completed_at IS NULL` (30 min). |
| `get_web_booking_resume` **UPDATE 2026-07-10 (`20260711_...`)** | Rozšířeno pro flow platba PŘED doklady: WHERE nově povoluje i **zaplacené** web rezervace (`status IN reserved/active AND payment_status IN paid/partial_refund/refund_pending`) vedle pending+unpaid — po platbě se zákazník vrací na krok DOKLADŮ přes `/rezervace?resume=<id>`. Přidána pole `payment_status`, `booking_status`, `pay_channel`, `docs_status` (=`check_booking_docs_status`, NULL=OK) — frontend (`pages-rezervace.js`) z nich odvodí `_docsPhase`. **UPDATE 2026-07-17 (`20260717_web_payment_method_switch.sql`):** výpočet `docs_status` přesunut do **izolovaného `BEGIN/EXCEPTION WHEN OTHERS` bloku** (dosud volán inline v `jsonb_build_object`). Bug: výjimka v `check_booking_docs_status` shodila CELÝ resume RPC → frontend dostal `n.error` a ukázal **„Rezervace nenalezena"** i pro naprosto validní nezaplacený draft, na který se zákazník vrací z platební brány (dá „Zpět", chce jinou metodu). Nově se WHERE (povolené stavy) ověří zvlášť do proměnných, docs_status se dopočte bezpečně (chyba → NULL), pak se poskládá výsledek. WHERE i vrácená pole beze změny. |
| `send_missing_booking_reserved_emails()` **UPDATE 2026-07-10** | Přidán **web-only guard**: pro `booking_source='web'` pošle `web_booking_reserved` (Smlouva+VOP+door codes) až když `check_booking_docs_status(...) IS NULL` (doklady ověřené) — v novém flow reserved chodí AŽ po dokladech; app beze změny. Nastaví `docs_completed_at`. Zůstává catch-up i primární odesílatel reserved pro web. |
| `send_abandoned_booking_emails()` **UPDATE 2026-08-10 (`20260810_restore_to_pending.sql`)** | Path AB nově filtruje `b.restored_at IS NULL` — rezervace obnovená adminem ve Velíně není „nedokončená", mail se neposílá. Path C beze změny. **UPDATE 2026-07-10** | Path C (`booking_missing_docs`): práh **5→30 min** od `confirmed_at` (aby připomínka nechodila během vyplňování kroku 3) + `docs_url` nově na **poslední krok flow** `/rezervace?resume=<id>` (ne `/upravit-rezervaci`). Path AB (15 min abandoned) beze změny. |
| `shorten_booking_with_refund(p_booking_id, p_new_start, p_new_end, p_reason)` | **NEW (2026-04-29):** Atomicky zkrátí rezervaci a vystaví refund podle storno tabulky (≥168h=100 %, ≥48h=50 %, jinak 0 %). Validace: ownership přes `auth.uid()=user_id`, status IN reserved/active, payment_status='paid', nové datumy uvnitř původních, aktivní rezervace má start zamčený. Per-day cena z `motorcycles.price_mon..price_sun` (ISODOW). UPDATE bookingu (start/end, total_price, `original_start_date`/`original_end_date` při prvním zásahu, modification_history append s `source='web_customer'`). Stripe refund přes `pg_net.http_post` na Edge `process-refund` (vytvoří dobropis credit_note). Vrací jsonb `{success, refund_amount, refund_percent, new_total}` nebo `{success:false, error}` (kódy: `unauthenticated`, `not_found`, `wrong_status`, `not_paid`, `not_a_shortening`, `invalid_range`, `active_start_locked`, `no_change`, `no_diff`). SECURITY DEFINER. GRANT EXECUTE TO authenticated. **VARIANTA B + dispatch fix 2026-06-11 (`20260611_discount_variant_b.sql`):** refund po stornu jde přes `_apply_discount_variant_b` (vrací se částka PO slevě, `discount_amount` se přepočítá); refund dispatch čte URL/key z `app_settings` v izolovaném BEGIN/EXCEPTION — původní `current_setting('app.settings.*')` na managed Supabase neexistoval, throw ROLLBACKoval i uložené zkrácení (stejný GUC bug jako v core do 2026-06-09). **UPDATE 2026-08-04 (`20260804_edit_money_fixes.sql`):** refund dispatch už NENÍ podmíněn `stripe_payment_intent_id IS NOT NULL` — dřív se vratka u QR/převod/hotově plateb tiše zahodila (zákazník viděl „vracíme X Kč", dobropis ani peníze nevznikly). Dispatch jde vždy; `process-refund` si PI dohledá ze session, a bez Stripe platby vystaví dobropis (stripe_refund_id NULL) + označí `refund_pending` = manuální vratka převodem na účet do 14 dnů (Velín: banner „VRÁTIT NA ÚČET" + akce „Vratka odeslána"). |
| `apply_booking_changes(p_booking_id, ...15 changes args, p_dry_run)` | **REFAKTOR 2026-05-02:** Po zavedení AI úprav rezervace (Cesta B) je tato funkce **thin wrapper**: získá `auth.uid()`, na NULL vrátí `unauthenticated`, jinak pře-deleguje volání na `_apply_booking_changes_core(v_user, ..., p_source := 'web_customer')`. Signatura beze změny — web app (Velín, motogo24.cz „Upravit rezervaci") volá dál stejně. SECURITY DEFINER. GRANT EXECUTE TO authenticated. Veškerá business logika je v core funkci. |
| `_apply_booking_changes_core(p_user_id, p_booking_id, ...15 changes args, p_dry_run, p_source)` | **NEW 2026-05-02:** Privátní jádro pro úpravu rezervace. Veškerá pravidla pro úpravu (storno tabulka ≥168h=100 %, ≥48h=50 %, <48h=0 %; per-day moto diff; license hierarchie AM<A1<A2<A;B;N=žádný; overlap check; zámek `start` a `moto_id` u status=active; delivery_fee 100 % refund + plný doplatek; Stripe refund přes pg_net na `/functions/v1/process-refund`). Volá se interně z `apply_booking_changes` (auth wrapper, source='web_customer') a `apply_booking_changes_anon` (anon wrapper, source='ai_agent'). `p_source` se ukládá do `modification_history` (klíč `source`) — používá to limit AI úprav (3 / den / rezervace). REVOKE ALL FROM PUBLIC, anon, authenticated — pouze SECURITY DEFINER wrappery. **FIX 2026-06-09:** Změna motorky (i jakákoliv úprava s refundem) na webu `/upravit-rezervaci` házela HTTP 400 „Something went wrong" a změnu neuložila. Příčina: refund dispatch četl `current_setting('app.settings.supabase_url')` / `...service_role_key` — GUC `app.settings.*` na hostovaném Supabase neexistuje (ALTER DATABASE = permission denied), takže `current_setting()` bez fallbacku vyhodil `unrecognized configuration parameter` (SQLSTATE 42704) → PostgREST 400. Protože throw přišel **až po** `UPDATE bookings`, celá transakce se rollbackla → úprava se nikdy neuložila (koreluje se starým 2026-05-19 nálezem „RPC neuložila změnu"). Stejný GUC bug byl dřív opraven u `send_message_via_edge` (2026-05-03) a `send_push_via_edge` (2026-06-06), tady zůstal. Oprava: URL/key se čtou z **tabulky `app_settings`** (`value #>> '{}'`), `net.http_post` izolován ve vlastním `BEGIN/EXCEPTION WHEN OTHERS` (výpadek dispatche už nikdy nerollbackne uloženou změnu; chybí-li app_settings řádky, změna se uloží, refund se jen nezafrontuje). **+ delivery_fee fix:** UPDATE dřív bezpodmínečně nastavoval `delivery_fee = COALESCE(p_new_pickup_fee,0)+COALESCE(p_new_return_fee,0)` → změna jen motorky/termínu (oba fee params NULL) **vynulovala přistavné** na rezervaci. Nově `CASE WHEN p_new_pickup_fee IS NOT NULL OR p_new_return_fee IS NOT NULL THEN … ELSE delivery_fee END` — přistavné se přepíše jen při reálné změně místa. Wrapper `apply_booking_changes` beze změny. **VARIANTA B 2026-06-11 (`20260611_discount_variant_b.sql`):** sleva se při úpravě přepočítá na nový obsah rezervace — hrubý rozdíl (po stornu) jde přes `_apply_discount_variant_b` (procentuální = efektivní sazba `discount/(total+discount)` × nová hrubá; fixed/voucher = `MIN(hodnota, nová hrubá)`); `net_diff`/`refund_amount`/`new_total` jsou PO slevě, UPDATE zapisuje `total_price` absolutně + nový `discount_amount`; history entry nese `price_diff` (net), `gross_diff`, `from/to_discount` (price_diff čte generate-invoice edit fallback). Re-aplikován fix 2026-06-09 (app_settings + izolovaný refund dispatch + delivery_fee CASE) — snapshot 2026-06-04 ho neobsahoval. **UPDATE 2026-08-04 (`20260804_edit_money_fixes.sql`):** a) refund dispatch bez podmínky na `stripe_payment_intent_id` (viz shorten — stejný fix; manuální vratky přes process-refund); b) overlap kontrola nově zahrnuje SERVISNÍ bloky z `maintenance_log` (service_date→scheduled_date, nedokončené) — změna motorky/termínu na stroj v servisu už neprojde; c) ŘP kontrola přes `license_groups` OR-match (fallback `[license_required]`) — skútr `{A1,B}` smí i zákazník se skupinou B. **UPDATE 2026-08-06 (`20260806_loyalty_surcharge_and_branch_address.sql`):** VĚRNOSTNÍ SLEVA NA DOPLATKU — u app rezervací (`booking_source='app'`) se KLADNÝ rozdíl pronájmu (dates_diff po stornu a late-pickup; delivery poplatky slevě nepodléhají) snižuje o % dle AKTUÁLNÍHO ranku zákazníka (stejný vzorec jako `split_booking_moto_swap`: level=min(20,ceil((qualifying+1)/2)), pct z `loyalty_levels`). Sleva se přičítá do `bookings.loyalty_discount_amount` (+ aktualizuje `loyalty_level`/`loyalty_percent`), response i `breakdown` nesou `loyalty_discount`/`loyalty_percent`/`loyalty_level`, history entry `loyalty_surcharge_discount`. Vratky beze změny. Zrcadleno klient-side v app `EditPriceCalc` a ve Velíně (`BookingModifyModal` přes `get_booking_loyalty_rate`). |
| `apply_booking_changes_anon(p_booking_id, p_contact, p_password_last4, ...15 changes args, p_dry_run)` | **NEW 2026-05-02:** Anon wrapper pro AI agenta (`ai-public-agent`). Ověření identity zákazníka třemi faktory: `p_booking_id` + `p_contact` (email NEBO CZ telefon — auto-detekce přes `@`; CZ telefon se porovnává na posledních 9 číslic, +420 prefix nehraje roli) + `p_password_last4` (4 znaky z hesla, ověření přes bcrypt z `profiles.password_last4_bcrypt`). Po ověření: kontrola `booking_source='web'`, `status IN reserved/active`, `payment_status='paid'`, **limit 3 reálné změny / den / rezervace** (počítáno z `modification_history` kde `source='ai_agent'` a `(at)::date = current_date` — dry-run se nezapočítává), pak volá `_apply_booking_changes_core(v_user_id, ..., p_source := 'ai_agent')`. Vrací jsonb `{success, payment_required, net_diff, refund_amount, new_total?, breakdown}` nebo `{success:false, error}` (kódy: `missing_inputs`, `not_found`, `verification_failed`, `password_check_unavailable`, `not_web_booking`, `wrong_status`, `not_paid`, `daily_limit_reached`, plus všechny kódy z core). SECURITY DEFINER. GRANT EXECUTE TO anon, authenticated. |
| `find_booking_for_modification(p_booking_id, p_contact, p_password_last4)` | **NEW 2026-05-02:** Read-only lookup rezervace pro AI agenta. Stejné 3faktorové ověření jako `apply_booking_changes_anon`. Vrací jsonb se stavem rezervace (booking_id, user_id, status, moto_id, start_date, end_date, pickup_method/address, return_method/address, delivery_fee, total_price, customer_name) + `mods_today_count` (počet dnešních AI úprav z `modification_history`). Používá se v edge funkci `ai-public-agent` jako tool `find_my_booking`, který musí předcházet jakékoliv úpravě (zákazníkovi se vrací stav, agent ho zákazníkovi shrne před voláním `preview_booking_change`). Error kódy: `missing_inputs`, `not_found`, `verification_failed`, `password_check_unavailable`, `not_web_booking`, `wrong_status`, `not_paid`. SECURITY DEFINER. GRANT EXECUTE TO anon, authenticated. |
| `resolve_booking_ref(p_ref)` | **NEW 2026-06-21 (D) (PŘEDLOŽENO JAKO TEXT — nutno aplikovat + redeploy `ai-public-agent`).** Přeloží „číslo rezervace" od zákazníka na plné UUID. Zákazník v potvrzovacím mailu i jeho předmětu vidí KRÁTKOU referenci `#XXXXXXXX` = **POSLEDNÍCH 8 znaků** UUID (`booking_number = id.slice(-8).toUpperCase()`), NE první blok — proto agent dřív chybně vyžadoval celé 36znakové UUID. Funkce přijme: plné UUID (i bez pomlček / 32 hex), nebo 8znakovou hex referenci (match `right(replace(id::text,'-',''),8)`, case-insensitive). Vrací `{success, booking_id}` nebo `{success:false, error}` (`bad_ref`, `not_found`, `ambiguous` = krátká ref sedí na >1 rezervaci). Volá ji edge `ai-public-agent` (`resolveBookingRef`) PŘED každým booking toolem; plné UUID si edge přeloží sám bez DB. **Pozn.:** match na `right(...,8)` není indexovaný (seq scan bookings) — pro řádový růst přidat functional index. SECURITY DEFINER, GRANT anon/authenticated. |
| `find_booking_light(p_booking_id)` | **NEW 2026-06-21 (C) (PŘEDLOŽENO JAKO TEXT — nutno aplikovat před deployem `ai-public-agent`).** LIGHT read-only lookup rezervace pro AI agenta JEN přes `booking_id` (bez kontaktu a hesla) — pro NEŠKODNÉ úpravy, kde uživatel nemá být šikanován heslem. Ověří jen existenci + `booking_source='web'` + `status IN reserved/active` + `payment_status IN paid/partial_refund/refund_pending`. **Minimalizace PII:** vrací jen stav BEZ osobních údajů (booking_id, status, start/end_date, moto_id, moto_name, pickup/return_method, mods_today_count) — NIKDY jméno/email/telefon/adresu/doklady. Error kódy: `missing_inputs`, `not_found`, `not_web_booking`, `wrong_status`, `not_paid`. SECURITY DEFINER. GRANT EXECUTE TO anon, authenticated. |
| `apply_booking_change_light(p_booking_id, ...9 changes args, p_new_pickup_time, p_reason, p_dry_run)` | **NEW 2026-06-21 (C) (PŘEDLOŽENO JAKO TEXT).** LIGHT úprava rezervace JEN přes `booking_id` (bez hesla). **Bezpečnost — guard nulového dopadu počítá SERVER, ne klient:** vždy nejdřív zavolá `_apply_booking_changes_core(..., p_dry_run := true, p_source := 'ai_agent')` a spočítá dopad. `p_dry_run=true` → vrátí preview jádra + `light` + `light_allowed` (true jen když `net_diff=0 AND payment_required=false AND refund_amount=0`). `p_dry_run=false` → REÁLNOU změnu přes jádro provede POUZE když je dopad nulový; jakmile by změna něco stála/vracela, vrátí `{success:false, error:'full_verification_required', net_diff, payment_required, refund_amount}` a agent spadne na 3FA (`apply_booking_changes_anon`). Limit 3 reálné AI úpravy/den/rezervace (z `modification_history` source='ai_agent'). Předává `user_id` rezervace do jádra. Error kódy: `missing_inputs`, `not_found`, `not_web_booking`, `wrong_status`, `not_paid`, `daily_limit_reached`, `full_verification_required` + všechny kódy z jádra. SECURITY DEFINER. GRANT EXECUTE TO anon, authenticated. |
| `sos_swap_bookings(incident_id, replacement_moto_id, ...)` | SOS výměna motorky — atomický swap |
| `expire_vouchers()` | Automatická expirace voucherů (pg_cron) |
| `expire_vouchers_and_promos()` | Expirace voucherů + deaktivace promo kódů po valid_to |
| `auto_cancel_expired_pending()` | **UPDATE 2026-08-10 (`20260810_restore_to_pending.sql`):** přidán guard `restored_at IS NULL` — rezervace OBNOVENÁ ve Velíně (tlačítko Obnovit → pending/unpaid + `bookings.restored_at`) se NIKDY neruší automaticky (lhůty jedou z `created_at`, obnovená by padla do 2 min); řeší ji admin ručně. **UPDATE 2026-05-14:** Auto-cancel pending+unpaid bookings (app: 10min, web: 4h). Pro **app** zdroj volá `send-cancellation-email` přes `pg_net.http_post` (refund=0, není platba → mail bez dobropisu). Pro **web** zdroj se mail NEPOSÍLÁ — webová pending+unpaid rezervace už dostala „Nedokončená rezervace" (`web_booking_abandoned`) z `send_abandoned_booking_emails()` po 15 min, druhý storno mail by byl chybný (rezervace fakticky nikdy nebyla potvrzená). `cancelled_by_source='auto'`. SECURITY DEFINER. pg_cron každé 2 min. Migrace `20260514_fix_abandoned_email_15min_velin_template.sql`. |
| `detect_customer_language(user_id, booking_id, order_id)` | **NEW 2026-05-03 (i18n 5.1):** Vrací jazyk zákazníka z profiles → bookings.language → shop_orders.language → 'cs' default. Použito v send-* edge funkcích a triggerech pro lokalizaci customer comms. STABLE SECURITY DEFINER. GRANT TO anon, authenticated, service_role |
| ~~`get_accessory_availability(p_moto_id, p_start, p_end, p_pickup_method)`~~ | **odstraněno 2026-06-24** (není v živém snapshotu). **NEW 2026-05-05 (inventory_v2):** Vrací JSONB mapu `{type: {size: available_qty}}` pro krok 5 rezervace na webu. Spočte fyzický počet kusů na pobočce motorky (`branches.id = motorcycles.branch_id`) a odečte souběžné rezervace (status pending/reserved/active s overlap termínem) podle helmet_size/jacket_size/gloves_size/pants_size/boots_size sloupců (rider + passenger). Při `p_pickup_method='delivery'` poolu napříč všemi pobočkami (sum quantity, overlap přes všechny moto). Filtruje typy z `accessory_types` podle `audience` matchnutého na `motorcycles.license_required` (N=child→child+both; jinak adult+both). STABLE SECURITY DEFINER. GRANT TO anon, authenticated. Web volá z `pages-rezervace.js#MG._loadAccessoryAvailability` jen když `feature_flags.inventory_v2.enabled=true`. |
| ~~`set_booking_language(booking_id, language)`~~ | **odstraněno 2026-06-24** (není v živém snapshotu). **NEW 2026-05-03 (i18n 5.7):** Post-create helper — frontend (web/app) volá po create_web_booking pro uložení detekovaného jazyka do `bookings.language`. Validuje proti seznamu 7 podporovaných jazyků (cs/en/de/nl/es/fr/pl). SECURITY DEFINER. GRANT TO anon, authenticated, service_role |
| ~~`set_shop_order_language(order_id, language)`~~ | **odstraněno 2026-06-24** (není v živém snapshotu). **NEW 2026-05-03 (i18n 5.7):** Stejně jako set_booking_language ale pro shop_orders.language. Volání z motogo-web-php/js/checkout.js a Flutter shop_provider.createShopOrder po create_web_shop_order/create_shop_order. SECURITY DEFINER |
| `claim_email_send(p_booking_id uuid, p_slug text, p_window_minutes int DEFAULT 30)` | **NEW 2026-08-12 (PŘEDLOŽENO JAKO TEXT — nutno aplikovat).** Atomický claim odeslání zákaznického mailu: `INSERT INTO email_send_locks ON CONFLICT (booking_id, template_slug) DO UPDATE SET locked_at=now() WHERE locked_at < now() - okno` — vrací TRUE právě jedné invokaci v okně (default 30 min), souběžné/opakované dostanou FALSE. Volá `send-cancellation-email` PŘED odesláním (fallback na starou sent_emails kontrolu, když RPC chybí). Vzniklo kvůli incidentu #DDC5A69D (4 duplicitní storno maily z souběžných invokací — read-then-write race). SECURITY DEFINER, `SET search_path=public`, GRANT service_role. |
| `set_my_license_group(p_groups text[])` | **NEW 2026-06-13 (`20260613_set_my_license_group.sql`):** Uloží skupinu ŘP přihlášeného zákazníka do `profiles.license_group`. Důvod: sloupec je ENUM pole (`license_group[]`) a PostgREST ho z prostého JSON pole NEUMÍ přetypovat → přímý UPDATE/UPSERT z mobilní appky, který license_group obsahoval, CELÝ padal a uložilo se jen jméno/e-mail/telefon z handle_new_user triggeru (adresa, datum narození, čísla dokladů i skupina ŘP zůstaly prázdné, stát spadl na default). Appka proto license_group z přímého zápisu vyjímá a posílá ho sem; funkce filtruje platné hodnoty (`AM/A1/A2/A/B/N`), uppercase, cast `text[]`→`license_group[]`, `UPDATE profiles SET license_group WHERE id=auth.uid()`. Volá editace profilu (`profile_screen._save`) + fallback registrace. Vrací `{success, count}` nebo `{error:'not_authenticated'}`. SECURITY DEFINER (píše jen vlastní řádek přes `auth.uid()`). GRANT EXECUTE TO authenticated. |
| `app_save_full_profile(p_data jsonb)` | **NEW 2026-06-13 (`20260613_app_save_full_profile.sql`):** Uloží CELÝ profil přihlášeného zákazníka jedním voláním — registrace v mobilní appce (`auth_provider.signUp`). Stejný princip jako `create_web_booking` na webu: přímý PostgREST zápis z appky padal na (a) RLS u INSERT větve upsertu a (b) ENUM poli `license_group[]`, takže se uložila jen trigger metadata. RPC běží server-side (obejde RLS i typy), upsertne řádek (pojistka INSERT id+email z auth.users) a UPDATE všech polí: full_name, phone, date_of_birth, street, city, zip, country, id_number, license_number, license_expiry, `license_group` (cast z JSON pole stringů → `license_group[]`, jen platné AM/A1/A2/A/B/N), language, registration_source='app', consentů. COALESCE(NULLIF(...,''), existující) = prázdné pole nepřepíše. Vrací `{success:true}` / `{error:'not_authenticated'}`. SECURITY DEFINER (jen vlastní řádek přes `auth.uid()`). GRANT EXECUTE TO authenticated. **UPDATE 2026-07-01 (`20260701_app_save_full_profile_marketing_optin.sql`, APLIKOVÁNO — success):** souhlasy se už nenastavují paušálně na `true`. **`marketing_consent` = OPT-IN** — RPC ho už nezapíná, bere jen explicitní hodnotu z p_data (appka ji zatím neposílá → `false`); zároveň ALTER defaultu sloupce `profiles.marketing_consent` na `false`. Povinné souhlasy **VOP/GDPR/data_processing** = `COALESCE((p_data->>'consent_*')::boolean, true)` — appka je nově reálně gacuje v UI registrace (krok 3 checkboxy `register_screen.dart`, oba Flutter balíky) a posílá jejich hodnotu. Transakční kanály (email/sms/push/whatsapp/photo/contract) zůstávají `true`. |
| `set_booking_form_seconds(p_booking_id uuid, p_started_at timestamptz)` | **NEW 2026-05-23:** Post-create helper — web (`pages-rezervace-steps.js`) volá po create_web_booking s ISO timestampem otevření formuláře (sessionStorage `mg_rez_started_at`). Uloží `bookings.form_started_at` + spočte `form_fill_seconds = now - p_started_at` (clamp 0–86400). Pouze `booking_source='web'`, jen první naměření (`form_started_at IS NULL` → resume/Zpět nepřepíše), p_started_at musí být v ≤24 h. SECURITY DEFINER. GRANT TO anon, authenticated, service_role. (mig. `20260523_booking_form_fill_seconds.sql`; dropuje starší variantu `(uuid, integer)`.) |
| `get_document_translation(template_slug, language)` | **NEW 2026-05-03 (i18n 5.6):** Vrací HTML z document_templates.content_translations[language] pokud existuje, jinak content_html (CZ default). Smlouva/VOP infrastruktura — překlady pro right-side bilingvní layout doplní admin přes Velin UI postupně. STABLE SECURITY DEFINER |
| `list_pending_ai_suggestions(p_limit)` | **NEW 2026-05-05 (AI customer messages):** Vrací frontu inbound zpráv s `ai_suggestion_status='pending'` (čekající na schválení adminem) — message_id, thread_id, channel, customer_id, inbound content, AI návrh, confidence, admin_note, časové razítko. JOIN přes message_threads. ORDER BY created_at DESC, default LIMIT 50. STABLE SECURITY DEFINER. GRANT TO authenticated. Velín → Zprávy → fronta AI návrhů (etapa 4). |
| `send_message_via_edge(channel, to, slug, vars, customer_id, booking_id, language)` | **UPDATE 2026-05-03 (i18n 5.4):** Helper pro send-message edge fn. **FIX:** Čte URL/key z `app_settings` tabulky (GUC `app.settings.*` na Supabase managed nefunguje). **+ p_language parametr** — předá do edge fn jako `language`, která načte per-jazyk řádek z `message_templates`. SECURITY DEFINER |
| `send_sms_and_wa(...)` | **UPDATE 2026-05-03:** Wrapper kolem send_message_via_edge volající 'sms' i 'whatsapp' s p_language parametrem. SECURITY DEFINER |
| `send_abandoned_booking_emails()` | **REFACTOR 2026-05-14:** Cron funkce pokrývá **2 stavy** rezervace v jednom běhu. **Path AB** (web pending+unpaid, **15 min od `created_at`**) → odešle jediný mail `type=booking_abandoned, source=web` přes `send-booking-email`. Edge fn pak vyzvedne DB šablonu z Velína přes `resolveSlug()` — primárně `web_booking_abandoned`, fallback `booking_abandoned`. `pay_url` = `stripe_checkout_url` pokud existuje, jinak `https://www.motogo24.cz/rezervace?resume=<id>` (klient se vrátí na krok 2 a doplní Mindee scan i platbu). Sjednoceno z původních Path A (chybí doklady) + Path B (doklady OK) — texty obou stavů řeší jediná Velín šablona. **Path C** (web reserved/active+paid+chybí doklady, 5 min od `confirmed_at`) beze změny — pošle `booking_missing_docs` s odkazem na `motogo24.com/upravit-rezervaci?id=<id>#doklady`. Detekce dokladů přes `check_booking_docs_status(user_id, end_date, moto_id)` (NULL = doklady OK, dětská motorka N = NULL automaticky). Dedup: AB přes `abandoned_email_sent_at`, C přes `docs_reminder_sent_at`. URL/key z `app_settings` rows (GUC nelze na Supabase managed). Inner blok `EXCEPTION WHEN OTHERS THEN RAISE WARNING`. SECURITY DEFINER. Cron `send-abandoned-booking-emails` `*/2 * * * *`. Vrací `{sent_abandoned, sent_missing_docs}`. Migrace `20260514_fix_abandoned_email_15min_velin_template.sql`. **FIX 2026-05-26 (`20260526_fix_abandoned_cron_docs_status_cast.sql`):** Path C volala `check_booking_docs_status(b.user_id, b.end_date, b.moto_id)`, ale `bookings.end_date` je `timestamptz` a jediná existující signatura je `(uuid, date, uuid)` → cron padal KAŽDÝ běh na `function check_booking_docs_status(uuid, timestamp with time zone, uuid) does not exist`. Protože je celá fn jedna transakce, neošetřená výjimka v Path C **rollbackla i Path AB** (zařazení abandoned mailu do pg_net + UPDATE `abandoned_email_sent_at`) → web pending+unpaid rezervacím nikdy nedorazil „Nedokončená rezervace" mail a v `sent_emails` nebyl ani failed řádek. Oprava: 1) cast `b.end_date::date`; 2) docs-check přesunut z `WHERE` do těla smyčky, izolovaný ve vlastním `BEGIN/EXCEPTION WHEN OTHERS` (`CONTINUE` při chybě) — budoucí chyba docs-checku už nikdy nezabije Path AB. Path AB beze změny. **FIX 2026-05-30 (`20260530_missing_docs_skip_serviced_branch.sql`):** Path C posílala `booking_missing_docs` KAŽDÉ zaplacené web rezervaci s nenahranými doklady bez ohledu na typ pobočky / způsob vyzvednutí → zaplacená rezervace s motorkou na OBSLUŽNÉ pobočce (MotoGo24 Mezná, `branches.type='obslužná'`) dostala připomínku, ačkoli na obslužné pobočce ověří OP/ŘP obsluha osobně při předání (nahrání předem je dobrovolné). Oprava: Path C dojoinuje `branches` přes `motorcycles.branch_id` a do `WHERE` přidává `COALESCE(br.type,'') <> 'obslužná'` + `COALESCE(b.pickup_method,'') <> 'delivery'` — připomínka chodí JEN pro samoobslužné vyzvednutí na pobočce (`branches.type` ∈ {`samoobslužná`,`obslužná`} z `BranchModal.jsx`; `pickup_method` ∈ {`pickup_at_rental`,`delivery`}), kde je nahrání nutnost pro vydání přístupových kódů ke dveřím. Obslužná pobočka i delivery (obsluha přiváží + ověří doklady osobně) se přeskočí. Path AB beze změny. **FIX 2026-07-16 (`20260716_missing_docs_skip_verified.sql`):** Path C od 20260712 gatovala jen na `docs_completed_at IS NULL` (bez docs-checku) → **přihlášený s doklady ověřenými ve Velíně** (krok dokladů přeskočil → `docs_completed_at` NULL) a **nový zákazník s vyplněnými jen ČÍSLY** (scany nepovinné) dostávali falešný `booking_missing_docs`, přestože už dostali `web_booking_reserved`. Oprava: do těla Path C vrácena `check_booking_docs_status(user_id, end_date::date, moto_id)` — `NULL` (doklady ověřené) → `CONTINUE` (žádná připomínka); izolováno v `BEGIN/EXCEPTION WHEN OTHERS→CONTINUE` (nezabije Path AB). 30 min práh + obslužná/delivery skip zachovány. Path AB beze změny.|
| ~~`send_abandoned_booking_emails()` původní (NEW 2026-04-29 / 2026-05-02 refactor)~~ | **NAHRAZENO 2026-05-14 jednotnou cestou AB (15 min od `created_at`, jediný Velín slug `web_booking_abandoned`).** Původně řešilo jen pending+unpaid (jeden typ emailu). Pokud je `checkout_started_at IS NOT NULL` a `now() − checkout_started_at ≥ 10 min` → odkaz vede přímo na uloženou Stripe Checkout URL. Jinak `now() − created_at ≥ 20 min` → odkaz vede na `motogo24.com/rezervace?resume=<id>` (klient se vrátí na krok 2). Po odeslání nastaví `bookings.abandoned_email_sent_at = now()` (deduplikace, jeden mail na booking). Volá `send-booking-email` přes `pg_net.http_post`; URL + service_role_key čte z **`app_settings`** rows (`key='supabase_url'`, `key='service_role_key'`) — GUC `app.settings.*` na Supabase managed nelze ALTER DATABASE-ovat (permission denied). Inner blok má `EXCEPTION WHEN OTHERS THEN RAISE WARNING` — selhání jednoho mailu nezablokuje další iterace. SECURITY DEFINER. Cron `send-abandoned-booking-emails` každé 2 min. |
| `send_missing_booking_reserved_emails()` | **NEW 2026-06-17 (`20260617_catchup_booking_reserved_email.sql`):** Záchranný cron pro nedoručený `booking_reserved` mail. **BUG (rezervace #E60E74DD, 2026-06-16):** zákazník dostal `web_door_codes`, ale NE `web_booking_reserved`. Door codes jdou DB triggerem přes pg_net (bez příloh) → spolehlivé; `booking_reserved` se posílá SYNCHRONNĚ uvnitř Stripe webhooku (`confirmBookingPayment` v `payment-confirmers.ts`), kde `send-booking-email` musí nejdřív vygenerovat 4 PDF přílohy (`ZF, DP, Smlouva, VOP` přes PDFShift). V časově omezené webhookové cestě se to „utne" → mail tiše zmizí (bez řádku v `sent_emails`/`message_log` i bez `debug_log`, protože try/catch vše spolkl). Potvrzeno z dat: PI event vyhrál atomic flip (`confirm_booking_payment` ok, `was_already_paid=false`), ale žádný `web_booking_reserved` řádek nevznikl. **Funkce:** najde web rezervace `status IN (reserved,active)` + `payment_status='paid'` + `confirmed_at < now()-3min` (grace pro realtime cestu), kterým v `message_log` chybí `web_booking_reserved`/`booking_reserved` (channel='email') A `reserved_email_sent_at IS NULL`, a došle mail přes `send-booking-email` (`type='booking_reserved'`, `source` dle `booking_source`). Běží v čistém kontextu mimo webhook → plný čas na generování příloh. **Dedup dvojvrstvý:** (1) `NOT EXISTS message_log` = realtime mail úspěšný → cron skipne; (2) `reserved_email_sent_at` marker (UPDATE po dispatchi) = cron se sám nezdvojí během dlouhé generace příloh (jeden pokus na rezervaci; realtime cesta marker nenastavuje, tam jistí jen message_log). URL/key z `app_settings`. Inner blok `EXCEPTION WHEN OTHERS THEN RAISE WARNING`. SECURITY DEFINER. Cron `send-missing-booking-reserved-emails` `*/2 * * * *`. Vrací `{sent_reserved}`. Ověřeno: backfill 2 chybějících mailů, druhý běh `{sent_reserved:0}`. Doplňkově `payment-confirmers.ts` nově loguje výsledek odeslání booking_reserved do `debug_log` (`booking_reserved_mail_sent`/`_http_error`/`_failed`/`_no_email`/`_outer_exception`). |
| `auto_complete_expired_bookings()` | **UPDATE 2026-08-20 (`20260820_test_calendar_seed_sep_oct.sql`):** oba kroky nově přeskakují `is_test IS TRUE` — testovací rezervace (seed obsazenosti kalendáře) se NIKDY nesmí aktivovat/dokončit (jinak by generovaly door codes, 0Kč KF a completed maily); zůstávají navždy `reserved`. **UPDATE 2026-07-19 (`20260719_obsluzna_activate_on_handover.sql`):** Nově **DVOUKROKOVĚ** — (1) paid `reserved` po `end_date` promotuje `reserved→active` (nastaví `picked_up_at`, `handover_protocol_filled_at`=now + `handover_protocol_autofilled=true` pokud byl NULL → projde strážcem `trg_gate_obsluzna_activation`), (2) `active→completed`. Důvod: KF trigger `generate_final_invoice_on_complete` střílí JEN na `active→completed`; po zavedení „aktivace až předáním protokolu" na obslužné pobočce (viz `_gate_obsluzna_activation` / `trg_gate_obsluzna_activation` níže + STATE_4) může rezervace zůstat `reserved` (no-show / obsluha nepotvrdila) a přímé `reserved→completed` by KF, poděkovací mail i slevový kód minulo. Krok 1 to zajistí i pro takové rezervace. Door kódy z reserved už existují (idempotentní, žádná nová SMS), completed je vzápětí deaktivuje. **UPDATE 2026-06-11:** Auto-complete: active/reserved + end_date < today + payment_status IN (**paid, partial_refund, refund_pending**) → completed (BEZ fakturace — KF generuje trigger). Rozšíření filtru: rezervace po rozdílové úpravě s vratkou má `partial_refund` a dřív se nikdy neauto-dokončila (zůstávala navždy active, KF + poděkovací mail nevznikly). SECURITY DEFINER |
| `generate_final_invoice_on_complete()` | **UPDATE 2026-06-11 (`20260611_kf_zero_credit_notes.sql`):** Trigger funkce: generuje KF (konečnou fakturu) při přechodu active→completed. Rozpis položek: Pronájem (brutto = total_price + discount_amount − extras − delivery), Příslušenství, Přistavení, Sleva (záporná), Odpočty DP (všechny: původní platba i rozdílové z úprav), **+ NOVĚ řádky „Vráceno dobropisem DB-…" (+\|credit_note\|)** — bez nich KF po úpravě s vratkou vycházela −\|refund\|. Guard rozšířen z `payment_status='paid'` na IN (paid, partial_refund, refund_pending). Assertion payload nově obsahuje `sum_credit_notes`. Matematika: Σslužby − ΣDP + Σ\|dobropis\| = 0. **+ LOYALTY 2026-06-11 (`20260611_loyalty_ranks.sql`):** brutto pronájmu += `loyalty_discount_amount`, nový záporný řádek „Věrnostní sleva X % — rezervace přes aplikaci MotoGo24", assertion payload + `loyalty_discount`. **+ Slevový kód `VRACENI-{booking_short}` (200 Kč, 1 rok od vystavení KF, max_uses=1, fixed)** vystavený do `promo_codes` (sloupce `code,type,value,active,valid_from,valid_to,max_uses,used_count`) při INSERT KF, idempotentní `ON CONFLICT (code) DO NOTHING`. **FIX 2026-05-13:** mig. `20260503_email_templates_unified.sql` původně INSERTovala do neexistujících sloupců `discount_value/is_active/description` → padalo v EXCEPTION a slevový kód v DB nevznikl; opraveno na reálné sloupce `value/active`. **+ KF assertion na 0 Kč** — pokud `v_total <> 0` po zaokrouhlení, zalogují se detaily do `debug_log` (warning); KF se přesto vystaví (zákonný požadavek). **+ LATE-PICKUP 2026-06-18 (`20260618_late_pickup_discount_kf.sql`):** brutto += `late_pickup_discount_amount`, záporný řádek „Sleva 50 % na 1. den (pozdní vyzvednutí)". **+ #3 ZÁPORNÁ KF → DOBROPIS 2026-06-23 (`20260623_negative_kf_to_credit_note.sql`):** KF číslo nově přes `next_document_number('KF')` (atomické, viz `next_document_number`); když po odečtu DP + dobropisů vyjde `v_total < 0` (záloha převýšila finální cenu — typicky sleva/test po složení zálohy), vystaví se `credit_note` (DB, `source='final_overpay'`, číslo přes `next_document_number('DB')`) na přeplatek a KF se dorovná řádkem „Vráceno dobropisem DB-…" na 0 — místo dříve záporné konečné faktury (bug KF-2026-0014 = −1168). Jen DOKLAD (žádný Stripe refund/mail). Loyalty i late-pickup řádky zachovány. Týká se jen budoucích dokončení (trigger skipuje rezervace s existující KF). SECURITY DEFINER |
| `next_document_number(p_prefix)` | **NEW 2026-06-23 (`20260623_atomic_document_numbering.sql`, #11):** Atomicky přidělí další číslo doklad. řady `PREFIX-YYYY-NNNN` (KF/DB/DP/ZF/FV). Upsertne řádek do `document_number_counters`, dorovná `last_seq` na `GREATEST(last_seq, živé MAX faktur té řady) + 1` v jednom `UPDATE … RETURNING` (row-lock = bezpečný souběh) → nikdy nevrátí nižší číslo než dosud nepřepsané edge fn s `MAX+1` (bezpečná koexistence). Řeší duplicity (`DB-2026-0001` 2×). Volá KF trigger `generate_final_invoice_on_complete`; **TODO:** přepsat i `process-refund`/`generate-invoice` z `MAX+1` na tuto fci pro plné otužení proti souběhu. Pojistka UNIQUE index `invoices_number_unique`. SECURITY DEFINER. GRANT authenticated, service_role. |
| `trg_send_booking_completed_email()` | **UPDATE 2026-05-03:** Trigger funkce: po vložení KF (invoices.type='final') zavolá send-booking-email (type='booking_completed') přes pg_net — pošle zákazníkovi poděkování s KF v příloze + slevový kód `VRACENI-*` (vyzvedne z `promo_codes`). **FIX:** Čte URL/key z tabulky `app_settings` (`value #>> '{}'`) — předchozí GUC `current_setting('app.settings.*')` na Supabase managed nefunguje a trigger tiše selhával bez doručení mailu. Dedup přes message_log. SECURITY DEFINER |
| `trg_send_shop_order_confirmed_email()` | **NEW 2026-05-03:** Trigger funkce na shop_orders (AFTER UPDATE OF payment_status, WHEN pending→paid). Volá send-booking-email s `type='shop_order_confirmed'` přes pg_net — pošle zákazníkovi potvrzení e-shop objednávky + DP. Dedup přes `message_log.content_preview LIKE '%order_number%'`. Source rozlišuje `web` (anonymní, customer_id IS NULL) vs `app`. SECURITY DEFINER |
| `trg_send_booking_modified_email()` | **NEW 2026-05-03 (B), INSTRUMENTOVÁN 2026-05-19:** Trigger funkce na bookings (AFTER UPDATE). Detekuje změnu klíčových polí (moto_id, start/end_date, total_price, pickup_method/address/time, return_method/address/time). Volá send-booking-email s `type='booking_modified'` + plný `original_*` payload z OLD → frontend vyrobí diff tabulku „Původní vs Nové". Pokrývá všechny tři kanály (Velin / web / Flutter / RPC apply_booking_changes / shorten_booking_with_refund) jediným místem. Status changes (reserved↔active↔completed↔cancelled) NEPOSÍLAJÍ — handlers v jiných triggerech. Dedup 5min window přes message_log (Velin/app může volat explicitně, druhý mail se skipne). **2026-05-19 instrumentace** (mig. `20260519_instrument_booking_modified_trigger.sql`): každý z 6 early-return paths + výsledek http_post/dispatch_email_event zapisuje řádek do `debug_log` (`source='trg_booking_modified_email'`, `action ∈ {skipped_status_change, skipped_status_not_eligible, skipped_no_field_change, skipped_dedup_5min, app_settings_missing, skipped_no_email, http_post_queued, http_post_failed, dispatch_event_failed, trigger_outer_exception}`). `http_post` a `dispatch_email_event` izolovány do vlastních BEGIN/EXCEPTION bloků — selhání jednoho nerollbackne ten druhý. SECURITY DEFINER |
| `auto_activate_reserved_bookings()` | **UPDATE 2026-08-20 (`20260820_test_calendar_seed_sep_oct.sql`):** přeskakuje `is_test IS TRUE` (testovací rezervace se nikdy neaktivují — viz auto_complete_expired_bookings). **UPDATE 2026-07-19 (`20260719_obsluzna_activate_on_handover.sql`):** Auto-activate `reserved + paid + start_date <= today → active` (+picked_up_at) — nově **VYNECHÁVÁ obslužné pobočky** (`branches.type='obslužná'` přes motorcycles.branch_id), ty se aktivují AŽ předávacím protokolem ve Velíně (viz `_gate_obsluzna_activation`). Samoobslužná / svoz / ostatní beze změny. SECURITY DEFINER |
| `_gate_obsluzna_activation()` | **NEW 2026-07-19 (`20260719_obsluzna_activate_on_handover.sql`):** Trigger fn (BEFORE UPDATE OF status, `trg_gate_obsluzna_activation`). Tvrdý strážce: každý přechod `→active` (z ne-active) na **obslužné pobočce** BEZ vyplněného `handover_protocol_filled_at` srazí zpět na `reserved` (a nechá `picked_up_at` na OLD). Cíl: na obslužné pobočce se aktivuje AŽ podpisem/uložením předávacího protokolu nebo potvrzením tištěného protokolu ve Velíně (`CheckInModal.markPickedUp` / `PickupReadiness` nastaví `handover_protocol_filled_at` spolu s active → projde). Odchytí i `confirm_payment` (same-day platba by jinak šla `pending→active`) BEZ nutnosti přepisovat křehkou `confirm_payment` (revive/atomic dedup). Auto-complete promote krok nastaví `handover_protocol_filled_at` → projde taky. Mimo obslužnou (samoobslužná/svoz/NULL) = no-op. SECURITY DEFINER, `SET search_path=public`. |
| `check_user_booking_overlap()` | Trigger: per-user overlap check — zákazník nesmí mít 2 překrývající se rezervace. Výjimka: dětské motorky (license_required=N). SECURITY DEFINER |
| `confirm_payment(booking_id, method)` | RPC: označí booking jako zaplacený. start_date<=dnes → pending→**active** (+picked_up_at), start_date>dnes → pending→**reserved** (+confirmed_at). **2026-05-08:** ATOMIC dedup — UPDATE WHERE payment_status<>'paid' RETURNING; vrací `was_already_paid` flag. Druhý paralelní Stripe webhook (checkout.session.completed vs payment_intent.succeeded) získá `was_already_paid=true` a neopouští žádný side-effect (mail/door codes). **UPDATE 2026-05-28:** přidána reaktivace cancelled rezervace — pokud zákazník zaplatí rezervaci se statusem `pending`/`cancelled` (enum `booking_status` `rejected` neobsahuje), RPC ji přepne na `reserved`/`active` (podle start_date) a vyčistí `cancelled_at`/`cancelled_by`/`cancelled_by_source`/`cancellation_reason`/`cancellation_notified` (jinak by UI dál ukazovalo „Zrušeno" badge). Vrací nově `revived:bool` (jen pro plátce, ne pro paralelní webhook). Logování `cancelled_booking_revived` do debug_log. Fix flow: zákazník v appce vytvořil rezervaci → zavřel × FAB banner → cancelPendingBooking → status=cancelled → doplnil kartu v profilu → dotáhl platbu → vznikalo paid+cancelled, nyní se rezervace korektně reaktivuje. SECURITY DEFINER |
| `confirm_shop_payment(order_id, method)` | RPC: označí shop objednávku jako zaplacenou (SECURITY DEFINER). **2026-06-06 ATOMIC dedup:** `UPDATE … WHERE payment_status IS DISTINCT FROM 'paid' RETURNING` + vrací `was_already_paid` flag. Druhý paralelní Stripe webhook (checkout.session.completed × payment_intent.succeeded) dostane `was_already_paid=true` a webhook (`confirmShopPayment`) skipne mail/dokumenty/voucher — dřív RPC dedup neměla a `web_voucher_purchased` mail (i e-shop maily) chodily 2× kompletně s přílohami. Ověření vlastníka/admina zachováno (service_role + anonymní web voucher s `customer_id IS NULL` projdou). |
| `check_booking_overlap()` | Trigger funkce: kontrola překrytí rezervací |
| ~~`generate_shop_invoice()`~~ | **odstraněno 2026-06-24** (není v živém snapshotu; shop faktura nyní přes `generate_shop_final_on_ship` — viz STATE_4). Trigger funkce: auto-faktura při zaplacení shop objednávky (type='shop_final', source='shop', SECURITY DEFINER) |
| `auto_process_voucher_order()` | BEFORE UPDATE trigger na shop_orders: při payment_status→'paid' automaticky generuje voucher kódy, posílá in-app notifikaci, nastavuje status (delivered pro digitální, confirmed pro mixed). SECURITY DEFINER |
| `send_message_via_edge(channel, recipient, slug, vars, customer_id, booking_id)` | Odešle zprávu přes Edge Function `send-message` pomocí pg_net. Loguje do `message_log`. SECURITY DEFINER |
| `send_sms_and_wa(to, slug, vars, customer_id, booking_id)` | Helper: odešle SMS + WhatsApp přes `send_message_via_edge`. SECURITY DEFINER |
| ~~`mark_thread_messages_read(p_thread_id)`~~ | **odstraněno 2026-06-24** (není v živém snapshotu). RPC: označí admin zprávy ve vlákně jako přečtené (read_at=now). Ověřuje vlastnictví vlákna. SECURITY DEFINER |
| `get_unread_thread_message_count(p_customer_id)` | RPC: vrací počet nepřečtených admin zpráv napříč všemi vlákny zákazníka. SECURITY DEFINER |
| `auto_generate_door_codes()` | Trigger funkce: auto-generuje 2 přístupové kódy (motorcycle+accessories) při přechodu bookingu na 'active'/'reserved'. Posílá kódy jako admin_message + email jen pokud má doklady. **UPDATE 2026-05-13:** přidaný lookup `motorcycles.license_required` — pro dětskou motorku (`license_required='N'`) se docs check úplně skipuje a kódy se rovnou vydají (`v_has_docs:=true`). **UPDATE 2026-05-20 (`20260520_door_codes_require_photo_or_ocr.sql`):** zrušeny oba „číslo-only" fallbacky (legacy `contract/protocol + license_number` i `license_number + id_number`). `v_has_docs` u dospělé motorky teď vyžaduje pro **KAŽDÝ** doklad zvlášť **fotku NEBO reálné OCR (`verified_at`)**: ŘP = `documents` typu `drivers_license`/`license_photo` NEBO `profiles.license_verified_at`; totožnost = `documents` typu `id_card`/`id_photo`/`passport` NEBO `profiles.id_verified_at`/`passport_verified_at`. Holé ručně vypsané `license_number`/`id_number` už kódy NEUVOLNÍ (slouží jen do smlouvy). Fotka se počítá i při neúspěšném OCR (mindee_status='failed'). Důvod: optimizer/test napsal jen čísla do formuláře a kódy přesto odešly. SECURITY DEFINER, EXCEPTION safe |
| `verify_customer_docs(p_ocr_name, p_ocr_dob, p_ocr_id_number, p_ocr_license_number, p_ocr_license_category, p_ocr_license_expiry, p_rental_end)` | Verifikace naskenovaných dokladů proti profilu. Kontroluje jméno, datum narození, číslo ŘP, platnost ŘP (i proti datu konce rezervace), skupiny ŘP. Vrací jsonb {success, status, mismatches, warnings}. SECURITY DEFINER |
| `auto_deactivate_door_codes()` | Trigger funkce: deaktivuje všechny aktivní kódy (is_active=false) při přechodu bookingu na 'completed' nebo 'cancelled'. SECURITY DEFINER, EXCEPTION safe |
| `release_withheld_door_codes()` | Trigger funkce na `documents` (AFTER INSERT, type IN id_card/passport/drivers_license/id_photo/license_photo): při nahrání dokladů uvolní zadržené door codes pro všechny aktivní bookings uživatele, posílá `door_codes` admin_message + SMS/WA. SECURITY DEFINER, EXCEPTION safe |
| `regen_door_codes_on_moto_change()` | Trigger funkce na `bookings` (AFTER UPDATE OF moto_id, status IN active/reserved): deaktivuje staré kódy a vygeneruje 2 nové pro novou motorku. Pokud má doklady → admin_message + SMS/WA. SECURITY DEFINER, EXCEPTION safe |
| `send_push_via_edge(user_id, title, body, data jsonb)` | Helper: pošle FCM push notifikaci přes Edge Function `send-push` (pg_net). **FIX 2026-06-06 (`20260606_fix_send_push_via_edge_app_settings.sql`):** čte `supabase_url` + `service_role_key` z **tabulky `app_settings`** (`value #>> '{}'`), ne z GUC `current_setting('app.settings.*')` — ten je na hostovaném Supabase prázdný (ALTER DATABASE = permission denied), takže pg_net.http_post šel „nikam" a push z triggeru `trg_push_on_admin_message` se nikdy neodesílal. Stejný GUC bug byl 2026-05-03 opraven u `send_message_via_edge` (SMS/WA), u pushe zůstal. Signatura beze změny (uuid, text, text, jsonb). SECURITY DEFINER, EXCEPTION safe |
| `send_door_codes_email(booking_id, user_id)` | Helper: pošle email s přístupovými kódy přes Edge Function `send-booking-email` (type=`door_codes`). Dedup přes `message_log` (template_slug='door_codes'/'web_door_codes', channel='email'). Volá se z `auto_generate_door_codes`, `release_withheld_door_codes`, `regen_door_codes_on_moto_change`, `release_my_door_codes`. SECURITY DEFINER, EXCEPTION safe |
| `trg_push_on_admin_message()` | Trigger funkce na `admin_messages` (AFTER INSERT): volá `send_push_via_edge()` s deep-link payloadem (`type: door_codes` nebo `message`, `id`). SECURITY DEFINER, EXCEPTION safe |
| `release_my_door_codes(p_booking_id)` | RPC pro authenticated zákazníka: ověří doklady a uvolní zadržené door codes pro vlastní booking. Posílá `door_codes` admin_message + SMS/WA + email (`send_door_codes_email`). Vrací jsonb {success, released, error?, withheld_reason?}. **UPDATE 2026-06-01:** kontrola dokladů sjednocena s kanonickou `check_booking_docs_status(v_uid, end_date, moto_id)` — dříve měla vlastní zastaralou inline logiku, která vyžadovala FYZICKÉ řádky v `documents` (id_card/passport + drivers_license) a ignorovala `profiles.*_verified_at`. To způsobovalo falešné „doklady nejsou kompletní" v `motogo24.cz/upravit-rezervaci?` (tlačítko „Hotovo – uložit doklady"), když OCR proběhl a nastavil `verified_at` (karty svítily ✓), ale řádky v `documents` chyběly/lišily se. Nově akceptuje **fotka NEBO OCR `verified_at`**, respektuje child-bike (`license_required='N'`) i expiraci ŘP — stejně jako `auto_generate_door_codes` / `release_withheld_door_codes_for_user`. `withheld_reason` teď nese přesný důvod z `check_booking_docs_status` (Chybí doklad totožnosti / Chybí ŘP / ŘP propadlý DD.MM.YYYY). SECURITY DEFINER. GRANT EXECUTE TO authenticated. |
| `check_booking_docs_status(p_user_id, p_end_date, p_moto_id default null)` | Vrací NULL pokud má zákazník platné doklady pro rezervaci končící p_end_date, jinak konkrétní withheld_reason: `Chybí doklady (OP/pas/ŘP)`, `Chybí doklad totožnosti (OP/pas)`, `Chybí ŘP`, `ŘP propadlý DD.MM.YYYY`. **UPDATE 2026-05-13:** přidaný volitelný 3. parametr `p_moto_id` — pokud je předán a moto má `license_required='N'` (dětská motorka), funkce vrátí NULL bez ohledu na docs. **UPDATE 2026-05-20 (`20260520_door_codes_require_photo_or_ocr.sql`):** sjednoceno s `auto_generate_door_codes` — totožnost i ŘP vyžadují **fotku NEBO reálné OCR (`verified_at`)**, holé `id_number`/`license_number` už nestačí (smazána větev `OR (v_id_num <> '')` na ID straně i požadavek `license_number` na ŘP straně). Zrušen reason `Neúplné údaje ŘP (chybí číslo)` (číslo je jen do smlouvy). Child-skip + kontrola expirace ŘP (`license_verified_until` < `p_end_date`) zachovány. STABLE, SECURITY DEFINER. Používá se v `auto_generate_door_codes`, `get_web_booking_confirmation`, `release_withheld_door_codes_for_user`, `regen_door_codes_on_moto_change`, `release_my_door_codes`. |
| `release_withheld_door_codes_for_user(p_user_id)` | Helper: projede všechny aktivní bookings uživatele, pro každý zvlášť zavolá `check_booking_docs_status` proti jeho `end_date`. Pokud OK → uvolní kódy + notifikuje. Pokud ne → jen přepíše withheld_reason (např. "Chybí doklady" → "ŘP propadlý 20.07.2023"). SECURITY DEFINER, EXCEPTION safe. **UPDATE 2026-05-30 (`20260530_release_withheld_status_guard.sql`):** do `WHERE` smyčky přidán explicitní guard `AND b.status IN ('reserved','active')` — pojistka, aby dodatečné nahrání dokladů (Velín „+ Nahrát doklady" / web / app) k zákazníkovi, který má jen **zrušené/dokončené** rezervace (nebo žádnou nadcházející), NIKDY neuvolnilo kódy ani neposlalo door_codes mail/SMS. Dosud se na to spoléhalo nepřímo přes `bdc.is_active=true` (completed/cancelled má kódy zneaktivněné triggerem `auto_deactivate_door_codes`); guard to dělá explicitně i pro případ nekonzistence dat. Hardening z `20260520` (fotka NEBO reálné OCR) i kontrola expirace ŘP zůstávají — `check_booking_docs_status` beze změny. |
| `_cap_refund_to_paid()` | **FIX 2026-06-11 (`20260611_discount_variant_b.sql` §4):** cap dobropisu nově = `Σ DP (payment_receipt) − Σ už vystavených dobropisů` místo `bookings.total_price` — total se při úpravě snižuje PŘED vystavením dobropisu, takže legitimní dobropis větší než nová cena (zkrácení 3000→1000 = vratka 2000) se chybně ořezával na 1000 a rozcházel se se skutečným Stripe refundem (i KF≠0). Bez DP řádků se neořezává (warning do debug_log; finance drží Stripe clamp). **PŮVODNÍ 2026-05-10** — BEFORE INSERT trigger funkce na `invoices`. Pro `NEW.type='credit_note'` s `NEW.booking_id` ořezávala na `total_price`. Cíl: pojistka proti dobropisu většímu než zaplacená částka v případech, kdy `shorten_booking_with_refund` / `_apply_booking_changes_core` spočítají refund z gross per-day pricingu, ale zákazník zaplatil méně (typicky 100 % slevový kód → reálně zaplaceno 128 Kč, navrhovaný refund 1 961 Kč). Stripe-side clamp už řeší `process-refund` edge fn (viz STATE_5), tento trigger zarovná navíc i účetní doklad a `accounting_entries` záznam. Volá se z `trg_cap_refund_to_paid` BEFORE INSERT na `invoices`. LANGUAGE plpgsql, žádný SECURITY DEFINER (trigger funkce). |
| `reschedule_booking_free(p_booking_id uuid, p_new_start date, p_new_end date, p_source text DEFAULT 'web_customer')` | **NEW 2026-06-08:** Posun nadcházející rezervace **zdarma** na jiný termín se zachováním počtu dní i ceny (`total_price` beze změny). Validace: `auth.uid()` = vlastník, `status='reserved'` + `payment_status='paid'`, stejná délka (`p_new_end − p_new_start = end_date − start_date`), `p_new_start >= dnes` (Europe/Prague), žádná změna; **overlap** s jinou rezervací stejné motorky (status pending/reserved/active) i s jinou rezervací **téhož zákazníka** (mimo dětské motorky `license_required='N'` — shoda s `check_user_booking_overlap`). UPDATE jen `start_date`/`end_date` (+ `original_start_date`/`original_end_date` při prvním zásahu, append do `modification_history` se `source=p_source`, `free_move=true`). Mail `web_booking_modified` / `booking_modified` + aktualizovaná smlouva → zařídí stávající trigger `trg_send_booking_modified_email` (routuje dle `booking_source`). Pre-checky jsou inkluzivní (date-based) = **striktnější** než BEFORE triggery `check_booking_overlap` (tstzrange, half-open) / `check_user_booking_overlap`, takže po průchodu pre-checku už tyto triggery nikdy nehodí výjimku (timestamp-overlap ⟹ date-inkluzivní-overlap). Vrací `{success, new_start, new_end, free}` nebo `{success:false, error}` (kódy: `unauthenticated`, `not_found`, `wrong_status`, `not_paid`, `length_mismatch`, `past_date`, `no_change`, `moto_overlap`, `customer_overlap`). SECURITY DEFINER, `SET search_path=public`. `REVOKE ALL FROM PUBLIC, anon`; `GRANT EXECUTE TO authenticated`. Volá web `/upravit-rezervaci` (záložka „Posunout termín", `p_source='web_customer'`) i Flutter app (`reservation_edit_screen` záložka move, `p_source='app'`). |
| `_apply_discount_variant_b(p_old_total, p_old_discount, p_gross_diff, p_dtype)` | **NEW 2026-06-11:** Jádro varianty B — z hrubého rozdílu úpravy spočítá `{new_gross, new_discount, new_total, net_diff}` s přepočtem slevy na nový obsah (percent = efektivní sazba, fixed = MIN(hodnota, nová hrubá)). IMMUTABLE, čistá matematika (unit-testováno: 98% sleva, vouchery, prodloužení, bez slevy). Volá core/shorten/update_booking_gear. |
| `_booking_discount_type(p_promo_code_id, p_promo_code, p_discount_code, p_voucher_id)` | **NEW 2026-06-11:** Vrací 'percent'/'fixed' — voucher_id → fixed; jinak lookup promo_codes přes id, pak přes kód; neznámé → fixed (konzervativní). STABLE SECURITY DEFINER, GRANT authenticated+service_role. |
| `_late_pickup_discount(p_moto_id, p_start, p_end, p_pickup_time)` | **NEW 2026-06-18 (`20260618_late_pickup_discount.sql`):** Autoritativní výpočet slevy 50 % na 1. den — `round(50 % ceny 1. dne)` když `pickup_time >= 12:00` A rezervace `>= 2 dny`, jinak 0. Cena 1. dne dle ISODOW (trojí fallback price_dow→price_weekend→price_weekday). STABLE SECURITY DEFINER, GRANT authenticated+service_role. Volá `create_web_booking`, trigger `validate_late_pickup_discount`, edit RPC. Ověřeno v DB 2026-06-18. |
| `validate_late_pickup_discount()` | **NEW 2026-06-18:** BEFORE INSERT trigger fn na `bookings` (`trg_validate_late_pickup`) — **clamp-down anti-cheat**: slevu NIKDY sám nepřidá (chrání Velín/SOS/admin inserty), jen ořeže `late_pickup_discount_amount`, když klient pošle víc než povoleno, a dorovná `total_price`. EXCEPTION safe. Sleva se aplikuje ve `create_web_booking` (web) a v appce. |
| `settle_late_pickup_time_change(...)` | **ZRUŠENO 2026-06-18** — nahrazeno čistší cestou: `p_new_pickup_time` je nově parametr `_apply_booking_changes_core` + `apply_booking_changes` (viz níže). |
| `_apply_booking_changes_core(..., p_new_pickup_time time DEFAULT NULL)` / `apply_booking_changes(..., p_new_pickup_time time DEFAULT NULL)` | **UPRAVENO 2026-06-18 (`20260618_late_pickup_discount_edit.sql`, Milestone 2):** Přidán **poslední** parametr `p_new_pickup_time` (DEFAULT NULL → `apply_booking_changes_anon` se NEMĚNÍ, čas přes AI se neřeší; stará web volání bez parametru se přes default napojí). Core nově: počítá `_late_pickup_discount` pro starý i nový stav (nový čas = `COALESCE(p_new_pickup_time, pickup_time)`) a zahrnuje rozdíl do hrubého rozdílu varianty B → změna data/motorky/ČASU vyzvednutí přes hranici 12:00 = doplatek (rozdílový DP) / vratka (dobropis). Ukládá `pickup_time` + `late_pickup_discount_amount`. **`shorten_booking_with_refund`** taktéž zahrnuje late-pickup (zkrácení může spadnout pod 2 dny). **`reschedule_booking_free` BEZE ZMĚNY** (drží stejnou cenu z principu). **KF** (`generate_final_invoice_on_complete`, `20260618_late_pickup_discount_kf.sql`) přidává samostatný řádek slevy. **Web `/upravit-rezervaci` nově posílá `p_new_pickup_time`**: změna jen času (paid) jde přes `_submitChange`→`apply_booking_changes` (doplatek/vratka), nezaplacená přes tichý `_time_update` (beze změny); kombinace místo+čas přidává `g.p_new_pickup_time`. App řeší vše klient-side v `EditPriceCalc`. |
| `apply_paid_gear_change(p_booking_id uuid, p_sizes jsonb)` | **NEW 2026-06-01:** Service-role wrapper pro server-side aplikaci placené změny výbavy po potvrzení Stripe platby (volá `webhook-receiver` u `type=extension` s `metadata.chg={_gear:{sizes}}`). Načte `bookings.user_id`, impersonuje vlastníka přes `set_config('request.jwt.claim.sub', user_id, true)` + `request.jwt.claims` a zavolá **existující** `update_booking_gear(p_booking_id, p_sizes, false)` — tím se znovupoužije jeho ověřená logika (10 `*_size` sloupců + gear `booking_extras` + `extras_price`/`total_price`). Idempotentní (net_diff počítaný vůči aktuálnímu stavu → druhý běh / souběžný klientský apply = 0). Vrací jsonb z `update_booking_gear` nebo `{success:false, error:'not_found'}`. SECURITY DEFINER. `REVOKE ALL FROM PUBLIC/anon/authenticated`, `GRANT EXECUTE TO service_role` (volá jen webhook). Účel: doplatková změna výbavy se uloží a `web_booking_modified` mail dorazí i když se zákazník nevrátí do prohlížeče s živým localStorage. |

| `get_loyalty_status()` | **NEW 2026-06-11 (`20260611_loyalty_ranks.sql`):** Věrnostní rank přihlášeného zákazníka — slevy 1–20 % JEN pro app rezervace. Spočte `v_cnt = _loyalty_qualifying_count(auth.uid())` (completed app rezervace), `pct = LEAST(20, CEIL((cnt+1)/2))`, dojoinuje `loyalty_levels` (aktuální + další level). Vrací `{level, percent, rank_name, color_hex, qualifying_count, max_level, next_rank_name, next_percent, next_color_hex, bookings_to_next}` (`bookings_to_next = 2*pct − cnt`, NULL při max). Volá Flutter `loyaltyStatusProvider` (ring loga, rank karta v profilu, sleva v sumarizaci, level-up animace). STABLE SECURITY DEFINER. GRANT TO authenticated (anon REVOKE). |
| `get_loyalty_celebration_motos() → jsonb` | **NEW 2026-06-27 (APLIKOVÁNO + OVĚŘENO uživatelem):** Personalizovaný výběr motorek pro level-up oslavu v appce. Bere z HISTORIE VÝPŮJČEK přihlášeného zákazníka (`bookings.user_id=auth.uid() AND status='completed' AND is_test=false`, app i web), dojoinuje `motorcycles` (ne `is_trailer`), seskupí per motorka a ke každé vybere nejlepší médium: **video (`videos[1]`) → hlavní foto (`image_url`) → 1. foto galerie (`images[1]`)**. Vrací `{is_personalized: bool, motos: [{id, brand, model, color, image_url, video_url, videos[], rentals, last_rented}]}` — top 3 řazené **dle POSLEDNÍ dokončené rezervace** (`last_rented` desc), takže `motos[0]` = naposledy půjčená motorka; appka přehraje **montáž `videos[]`** (všechna videa té motorky po sobě) a po nich `image_url` jako hlavní fotku. (`video_url` = `videos[1]` pro zpětnou kompatibilitu.) **UPDATE 2026-06-27 (I2): přidáno pole `videos` (to_jsonb(mc.videos)).** (UPRAVENO 2026-06-27 (I): dřív se kusy s videem řadily první → opraveno na striktně dle poslední rezervace.) **Fallback** (zákazník bez použitelné historie): `is_personalized=false` + prémiový showcase kus flotily (`status='active'`, má médium, řazeno: má-video → `sort_order` → `model`). LANGUAGE sql, STABLE SECURITY DEFINER, `SET search_path=public`. REVOKE public/anon; GRANT authenticated, service_role. Volá Flutter `fetchLoyaltyCelebrationMotos()` (hero v level-up animaci). **Pozn.:** volitelné pole `cutout_url` (transparentní výřez motorky bez pozadí pro „samolepkový" hero) RPC zatím NEVRACÍ — připraveno na straně appky (`CelebrationMoto.cutoutUrl`), čeká na zdroj výřezů (viz roadmapa). |
| `_loyalty_qualifying_count(p_user_id)` | **NEW 2026-06-11:** Interní helper — počet kvalifikačních rezervací (`booking_source='app' AND status='completed'`). REVOKE od public/anon/authenticated — volá jen `get_loyalty_status` a trigger guard. **Pozn. (snapshot 2026-06-24):** v živé DB váží rezervaci dle délky (`> 7 dní → 4`, jinak `1`) a filtruje `is_test=false`. |
| `_loyalty_effective_count(p_user_id) → integer` | **NEW (snapshot 2026-06-24):** Interní helper — `_loyalty_qualifying_count(p_user_id)` + `profiles.loyalty_bonus_points` (bonus za výhru v leaderboardu). Používá ho `get_loyalty_leaderboard` a `loyalty_award_monthly_winner` pro výpočet levelu. LANGUAGE sql, STABLE SECURITY DEFINER, `SET search_path=public`. GRANT EXECUTE jen `service_role`. |
| `get_loyalty_leaderboard(p_limit integer DEFAULT 20) → jsonb` | **NEW (snapshot 2026-06-24):** Měsíční žebříček věrnostních zákazníků pro app. Sečte odjeté dny aktuálního měsíce z app rezervací (`booking_source='app'`, status active/completed, `start_date` v měsíci), filtruje opt-in profily s nickname (`loyalty_leaderboard_opt_in=true AND loyalty_nickname<>''`), seřadí dle dnů a vrátí top `p_limit` jako pole `{rank_pos, nickname, days, level, percent, rank_name, color_hex, is_me}` (level z `_loyalty_effective_count`, dojoin `loyalty_levels`). Při nepřihlášeném `{error:'unauthenticated'}`. STABLE SECURITY DEFINER, `SET search_path=public`. GRANT EXECUTE TO authenticated, service_role. |
| `loyalty_award_monthly_winner(p_month date DEFAULT minulý měsíc) → jsonb` | **NEW (snapshot 2026-06-24):** Vyhodnotí vítěze měsíčního leaderboardu (default předchozí měsíc) a přidělí bonus. Idempotentní přes `loyalty_monthly_winners` (`month` UNIQUE → `{status:'already_awarded'}`). Najde profil s nejvíc odjetými dny (app, active/completed, opt-in + nickname), připíše `+4` do `profiles.loyalty_bonus_points`, zapíše řádek do `loyalty_monthly_winners` (month, user_id, nickname, days, awarded_level_before). Bez účastníků → `{status:'no_participants'}` (+ prázdný řádek). Vrací `{status:'awarded', month, winner, days, bonus_points:4}`. Určeno pro cron. SECURITY DEFINER, `SET search_path=public`. GRANT EXECUTE jen `service_role`. |
| `set_loyalty_leaderboard_optin(p_opt_in boolean) → void` | **NEW (snapshot 2026-06-24):** Přihlášený zákazník (`auth.uid()`) zapne/vypne zveřejnění v leaderboardu — `UPDATE profiles SET loyalty_leaderboard_opt_in`. LANGUAGE sql, SECURITY DEFINER, `SET search_path=public`. GRANT EXECUTE TO authenticated, service_role. |
| `set_loyalty_nickname(p_nickname text) → void` | **NEW (snapshot 2026-06-24):** Přihlášený zákazník nastaví přezdívku do leaderboardu — `UPDATE profiles SET loyalty_nickname = NULLIF(btrim(p_nickname),'')` WHERE `id=auth.uid()`. LANGUAGE sql, SECURITY DEFINER, `SET search_path=public`. GRANT EXECUTE TO authenticated, service_role. |
| `validate_app_loyalty_discount()` | **NEW 2026-06-11:** BEFORE INSERT trigger fn na `bookings` (`trg_validate_app_loyalty`) — klientovi se nevěří: ne-app rezervaci loyalty sloupce vynuluje (a vrátí slevu do total_price), u app rezervace přepočte nárok (`pct` dle `_loyalty_qualifying_count`, cap = pct % z [total+slevy−delivery]) a případně ořízne `loyalty_discount_amount`/`percent`/`level` + dorovná `total_price`. EXCEPTION safe. |
| `get_booking_loyalty_rate(p_booking_id uuid) → jsonb` | **NEW 2026-08-06 (`20260806_loyalty_surcharge_and_branch_address.sql`):** Aktuální věrnostní sazba k rezervaci — `{level, percent}` dle AKTUÁLNÍHO ranku vlastníka (vzorec jako swap). Přístup: vlastník rezervace, `is_admin()` nebo service_role; ne-app rezervace / cizí rezervace → `{level:0, percent:0}`. Používá Velín `BookingModifyModal` pro automatickou věrnostní slevu na doplatku při admin úpravě app rezervace. STABLE SECURITY DEFINER, `SET search_path=public`. REVOKE PUBLIC/anon, GRANT authenticated + service_role. |
| `get_branch_routes(p_branch_id uuid default null)` | **NEW 2026-06-21 (`20260621_routes_feature.sql`):** Vrátí publikované trasy (`routes.is_active=true`) volitelně filtrované dle pobočky, každou se zanořeným polem `pois` (body zájmu z `route_pois`, řazené dle `sort_order`). Volá se BEZ parametru z mobilní appky (`routesDataProvider`) → vrátí všechny trasy; klient si filtruje pobočku lokálně. Vrací `jsonb` pole řádků `routes.*` + `pois`. STABLE SECURITY DEFINER, `SET search_path=public`. GRANT EXECUTE TO anon, authenticated. Appka má fallback na přímý `select *, pois:route_pois(*)`, kdyby RPC chyběla. |
| `set_updated_at()` | **NEW 2026-06-21 (`20260621_routes_feature.sql`):** Generická BEFORE UPDATE trigger fn — nastaví `NEW.updated_at = now()`. Použita triggery `trg_routes_updated` a `trg_route_pois_updated` (+ od 2026-06-29 `trg_user_pois_updated`, `trg_poi_ratings_updated`). `create or replace` (idempotentní). LANGUAGE plpgsql. |
| `get_branch_routes` **UPDATE 2026-06-29** | `20260629_community_routes_pois.sql` přepsala fn: WHERE nově `is_active=true AND status='approved'` (nepotvrzené uživatelské návrhy se NEvrací) + ke každému POI doplní `avg_rating`/`rating_count` (`to_jsonb(p) || jsonb_build_object(...)`). |
| `rate_poi(p_route_poi_id uuid, p_user_poi_id uuid, p_rating int)` | **NEW 2026-06-29 (`20260629_community_routes_pois.sql`):** Ohodnotí bod zájmu hvězdami 1–5 (právě jeden cíl — route_poi NEBO user_poi). Upsert přes ON CONFLICT (user_id+target) → max 1 hodnocení na uživatele/bod, lze měnit; `p_rating` mimo 1–5 = smaže hodnocení. Vyžaduje `auth.uid()`. plpgsql SECURITY DEFINER. GRANT authenticated. |
| `poi_rating_summary(p_route_poi_id uuid, p_user_poi_id uuid)` | **NEW 2026-06-29:** Vrátí `avg_rating` (round 1), `rating_count`, `my_rating` (hodnocení volajícího přes `auth.uid()`) pro daný bod. sql STABLE SECURITY DEFINER. GRANT anon, authenticated. Čte appka v POI panelu (`poi_rating.dart`). |
| `get_user_pois()` | **NEW 2026-06-29:** Vrátí `jsonb` pole **schválených** komunitních bodů zájmu (`user_pois.status='approved'`) + ke každému `avg_rating`/`rating_count`. sql STABLE SECURITY DEFINER. GRANT anon, authenticated. Appka sloučí do katalogu bodů zájmu (`userPoisProvider`). |
| `get_branch_routes` **UPDATE 2026-07-01** | `20260701_route_reviews.sql` znovu přepsala fn: ke každé trase doplní `review_avg` (round 1) + `review_count` z `route_reviews` (jen `status='approved'`). Zachovává chování z 2026-06-29 (WHERE `is_active AND status='approved'`, POI ratings). |
| `rate_poi` / `poi_rating_summary` **UPDATE 2026-07-04** | `20260704_points_of_interest.sql` (NUTNO APLIKOVAT): obě fn rozšířeny o 4./3. cíl `p_poi_id uuid default null` (katalogové body `points_of_interest`); staré signatury (`rate_poi(uuid,uuid,int)`, `poi_rating_summary(uuid,uuid)`) se DROPují kvůli přetížení. Volání se 2/3 pojmenovanými argy dál funguje (p_poi_id default null). Nové signatury: `rate_poi(uuid,uuid,int,uuid)`, `poi_rating_summary(uuid,uuid,uuid)`. GRANT beze změny. |
| `get_pois_catalog()` **UPDATE 2026-07-08 (`20260708_pois_catalog_lite.sql`)** — ODLEHČENO kvůli výkonu prvního načtení v appce (katalog má desítky tisíc bodů, payload byl jednotky MB). Nově vrací JEN lehká pole pro SEZNAM: `id,name`, `translations` **jen s názvy** (`{lang:{name}}` — kvůli lokalizaci názvu + hledání napříč jazyky), `lat,lng`, `image_url = coalesce(image_url, images[1])` (náhled bez posílání celé galerie), `category,country,is_catalog=true`, `avg_rating`/`rating_count` (předagregováno LEFT JOIN nad `poi_ratings`, ne 2 poddotazy na řádek). **Už NEvrací** `description`, `surroundings`, celé `images[]` ani překlady popisů/okolí — ty dotáhne `get_poi_detail`. Bez `ORDER BY` (appka si řadí sama). sql STABLE SECURITY DEFINER, `SET search_path=public`. GRANT anon, authenticated. Appka: `catalogPoisProvider`. Původní (2026-07-04) verze vracela plný payload vč. popisů/galerie/všech překladů. |
| `get_poi_detail(p_id uuid)` | **NEW 2026-07-08 (`20260708_pois_catalog_lite.sql`):** Plný detail JEDNOHO katalogového bodu — `id,name,description,surroundings,lat,lng,image_url,images,translations` (kompletní), `category,country,is_catalog=true`, `avg_rating`/`rating_count`. Pro neexistující/neaktivní `id` vrací JSON `null`. sql STABLE SECURITY DEFINER, `SET search_path=public`. GRANT anon, authenticated. Appka (`fetchCatalogPoiDetail` → `route_poi_sheet.dart`) volá až při otevření detailu bodu (seznam je odlehčený, viz `get_pois_catalog`). |
| `jsonb_name_translations(t jsonb)` | **NEW 2026-07-25 (`20260725_routes_list_speedup.sql`):** Helper — z `translations` vytáhne jen `{lang:{name}}`. sql IMMUTABLE (kvůli generated sloupci `points_of_interest.translations_names`). Používá `get_pois_catalog`, `get_branch_routes_lite`. |
| `get_branch_routes_lite()` | **NEW 2026-07-25 (`20260725_routes_list_speedup.sql`):** ODLEHČENÝ seznam tras pro appku — plný payload `get_branch_routes` narostl na ~59 MB (překlady popisů 8,4k route_pois do ~10 jazyků) a fn padala na statement timeout (57014) → appka jela 59MB fallback selectem (tab Trasy ~20 s). Lite vrací: id, branch_id, name, route_type, distance_km, duration_min, difficulty, waypoints, mapy_url, cover_image (coalesce images[1]), countries, translations JEN názvy, review_avg/count + `pois` jen id/name/lat/lng/image_url/translations-názvy/avg_rating/rating_count. Recenze/hodnocení předagregované LEFT JOINem (žádné korelované poddotazy). WHERE `is_active AND status='approved'`. sql STABLE SECURITY DEFINER. GRANT anon, authenticated. Appka: `routes_cache.dart` (primární), fallback stará RPC → přímý select. |
| `get_route_detail(p_id uuid)` | **NEW 2026-07-25:** PLNÝ detail jedné trasy (stejný tvar jako prvek `get_branch_routes`: routes.* + review_avg/count + pois s ratingy) — appka (`routeFullProvider`) dotahuje při otevření detailu/navigace (seznam je lite). Pro neexistující/neaktivní vrací JSON `null`. GRANT anon, authenticated. |
| `get_route_poi_detail(p_id uuid)` | **NEW 2026-07-25:** PLNÝ detail jednoho trasového bodu (route_pois.* + avg_rating/rating_count) — appka (`fetchRoutePoiDetail` → `route_poi_sheet.dart`) dotahuje při otevření bodu z lite seznamu (obdoba `get_poi_detail` pro katalog). GRANT anon, authenticated. |
| `get_pois_catalog()` **UPDATE 2026-07-25** | `20260725_routes_list_speedup.sql`: fn padala na statement timeout (per-row `jsonb_each` nad velkými `translations` 37k bodů — narostly překladovými crony) → appka měla prázdný katalog. Nově čte hotový generated sloupec `translations_names`; výstup BEZE ZMĚNY. + indexy `idx_poi_ratings_route_poi/_poi/_user_poi`, `idx_route_reviews_route`. |
| `submit_route_review(p_route_id uuid, p_rating int, p_review_text text default null, p_photos text[] default '{}')` | **NEW 2026-07-01 (`20260701_route_reviews.sql`):** Vloží/upraví recenzi trasy (upsert ON CONFLICT user_id+route_id → max 1/uživatel/trasa). `rating` 1–5 (jinak exception), text `nullif(btrim,'')`, `photos` do `route_reviews.photos`. Vrací `id` recenze; při úpravě resetuje `status` na `approved`. Vyžaduje `auth.uid()`. plpgsql SECURITY DEFINER. GRANT authenticated. |
| `delete_route_review(p_route_id uuid)` | **NEW 2026-07-01:** Smaže vlastní recenzi volajícího k dané trase. plpgsql SECURITY DEFINER. GRANT authenticated. |
| `save_user_route(p_name, p_description, p_source_route_id, p_waypoints jsonb, p_poi_refs jsonb, p_profile, p_distance_km, p_duration_min)` | **NEW 2026-07-15 (`20260725_my_experiences.sql`):** Uloží vlastní trasu do „Mých tras" (jen pro volajícího). Validace: auth, název povinný (ořez 120), `waypoints` = jsonb array ≥1, profil whitelist, limit 200 tras/uživatel (`limit_reached`). Vrací `{success,id}` / `{success:false,error}`. plpgsql SECURITY DEFINER. GRANT authenticated. Volá appka (route builder „Uložit", navigace po dokončení vlastní trasy). |
| `delete_user_route(p_id uuid)` | **NEW 2026-07-15:** Smaže vlastní uloženou trasu (`WHERE user_id=auth.uid()`). Vrací `{success:found}`. SECURITY DEFINER. GRANT authenticated. |
| `get_my_saved_routes()` | **NEW 2026-07-15:** Vrátí jsonb pole uložených tras volajícího (id, name, description, source_route_id, waypoints, poi_refs, profile, distance_km, duration_min, created_at; order created_at desc). sql STABLE SECURITY DEFINER. GRANT authenticated. Appka → Moje zážitky → Moje trasy. |
| `mark_place_visited(p_route_poi_id, p_user_poi_id, p_poi_id, p_route_id)` | **NEW 2026-07-15:** Označí bod zájmu jako OBJEVENÝ (dojezd navigací ≤200 m). Právě jeden cíl (route_poi/user_poi/katalogový poi); název+GPS se dohledají server-side ze zdrojové tabulky (nelze podvrhnout). Upsert: první dojezd = insert, opakovaný = `visit_count+1` + `last_visited_at`. Vrací `{success, first_visit, visit_count, total_places}`. Chyby: `not_authenticated`, `one_target_required`, `place_not_found`. plpgsql SECURITY DEFINER. GRANT authenticated. |
| `get_my_places()` | **NEW 2026-07-15:** Vrátí jsonb pole objevených míst volajícího — LEFT JOIN na zdrojové body (aktuální name/lat/lng/image_url s fallbackem na snapshot) + `route_name`; order `last_visited_at desc`. sql STABLE SECURITY DEFINER. GRANT authenticated. Appka → Moje zážitky → Moje místa. |
| `get_route_reviews(p_route_id uuid)` | **NEW 2026-07-01:** Vrátí `jsonb` `{avg, count, my:{rating,review_text,photos}|null, reviews:[{id,rating,review_text,photos,status,is_mine,author,created_at}]}`. Viditelné jen `approved` (+ vlastní + admin). `author` = `profiles.loyalty_nickname` → `full_name` → „Motorkář". Vlastní recenze řazena první. sql STABLE SECURITY DEFINER. GRANT anon, authenticated. Čte appka v detailu trasy (`route_reviews.dart`). |
| `_is_self_service_booking(p_booking_id)` | **NEW 2026-06-23 (`20260623c_autonomous_handover_protocol.sql`):** boolean — je rezervace na samoobslužné pobočce? (`bookings`→`motorcycles.branch_id`→`branches.type='samoobslužná'`). STABLE SECURITY DEFINER. |
| `start_handover_protocol_window(p_booking_id)` | **NEW 2026-06-23:** spustí 1h okno pro samovyplnění předávacího protokolu (appka při zadání kódu ke dveřím / otevření aktivní samoobslužné rezervace). Ověří vlastníka (`auth.uid()`)/admin, status reserved/active, samoobslužnou pobočku; `bookings.handover_protocol_started_at=now()` JEN poprvé (idempotentní). Vrací `{success, started_at, deadline, filled_at, autofilled}` / `{success:false,error}` (`not_found`/`forbidden`/`wrong_status`/`not_self_service`). SECURITY DEFINER, GRANT authenticated. **+ 2026-06-23 (d):** při PRVNÍM spuštění okna pošle push „máte 60 minut" přes `send_push_via_edge` (data `{type:handover_protocol, id}`). |
| `get_handover_protocol_state(p_booking_id)` | **NEW 2026-06-23:** stav protokolu pro appku — `{is_self_service, started_at, deadline, filled_at, autofilled, locked, can_fill}`. `can_fill`=samoobslužná+reserved/active+okno běží+nevyplněno. STABLE SECURITY DEFINER, GRANT authenticated. |
| `autofill_overdue_handover_protocols()` | **NEW 2026-06-23:** cron (pg_cron `autofill-handover-protocols` */5) — samoobslužné rezervace s okny > 1 h bez vyplněného protokolu → pro každou zavolá edge fn `submit-handover-protocol` (mode='auto') přes pg_net (URL/key z `app_settings`). Edge fn nastaví `handover_protocol_filled_at` → další běh přeskočí. SECURITY DEFINER, EXCEPTION safe. **+ 2026-06-23 (d):** před auto-fillem pošle push PŘIPOMÍNKU „poslední výzva" rezervacím s oknem > 50 min (dedup přes `handover_protocol_reminder_sent_at`). |

### Další funkce v reálné DB (ne v migracích)
| Funkce | Popis |
|--------|-------|
| `auto_accounting_on_booking_paid()` | Auto účetní záznam při zaplacení bookingu. **FIX 2026-06-11 (`20260611_fix_auto_accounting_income_category.sql`, aplikováno):** od 20260312d vkládala bez `category`, ale `accounting_entries.category` je v reálné DB **TEXT NOT NULL** → INSERT padal a EXCEPTION blok chybu spolkl → příjmové záznamy NIKDY nevznikaly (Dashboard/Finance tržby 0, jen výdaje). Nově INSERT s `category='pronájem'` + `date=CURRENT_DATE`; součástí migrace backfill 11 zaplacených rezervací (54 669 Kč, ověřeno). Typ zůstává `'income'` (ENUM `entry_type` zná jen income/expense). SECURITY DEFINER, EXCEPTION safe |
| `auto_reply_sos()` | Automatická odpověď na SOS |
| `auto_schedule_services()` | Auto plánování servisů |
| `auto_check_service_parts()` | Auto kontrola dílů pro blížící se servisy (~30 kalendářních dní) — zkontroluje sklad, dedup dle konkrétních dílů (nová PO jen pokud díl nemá otevřenou draft/sent objednávku), vytvoří PO seskupené dle dodavatele. Vrací jsonb {created_orders}. SECURITY DEFINER |
| `calc_booking_price_v2()` | Kalkulace ceny bookingu v2 |
| `calculate_moto_roi()` | Výpočet ROI motorky |
| `check_admin_permission()` | Kontrola admin oprávnění |
| `check_moto_availability()` | Kontrola dostupnosti motorky (status: pending/reserved/active) |
| `create_sos_incident()` | Vytvoření SOS incidentu |
| `extend_booking()` | Prodloužení bookingu |
| `generate_invoice_number()` | Generování čísla faktury |
| `get_available_motos()` | Získání dostupných motorek |
| `handle_new_user()` | Zpracování nového uživatele (auth trigger) |
| `send_admin_message()` | Odeslání admin zprávy |
| `snapshot_daily_stats()` | Snapshot denních statistik |
| `sos_share_location()` | Sdílení lokace v SOS |
| ~~`trigger_sos_auto_reply()`~~ | **SMAZÁNO 2026-03-24** — byl no-op, žádný trigger ho nevolal |
| ~~`calc_booking_price()`~~ | **SMAZÁNO 2026-03-24** — nahrazeno calc_booking_price_v2 |
| `update_moto_after_service()` | Aktualizace motorky po servisu |
| `validate_voucher_code(p_code)` | Validace voucherového kódu (vrací {valid, id, type:'fixed', value, code}) |
| `create_test_booking(p_user_id, p_moto_id, p_start, p_end)` | Vytvoření testovací rezervace (SECURITY DEFINER, bypass RLS). Vrací uuid. |
| `update_test_booking_status(p_booking_id, p_status, p_payment_status?)` | Změna stavu testovací rezervace (SECURITY DEFINER). Triggery se spouští normálně (KF, SMS, door codes). |
| `create_test_service_order(p_moto_id, p_type, p_notes)` | Vytvoření testovací servisní zakázky (SECURITY DEFINER, bypass RLS). Vrací uuid. |
| `update_test_booking_fields(p_booking_id, p_fields jsonb)` | Universální update booking polí (end_date, picked_up_at, mileage, rating, pickup/return address+GPS). SECURITY DEFINER. |
| `create_test_sos_timeline(p_incident_id, p_action, p_data)` | Zápis do SOS timeline (SECURITY DEFINER). |
| `update_test_sos_status(p_incident_id, p_status, p_notes)` | Změna stavu SOS incidentu (SECURITY DEFINER). |
| `update_test_profile(p_user_id, p_data jsonb)` | Update profilu zákazníka (full_name, phone, city, license_group). SECURITY DEFINER. |
| `cleanup_all_test_data()` | Smaže vše testovací: bookings, SOS, service, profiles + auth.users. SECURITY DEFINER. Vrací jsonb. |
| `get_moto_booked_dates(p_moto_id)` | Veřejná RPC: vrací obsazené date ranges pro kalendář (start_date, end_date, status, created_at). SECURITY DEFINER, STABLE. Bez citlivých dat — bezpečné pro anon. **UPDATE 2026-05-26 (`20260526_get_moto_booked_dates_service_range.sql`):** kromě rezervací (`bookings` se statusem pending/reserved/active) vrací i **servisní bloky z `maintenance_log`** jako `status='service'`. Servis blokuje **POUZE rozsah `service_date` → `COALESCE(scheduled_date, service_date)`** (jednodenní servis = `scheduled_date` NULL → jen `service_date`). Filtr: `service_date IS NOT NULL AND completed_date IS NULL AND status NOT IN (completed,cancelled) AND COALESCE(scheduled_date,service_date) >= CURRENT_DATE`. **NIKDY se nebere `created_at` jako začátek servisu** — předchozí stav značil motorku jako obsazenou už ode dne zadání servisu (web kalendář v detailu motorky ukazoval „od dneška až do plánovaného dne"). Dokončený/zrušený servis už neblokuje. Web (`katalog-detail.php#buildBookedDays`) servisní řádky vykreslí jako „occupied". **UPDATE 2026-06-16 (`20260616_trailer_addon.sql`, vozík):** přidána **UNION větev `trailer_moto_id`** — kalendář kusu je nově obsazený i rezervacemi, kde je tento kus přiřazený jako **gear add-on „Vozík"** (`bookings.trailer_moto_id = p_moto_id`, status pending/reserved/active). Vozík půjčený samostatně (přes `moto_id`) i jako příslušenství tak sdílí jeden kalendář. **RE-ISSUE 2026-07-19 (`20260726_reissue_get_moto_booked_dates_trailer.sql`):** na živé DB běžela stará verze BEZ vozíkové větve (soubor 20260616 je v `supabase/functions/migrations/`, který deploy-sql.yml nehlídá) → web katalog-detail neukazoval vozík obsazený gear-rezervací. Funkce znovu aplikována ručně + zaevidována řádnou migrací; ověřeno SELECTem (vrací gear-rezervaci 20.–21. 7.). |
| `get_trailer_availability(p_start date, p_end date, p_exclude_booking uuid DEFAULT NULL)` | **NEW 2026-06-16 (`20260616_trailer_addon.sql`, vozík):** Vrací jsonb `{available_count int, trailer_id uuid|null}` — kolik kusů **vozíku** (`motorcycles.is_trailer=true AND COALESCE(status,'active')='active'`) je volných v termínu `p_start`–`p_end` + **id prvního volného** (`ORDER BY created_at` → auto-výběr). Obsazenost počítá z přímých rezervací (`moto_id`), gear-přiřazení (`trailer_moto_id`, status pending/reserved/active, overlap `[]`) i servisu (`maintenance_log`). `p_exclude_booking` vyřadí danou rezervaci (úpravy). Web rezervace krok 5 (`pages-rezervace.js#MG._refreshTrailer`, volá při změně motorky/termínu, nezávisle na inventory_v2) i Flutter app (`trailerAvailabilityProvider`, params `{p_start, p_end}`) z toho rozhodují, zda dlaždici „Vozík" zobrazit a který kus přiřadit do `bookings.trailer_moto_id`. Volný kus = `available_count>0 && trailer_id != null`. STABLE SECURITY DEFINER. GRANT TO anon, authenticated, service_role. |
| `create_web_booking(...)` | Vytvoří rezervaci: najde/vytvoří auth usera + profil, zkontroluje dostupnost, vypočte cenu (per-day pricing price_mon..price_sun, inclusive start+end), vytvoří booking. Aktuální signatura (37 parametrů): `p_moto_id, p_start_date, p_end_date, p_name, p_email, p_phone, p_street, p_city, p_zip, p_country, p_note, p_pickup_time, p_delivery_address, p_return_address, p_extras, p_discount_amount, p_discount_code, p_promo_code, p_voucher_id, p_license_group, p_password, p_helmet_size, p_jacket_size, p_pants_size, p_boots_size, p_gloves_size, p_passenger_helmet_size, p_passenger_jacket_size, p_passenger_gloves_size, p_passenger_boots_size, p_return_time, p_existing_booking_id, p_passenger_pants_size, p_consent_vop, p_consent_gdpr, p_marketing_consent, p_consent_photo`. p_extras formát: `[{name, unit_price}]`. p_password: nastaví heslo. Velikosti výbavy: 5 sloupců pro řidiče (helmet/jacket/pants/boots/gloves) + 5 pro spolujezdce (helmet/jacket/pants/gloves/boots — `p_passenger_pants_size` doplněn 2026-05-11, migrace `20260511_create_web_booking_passenger_pants_size.sql` — sloupec `bookings.passenger_pants_size` existoval už dřív, jen ho RPC nezapisovalo; nový param přidán na konec signatury, proto se nejdřív DROPne stará 32-arg verze). **p_return_time** (text HH:MM, default NULL) — frontend ho posílá pouze při vrácení mimo provozovnu (return-other zaškrtnuto, nebo delivery + same-as-delivery); jinak zůstává NULL (vrácení v půjčovně Mezná). **p_existing_booking_id** (uuid DEFAULT NULL, od 2026-04-28) — když uživatel klikne "Zpět" v kroku 2 a vrací se s úpravami, frontend pošle bookingId původní pending rezervace. Pokud booking patří stejnému user_id, je pending+unpaid A `booking_source='web'`, RPC ji **UPDATE**uje (přepíše motorku, datum, extras, slevu, velikosti, ...) místo vytvoření nové; vlastní existující booking ignoruje v overlap checku. App/SOS/admin bookingy nelze přes tuto RPC nikdy přepsat (kontrola `booking_source='web'`). Tím se předchází chybě "Booking overlap — motorka není v termínu dostupná" při změně rezervace přes Zpět. **Pozn.:** v DB NENÍ `p_booking_source` — booking_source je do INSERT zapisován hardcoded jako `'web'`. **Deduplikace zákazníka:** pouze podle `lower(trim(email))` proti `auth.users.email`. Číslo OP / ŘP se NEpoužívá (ochrana před chybou OCR). **FIX 2026-05-07 (`bookings_user_id_fkey` violation):** v existing-user větvi se profil dotvoří `INSERT ... ON CONFLICT (id) DO UPDATE` místo pouhého `UPDATE`. Pokud má `auth.users` osiřelý záznam bez profilu (např. starý signup, edge fn, AI test simulátor), profil se dotvoří ze submitnutých dat — předtím INSERT do `bookings` padl na FK `bookings.user_id → profiles.id`. Migrace `20260507_fix_create_web_booking_profile_upsert.sql`. **Souhlasy (2026-05-23, migrace `20260523_create_web_booking_consents.sql`):** 4 nové parametry `p_consent_vop, p_consent_gdpr, p_marketing_consent, p_consent_photo` (boolean DEFAULT NULL) přidané na konec signatury → DROP staré 33-arg verze + recreate. Profil INSERT/upsert nově zapisuje `consent_vop, consent_gdpr, marketing_consent, consent_photo` — web (`pages-rezervace-steps.js`) je posílá z checkboxů kroku „Souhlasy" (vop/gdpr povinné, marketing/foto volitelné). NULL param = neměnit (nový profil → DEFAULT true, existující → zachovat). Zbylých 6 souhlasů (email/sms/push/whatsapp/data_processing/contract) web nesbírá → zůstávají DEFAULT true. Fix: Velín detail zákazníka dřív ukazoval všechny souhlasy „Ano", protože RPC je nikdy nezapisovalo. **Datum narození (2026-05-30, migrace `20260530_create_web_booking_dob.sql`):** nový poslední parametr `p_date_of_birth text DEFAULT NULL` (37→38 arg → DROP staré 37-arg verze + recreate). Web nově sbírá „Datum narození" jako **povinné** pole v kroku 1 (`pages-rezervace.js`, input `#rez-dob`), posílá jako `p_date_of_birth`. Profile INSERT/upsert v obou větvích zapisuje `date_of_birth = COALESCE(v_dob, profiles.date_of_birth)` (prázdné/neplatné datum nepřepíše existující → bezpečné pro QR resume/„Zpět", kde se DOB znovu neposílá). Věk se ve Velíně dopočítá z `date_of_birth`. **VÍCE SLEVOVÝCH KÓDŮ 2026-06-23 (`20260623_create_web_booking_multi_discount.sql`, APLIKOVÁNO + OVĚŘENO — success):** blok výpočtu slevy přepsán — místo jednoho `p_promo_code` + jednoho `p_voucher_id` se nyní rozparsuje `p_discount_code` (čárkou oddělený seznam VŠECH kódů uplatněných v UI), každý token se autoritativně dohledá (promo dle `code` → percent/fixed, jinak voucher dle `code`), sečte se (procento ze základu `v_total` po late slevě, fixní/voucher = hodnota) a capne na základ `LEAST(Σ, v_total)` (shodně s UI `Math.min(sum, base)`). `promo_code_id`/`voucher_id` drží PRVNÍ nalezený (zpětně kompat reference), `discount_amount` = celý součet, `discount_code` = seznam z UI. Dedup tokenů; fallback na jediný `p_promo_code` při prázdném `p_discount_code` + legacy `p_voucher_id` přes id (public-api/mcp-server). **Bez změny signatury** (37 param). Fix bugu: web v dokladu (ZF/DP/KF, `generate-invoice` čte `discount_amount` + popisek `discount_code`) vypsal VŠECHNY kódy, ale RPC přepočítával slevu jen z jednoho promo + jednoho voucheru → odečetl se jen jeden. **ŘP PROTI POLI `license_groups` 2026-06-26 (`20260626_create_web_booking_license_groups.sql`):** kontrola ŘP (blok „2) MOTORKA + ŘP") nově staví `v_moto_groups` = `motorcycles.license_groups` (uppercase, fallback `[license_required]` když pole prázdné) a místo `v_moto_license = ANY(v_allowed_groups)` testuje PRŮNIK polí `v_moto_groups && v_allowed_groups` (OR — stačí, aby skupiny pokryté zákazníkovým ŘP protnuly kteroukoliv přijímanou skupinu motorky). Dětská/bez-ŘP se přeskočí přes `'N' = ANY(v_moto_groups)`. Fix bugu: web odmítal 125 skútr s `license_groups={A1,B}` (automat, v ČR řiditelný i na ŘP B) zákazníkovi s B, protože Velín odvodí `license_required='A1'` a stará kontrola porovnávala jen tu jednu hodnotu (`A1 ∉ {B,AM}` → 400). Nově shodná OR-sémantika jako web katalog (`motoLicenseGroups`) i Flutter app (`licenseGroupsOrFallback`/`BookingValidator.checkLicense` — appka byla správně už předtím, měnit ji netřeba). Hierarchie A>A2>A1>AM a B={B,AM} beze změny (přívěs {B} pro samotné A dál neprojde). Pokrývá i AI/public-api/mcp (volají tuto RPC). **Bez změny signatury.** **OVERLAP IGNORUJE VLASTNÍ PENDING DRAFT 2026-06-29 (`20260629_create_web_booking_overlap_own_pending.sql`, APLIKOVÁNO + OVĚŘENO — success):** blok „4) OVERLAP" nově do EXISTS přidává `AND NOT (user_id = v_user_id AND status='pending' AND payment_status='unpaid' AND booking_source='web')` — vlastní rozpracovaný (pending+unpaid web) draft zákazníka už ho NEblokuje (reuse přes `p_existing_booking_id` vyloučil jen jeden řádek; ostatní vlastní drafty z dvojkliku / abandonované session házely falešné „Booking overlap — motorka není v termínu dostupná" na volné motorce, po pár pokusech to prošlo). Cizí zákazník (i jeho pending) a potvrzené/aktivní rezervace blokují dál beze změny; auto-cancel `auto_cancel_expired_pending()` vlastní drafty stejně uvolní. Frontend `_submitReservation` (`pages-rezervace-steps.js`) zároveň dostal re-entrancy guard `MG._rezSubmitting` proti dvojímu odeslání. **Bez změny signatury.** **PLNÍ BOOKING_DISCOUNTS 2026-06-30 (`20260630_create_web_booking_populate_booking_discounts.sql`, APLIKOVÁNO + OVĚŘENO — success):** sekce 5c sbírá každou uplatněnou slevu/voucher do lokálního `v_bd` (jsonb) a nová sekce 8 je po INSERT/UPDATE bookingu vloží do `booking_discounts` (při re-use draftu smaž a vlož znovu) + zavolá `_reallocate_booking_discounts(v_booking_id, v_total + p_discount_amount)` pro rozpočítání skutečně odečtených částek. **Fix bugu:** přepisy 2026-06-26/2026-06-29 (výše) omylem zahodily zápis do `booking_discounts`, který zavedla `20260625_multi_discount_create_web_booking.sql` → trigger `redeem_booking_discounts_on_paid` neměl co uplatnit, takže `promo_codes.used_count` nerostl a vouchery se po zaplacení neoznačovaly jako `redeemed` (Velín „nereflektoval počet použití"). Historie srovnána jednorázovým `20260630_backfill_discount_usage.sql`. **Bez změny signatury.** Vrací {booking_id, amount, user_id, is_new_user, reused}. SECURITY DEFINER |
| `set_web_booking_device(p_booking_id, p_device, p_phase)` | **NEW 2026-05-30 (`20260530_booking_device_tracking.sql`):** Post-create/post-payment helper pro sledování zařízení web rezervace. `p_device` ∈ {`pc`,`mobile`,`tablet`} (normalizace, neznámé → error). `p_phase='created'` nastaví `bookings.created_device` **jen poprvé** (`created_device IS NULL`, ať „Zpět"/resume nepřepíše start); `p_phase='completed'` nastaví `bookings.completed_device` (poslední zařízení = ze kterého se platilo). Guard `booking_source='web'`. Frontend detekuje přes `MG._rezDeviceType()` a volá z `pages-rezervace-steps.js` (created, po `create_web_booking`) a `pages-rezervace-scan.js#_rezSubmitPayment` (completed). Velín `BookingSummary.jsx` zobrazí „Zařízení" — při shodě jen jedno, při rozdílu „PC → Mobil" (cross-device). EXCEPTION-safe (`{error: SQLERRM}`). SECURITY DEFINER, SET search_path=public, GRANT EXECUTE TO anon, authenticated. |
| `set_web_booking_password(p_booking_id, p_password)` | Nastaví heslo zákazníka přes booking ID (pouze web bookings). UPDATE `auth.users.encrypted_password` (bcrypt celého hesla) + UPDATE `profiles.password_last4_bcrypt` (bcrypt posledních 4 znaků — používá AI agent pro 3. ověřovací faktor v `find_booking_for_modification` / `apply_booking_changes_anon`, viz 2026-05-02). Heslo se použije pro editaci rezervace, přihlášení do app i AI úpravu rezervace. EXCEPTION-safe — při SQL chybě vrací `{error: SQLERRM}`. SECURITY DEFINER. |
| `set_web_booking_company(p_booking_id, p_company_name, p_ico, p_dic, p_company_address)` | **NEW 2026-08-18 (`20260818_web_booking_company_fields.sql`, APLIKUJE AUTO-DEPLOY po merge):** Post-create helper — web krok 3 (checkbox „Firemní údaje", `pages-rezervace-steps.js`) ji volá po `create_web_booking` pro uložení firemních fakturačních údajů do profilu majitele rezervace. UPDATE `profiles.company_name / ico / dic / company_address` — vše přes `COALESCE(NULLIF(trim(...)), původní)`, prázdné hodnoty nepřepíší existující. Klíčováno přes `booking_id`, guard `booking_source='web'` (stejný vzor jako `set_web_booking_docs`). Volání na frontendu je tolerantní (try/catch + console.warn) — výpadek RPC neshodí rezervaci. Doklady: `generate-invoice` čte company_name/company_address tolerantním dotazem (`loadCustomerCompany`) a tiskne ODBĚRATEL řádky „Firma" + „Sídlo". EXCEPTION-safe (`{error: SQLERRM}`). SECURITY DEFINER, SET search_path=public, GRANT EXECUTE TO anon, authenticated. |
| `set_web_booking_docs(p_booking_id, p_id_number, p_license_number, p_license_expiry, p_license_group, p_date_of_birth)` | **UPDATE 2026-06-18 (`20260618_set_web_booking_docs_multi_license.sql`):** `p_license_group` nyní přijímá **VÍCE skupin ŘP oddělených čárkou** (např. `"A,B"`) — krok 2 webu má multi-select chipy (motorka + vozík → např. B na vozík). Parsuje se `string_to_array(p_license_group, ',')` → uppercase → filtr platných (AM/A1/A2/A/B/N) → `array_agg(DISTINCT ...)::license_group[]`. Neplatné/prázdné tokeny ignoruje; když nic platného nezbyde → NULL (COALESCE nepřepíše existující). Zpětně kompatibilní (jedna skupina "A" prochází stejně). `create_web_booking` se NEMĚNÍ — frontend mu posílá jen jednu pokrývající skupinu (`MG._rezPrimaryLicGroup`) kvůli validaci proti motorce, kompletní seznam ukládá tato RPC. **UPDATE 2026-05-30 (`20260530_set_web_booking_docs_dob.sql`):** přidán 6. parametr `p_date_of_birth` (date jako text, COALESCE do `profiles.date_of_birth`, neplatný formát ignorován). 5→6 arg → DROP staré 5-arg verze + recreate. **Root cause re-fixu:** zákazník nahlásil, že se u nové web rezervace neuloží skupina ŘP / platnost / číslo OP / číslo ŘP / datum narození / věk — ukládá se POUZE krok 1 (jméno/mail/telefon/adresa přes `create_web_booking`). Příčina (ověřeno dotazem na produkci 2026-05-30): v DB byla nasazená **6-arg varianta s 6. parametrem `p_license_categories`** (osiřelý pozůstatek dřívějšího pokusu o fix — nic ji s tím parametrem nevolá), jejíž tělo spolehlivě neuložilo doklady, a datum narození neřešila vůbec. Frontend navíc fallbackoval na `profiles.update()`, který RLS u nepřihlášeného nového web zákazníka blokuje. Při re-issue bylo nutné dropnout 5-arg **i** 6-arg signaturu (`CREATE OR REPLACE` neumí přejmenovat parametr — chyba `42P13 cannot change name of input parameter`). Po nasazení čistá 6-arg verze s `p_date_of_birth` spolehlivě ukládá id_number/license_number/license_group/license_expiry/date_of_birth. Datum narození primárně teče přes `create_web_booking` (krok 1, povinné pole), zde je navíc záchyt z OCR. **PŮVODNÍ 2026-05-25:** Post-create helper — web krok 2 (`pages-rezervace-scan.js#MG._rezPersistDocs`) ji volá pro uložení ručně zadaných čísel dokladů do profilu majitele rezervace. UPDATE `profiles.id_number / license_number / license_group (enum array) / license_expiry (date)` — všechna pole přes `COALESCE(NULLIF(...), původní)`, takže prázdné/neplatné hodnoty nepřepíší existující. Klíčováno přes `booking_id` (stejný vzor jako `set_web_booking_password`), guard `booking_source='web'`. **Záměrně NEnastavuje `id_verified_at`/`license_verified_at`** — ruční čísla dál neuvolní přístupové kódy ke dveřím (to vyžaduje fotku/OCR), jen se vytisknou do smlouvy. EXCEPTION-safe (`{error: SQLERRM}`). SECURITY DEFINER, SET search_path=public, GRANT EXECUTE TO anon, authenticated. **Root cause fixu:** RPC dosud NEEXISTOVALA — frontend ji volal, volání tiše spadlo (`console.warn`) a kód se spolehl na fallback klientský `profiles.update()`, který u nepřihlášeného nového web zákazníka blokuje RLS → čísla OP/ŘP se neuložila → smlouva (`generate-document`) je četla z profilu prázdná. U testovacího účtu (profil vyplněný předem) čísla naskočila, proto se chyba projevila jen u random zákazníků. Migrace `20260525_set_web_booking_docs.sql`. |
| `get_web_booking_resume(p_booking_id)` | **UPDATE 2026-05-08:** Veřejná RPC pro resume flow (QR kód / abandoned email). Vrací booking data jen pro pending+unpaid+web bookings. **Rozšířeno** tak, aby zákazník po naskenování QR kódu v kroku 2 viděl na druhém zařízení vše, co už vyplnil v kroku 1 (jinak by musel formulář vyplňovat znovu). Nyní vrací: `{booking_id, user_id, moto_id, moto_model, start_date, end_date, total_price, extras_price, discount_amount, discount_code, promo_code, voucher_id, voucher_code, voucher_amount, pickup_time, return_time, delivery_address, return_address, notes, helmet_size, jacket_size, pants_size, boots_size, gloves_size, passenger_helmet_size, passenger_jacket_size, passenger_gloves_size, passenger_boots_size, customer_name, customer_email, customer_phone, customer_street, customer_city, customer_zip, customer_country, license_group, license_expiry, id_number, license_number, has_id_number, has_license_number, has_password, extras: [{name, unit_price, quantity}]}`. JOIN přes `profiles` (adresa, license_group, license_expiry, id_number, license_number, password_last4_bcrypt), `motorcycles` (model), `promo_codes` (code), `vouchers` (code, amount) a `booking_extras` (jsonb_agg). **2026-05-08 doplněno:** `id_number`, `license_number`, `license_expiry` reálné hodnoty (vedle ponechaných booleanů `has_*` pro zpětnou kompatibilitu) — frontend (`pages-rezervace.js#resume`) předtím četl jen booleany a do polí kroku 2 dával placeholder „(vyplněno)" místo skutečné hodnoty, takže zákazník na mobilu po QR scanu viděl prázdné/zástupné údaje a musel vyplňovat znovu (migrace `20260508_get_web_booking_resume_doc_values.sql`). **2026-05-09 doplněno:** boolean `has_password` (`profiles.password_last4_bcrypt IS NOT NULL`) — po PC auto-persistu hesla v kroku 2 (přes `set_web_booking_password`) mobil po QR scanu zobrazí v sekci 3 zelený banner „Heslo je nastavené" místo prázdných polí (frontend čte `_passwordSet`). **2026-05-30 doplněno:** `date_of_birth` (`p.date_of_birth`) — aby se po návratu z platební brány (resume) a „Zpět" na krok 1 předvyplnilo datum narození i nepřihlášenému zákazníkovi (povinné pole, jinak by blokovalo odeslání). Frontend čte do `formData._dob`. Beze změny ostatní logiky (`has_password` zachován). Migrace `20260530_get_web_booking_resume_dob.sql`. STABLE SECURITY DEFINER. GRANT EXECUTE TO anon, authenticated. |
| `get_web_booking_confirmation(p_session_id, p_booking_id)` | **NEW 2026-05-07:** Veřejná RPC pro děkovací stránku `/potvrzeni` po Stripe redirectu. Web booking je anonymní (žádná auth session) → přímý SELECT na `bookings` neprošel přes RLS `bookings_user_select` (`user_id = auth.uid()`); navíc tabulka `bookings` nemá sloupce `customer_name`/`customer_email` (jsou v `profiles`). Tato RPC obejde RLS, vrátí jen pole pro confirmation page: `{id, moto_id, start_date, end_date, total_price, payment_status, status, customer_name, customer_email, docs_status}`. Přijímá BUĎ `p_session_id` (Stripe checkout) NEBO `p_booking_id` (free booking se 100% slevou). Omezeno na `booking_source='web'` proti enumeration jiných typů rezervací. Vrací `{error: 'missing_identifier'\|'not_found'\|'not_web_booking'}` při neplatném vstupu. SECURITY DEFINER, GRANT EXECUTE TO anon, authenticated, service_role. **REFAKTOR 2026-05-13:** RPC byla na produkci dropnuta (zákazník hlásil 404 v console při /potvrzeni). Recreated s tím, že `docs_status` se nepočítá inline — deleguje se na `check_booking_docs_status(user_id, end_date, moto_id)`, který od stejné migrace umí child-bike check (license_required='N' → NULL). Tím /potvrzeni a door codes trigger sdílejí jediný zdroj pravdy. Volá `motogo-web-php/js/pages-potvrzeni.js` v polling smyčce (15× á 2 s pro Stripe checkout, 6× á 1.5 s pro free booking_id flow). |
| `get_web_shop_order_confirmation(p_order_id)` | **NEW 2026-05-13:** Veřejná RPC pro `/potvrzeni` page po Stripe redirectu z e-shop / voucher flow. Lookup `shop_orders` podle ID, vrátí `{id, order_number, customer_name, customer_email, total, subtotal, shipping_cost, discount, payment_status, status, shipping_method, created_at, confirmed_at, notes, vouchers: [{code, amount, valid_until}]}`. Voucher pole je `jsonb_agg` z `vouchers WHERE order_id = p_order_id`. Vrací `{error: 'missing_identifier'\|'not_found'}` při chybách. SECURITY DEFINER, GRANT EXECUTE TO anon, authenticated, service_role. Volá `pages-potvrzeni.js` v polling smyčce (12× á 2 s). |
| `notify_web_booking_abandoned()` | Trigger funkce: při auto-cancel web bookingu (pending→cancelled, source=web, unpaid) odešle abandoned email přes send-booking-email. SECURITY DEFINER, EXCEPTION safe |
| `check_web_booking_email(p_email text)` | **NEW 2026-05-09:** Anon-callable lookup pro real-time check existujícího mailu v poli rezervačního formuláře. Vrací jen `{exists: bool}` (žádná profilová data) — frontend (`motogo-web-php/js/pages-rezervace-auth.js#_checkEmailExists`) volá při blur na poli `rez-email` v kroku 3, při `exists=true` zobrazí inline hint „Tento e-mail u nás už máme. Pokračujte přihlášením." s CTA, které otevře login panel. STABLE SECURITY DEFINER. SET search_path=public,auth. REVOKE FROM PUBLIC, GRANT EXECUTE TO anon, authenticated. Migrace `20260509_create_web_booking_block_anon_known_email.sql`. Související: `create_web_booking` v anon kontextu (`current_user='anon'`) odmítá rezervaci pro známý e-mail bez `p_existing_booking_id` s `{error:'email_exists', message:'…'}`; v authenticated kontextu odmítá cross-account zápis (`auth.uid() <> nalezený user_id`) s `{error:'email_mismatch'}`. Service_role (AI agent / edge fn) projde beze změny. Fix bugu confirmation page „Doklady ✓ Ověřeny" pro cizí osobu pod e-mailem jiného profilu. |
| `get_ai_traffic_stats(p_from, p_to)` | Agregované AI stats pro Velín dashboard (Analýza → AI traffic). Vrací jsonb {total_requests, by_source, by_bot, top_paths, bookings_from_ai, top_partners, daily_timeline}. Admin only. SECURITY DEFINER |
| `get_page_ai_traffic(p_path, p_from, p_to)` | Per-stránka AI traffic detail (CMS → AI návštěvnost drawer). Vrací jsonb {total, by_bot, daily, led_to_bookings}. Admin only. SECURITY DEFINER |
| `create_api_key(p_partner_name, p_partner_email, p_partner_url, p_rate_limit_rpm, p_scopes)` | Vytvoří REST API klíč pro partnera. Generuje 32-byte random klíč (mk_live_<base64url>), uloží sha256 hash do api_keys. Vrací jsonb {id, api_key, prefix, warning} — **api_key se vrací POUZE TEĎ**, dál uložen není. Admin only. SECURITY DEFINER |
| `revoke_api_key(p_id, p_reason)` | Zneplatní API klíč (is_active=false, revoked_at=now). Admin only. SECURITY DEFINER |
| ~~`get_app_installation_stats()`~~ | **odstraněno 2026-06-24** (není v živém snapshotu). **NEW 2026-05-28:** Agregované statistiky instalace mobilní aplikace MotoGo (zdroj: `push_tokens.active=true`). Vrací jsonb `{unique_users_with_app, total_active_devices, ios_devices, android_devices, total_customers, install_rate_pct}`. `unique_users_with_app` = COUNT DISTINCT `user_id` z aktivních tokenů (= zákazníci s aplikací, bez ohledu na počet zařízení). `total_active_devices` = počet aktivních řádků (jeden uživatel může mít víc zařízení). `total_customers` = `profiles` bez testovacích účtů (`is_test_account=false`). `install_rate_pct` = procentuální podíl zákazníků s aplikací. Volá Velín `AnalyzaZakazniku.jsx` (Analýza → Zákazníci → sekce „📱 Mobilní aplikace MotoGo"). **Žádný zásah do appky** — Flutter `push_service.dart` už dnes upsertuje tokeny při registraci push notifikací, RPC jen agreguje existující data. Admin only (`is_admin()`), jinak `{error:'not_authorized'}`. STABLE SECURITY DEFINER, SET search_path=public. REVOKE FROM PUBLIC, GRANT EXECUTE TO authenticated. |
| `update_booking_gear(p_booking_id uuid, p_sizes jsonb, p_dry_run boolean DEFAULT false)` | **NEW 2026-06-01:** Editace výbavy rezervace zákazníkem z webu `/upravit-rezervaci` (záložka „Výbava"). **POZN. 2026-07-29:** frontend záložky do té doby CHYBĚL (`_renderTabGear` nebyla definovaná → TypeError) — doplněn v `js/pages-upravit-rezervaci-gear.js` (viz STATE_6 2026-07-29 B); zápis do `modification_history` u gear změn nově zajišťuje trigger `trg_track_booking_content_changes` (STATE_4). SECURITY DEFINER (kvůli RLS na `booking_extras`). Ověření: `auth.uid()=bookings.user_id` NEBO `is_admin()`; `status IN (reserved,active)`; `payment_status='paid'`. `p_sizes` jsonb s 10 klíči: `helmet/jacket/pants/boots/gloves` (řidič) + `passenger_helmet/passenger_jacket/passenger_pants/passenger_gloves/passenger_boots` (NULL/prázdné = odebrat). **Cenová logika:** řidičova helma/bunda/kalhoty/rukavice = zdarma; placené karty z `accessory_types` (`passenger_gear`/`boots_rider`/`boots_passenger`, fallback 690/290/290) účtované jen při **nově přidané** placené výbavě. `net_diff = nová placená − stávající placená` (stávající z gear `booking_extras` klasifikovaných dle názvu bot/boots/spolujez/passenger). `p_dry_run=true` → `{success, payment_required:net_diff>0, net_diff, refund_amount, new_total}` bez zápisu. Ostré: UPDATE 10 size sloupců + DELETE pouze gear `booking_extras` + INSERT nových (`Výbava spolujezdce`/`Boty řidič`/`Boty spolujezdce`) + `extras_price`/`total_price` += `net_diff` (GREATEST 0). Doplatek (net_diff>0) řeší frontend přes Stripe `process-payment type=extension` PŘED voláním ostrého apply (po `?paid_booking` návratu). Změna `total_price` spustí `trg_send_booking_modified_email`. Velín čte výsledek ze sloupců (`BookingSummary.jsx`). Chybové kódy: `unauthenticated`/`not_found`/`not_owner`/`wrong_status`/`not_paid`. `REVOKE ALL FROM PUBLIC`, `GRANT EXECUTE TO authenticated`. **VARIANTA B 2026-06-11:** hrubý gear rozdíl jde přes `_apply_discount_variant_b` — `net_diff`/`new_total` v dry-run i ostrém běhu jsou PO slevě (98% zákazník platí za boty 2 % ceny), `total_price` absolutně + `discount_amount` přepočítán; `extras_price` zůstává hrubá (KF rozpis sedí: Pronájem = total − extras − delivery + discount). Response navíc `gross_diff` + `new_discount`. **UPDATE 2026-08-06:** u app rezervací se kladný gear rozdíl snižuje o věrnostní slevu dle aktuálního ranku (kumulativně do `loyalty_discount_amount` + `loyalty_level/percent`; dry-run i ostrý response nesou `loyalty_discount`/`loyalty_percent`/`loyalty_level`). |
| `delete_customer_account(p_user_id uuid)` | **NEW 2026-05-23:** Atomické smazání zákaznického účtu. Volá Velín (`CustomerDetail.jsx`) i Flutter app (`profile_screen.dart`). **Předtím funkce v DB neexistovala** (žádná migrace ji nevytvořila) → volání padalo na `42883 function does not exist` a Velín spadl do fallbacku, který smazal POUZE `public.profiles`. Protože FK je jednosměrný (`profiles.id REFERENCES auth.users ON DELETE CASCADE`), v `auth.users` zůstal osiřelý záznam → `check_web_booking_email` i `create_web_booking` dedup (oba čtou `auth.users.email`) hlásily e-mail navždy jako známý. RPC maže `DELETE FROM auth.users WHERE id=p_user_id` → kaskádou padne profiles + dle FK SET NULL/CASCADE u navázaných tabulek (invoices, shop_orders, vouchers…). Oprávnění: `is_admin()` NEBO `auth.uid()=p_user_id` (Flutter self-delete), jinak `{error:'not_authorized'}`. Blokace při aktivních/čekajících/rezervovaných rezervacích → `{error:'active_bookings', count}`. Vrací `{success, user_id}` nebo `{error}`. SECURITY DEFINER, SET search_path=public,auth, EXCEPTION-safe. REVOKE FROM PUBLIC, GRANT EXECUTE TO authenticated. Migrace `20260523_create_delete_customer_account.sql`. **UPDATE 2026-05-27 (kompletní mazání):** RPC samotná zůstává beze změny (maže `auth.users` → kaskáda). Doplněno mimo RPC: a) FK `booking_complaints.customer_id` převeden na `ON DELETE SET NULL` (byl `NO ACTION` → zákazník s reklamací nešel smazat, RPC padala na FK a v `auth.users` zůstal sirotek); b) **naskenované doklady ve Storage** (`documents/<user_id>/…`) maže Velín frontend (`CustomerDetail.jsx#handleDelete`) přes Storage API PŘED voláním RPC — přímý `DELETE FROM storage.objects` z SQL/RPC blokuje trigger `storage.protect_delete()`; DB řádky `documents` padnou kaskádou (CASCADE). c) Frontend nově čte i `result.data.error` (byznys-chyby uvnitř jsonb), dřív ukazoval falešné „smazáno" při selhání RPC. Účetní soubory (`invoices/…`, `generated/…`) mají jiný prefix → zůstávají. Výsledek: identita pryč, účetnictví (faktury/smlouvy/bookingy) zůstává odpojené. |

### Doplněno 2026-06-04 ze snapshotu (dříve nezdokumentované funkce)
**RPC / helpery (callable):**
| Funkce | Popis |
|--------|-------|
| `check_license_for_moto(p_moto_id uuid, p_rental_end date DEFAULT NULL)` | Ověří, zda skupiny ŘP zákazníka stačí na danou motorku (+ platnost ŘP vs. konec nájmu). Vrací jsonb. Používá `_license_moto_rank`. |
| `_license_moto_rank(p_g license_group) → int` | Interní: číselný rank skupiny ŘP (hierarchie AM<A1<A2<A) pro porovnání. |
| `merge_license_group(p_existing license_group[], p_new text) → license_group[]` / `merge_license_group_multi(p_existing license_group[], p_raw text) → license_group[]` | Sloučí/normalizuje skupiny ŘP do pole; `_multi` naparsuje víc skupin z textu (např. „A1, B"). |
| `apply_profile_license_group(p_user_id uuid, p_group text) → void` | Uloží/sloučí skupinu ŘP do `profiles.license_group`. |
| `find_email_templates_for_event(p_event_slug text) → TABLE(id, slug, name, subject, body_html, active, attachments jsonb, triggers jsonb, subject_translations, body_translations)` | Vyhledá aktivní Velín mailové šablony navázané na událost — zdroj přílohové/spouštěcí konfigurace pro send-* edge fce. |
| `get_motos_availability_status() → TABLE(moto_id, next_available_date)` | Nejbližší dostupný den per motorka (Velín/web). |
| `get_visitor_stats(p_from, p_to, p_host text DEFAULT NULL, p_granularity text DEFAULT 'day') → jsonb` | Agregace návštěvnosti z `visitor_log` (Velín Analýza). SECURITY DEFINER, `is_admin()` guard. **2026-06-24:** `SET statement_timeout='20s'` (přebíjí ~8s limit role `authenticated`) + CTE čte jen 9 použitých sloupců (ne `select *`) — fix timeoutu. |
| `normalize_supplier_name(input text) → text` | Normalizace názvu dodavatele bez diakritiky pro matching (`suppliers.normalized_name`). |
| `regen_voucher_for_order(p_order_id uuid) → jsonb` | Znovu vygeneruje voucher kódy pro objednávku. |
| `cleanup_old_verification_documents() → int` | Úklid starých naskenovaných verifikačních dokladů; vrací počet smazaných. |
| `create_test_customer()` / `create_test_maintenance_log(...)` / `create_test_sos_incident(...)` | Testovací helpery (SECURITY DEFINER, bypass RLS). |

**Trigger funkce (vazby viz STATE_4):** `update_updated_at` / `set_updated_at` / `set_updated_at_now` (auto `updated_at`); `generate_shop_order_number` (OBJ-YYYY-NNNNN); `increment_promo_used_count` (promo used_count); `auto_liabilities_from_payroll` (mzda→závazky); `check_user_booking_overlap` (per-user overlap); `release_codes_on_profile_verify` (ověření dokladů v profilu → uvolnění door codes); `sync_motorcycle_to_assets` (moto→dlouhodobý majetek); `sync_invoice_to_documents` / `sync_invoice_pdf_update` / `sync_generated_doc_to_documents` (doklady→`documents`); `sync_moto_day_prices_to_motorcycles`; `sos_auto_severity` / `sos_auto_timeline` / `sos_notify_user_on_create` / `check_one_active_sos`; `bridge_admin_message_to_app`; `restore_vouchers_on_cancel`; `handle_new_auth_user` (auth.users→profil); `fix_auth_users_null_tokens`; `notify_booking_activated_email` / `notify_booking_cancelled_email` / `notify_booking_completed_email` / `notify_sos_incident_email` (**LEGACY email — viz STATE_4**, route přes send-booking-email = Velín šablony, dedup, GUC-no-op).

### Logistika zboží (NEW 2026-06-23, `20260623_gear_logistics.sql`)
Evidence výbavy na pobočkách + napojení na sklad/objednávky. Velín → **📦 Logistika zboží** (záložky Dostupnost/Chybí kus/Sklad/Naskladnění). Tabulka `gear_shortages` (STATE_1), trigger `gear_shortage_on_booking` (STATE_4).

| Funkce | Popis |
|--------|-------|
| `detect_gear_shortages_for_window(p_branch_id, p_from, p_to)` | Jádro detekce: per den spočítá poptávku po výbavě (10 size sloupců bookings řidič+spolujezdec namapovaných na fyzické typy helmet/jacket/pants/boots/gloves) vs. `branch_accessories.quantity`, UPSERTne deficity do `gear_shortages`, vyřešené (doplněno/storno) auto-resolvuje. **Poptávka se agreguje per (den, typ, velikost) — NE per audience** (audience-split kolidoval s UNIQUE klíčem a podhodnocoval deficit; audience se odvozuje `bool_or(lic='N')`). **Hlídá JEN typy přítomné v `branch_accessories`** (guard `exists(...)` → bunda až po zavedení). Jen status pending/reserved/active, `pickup_method<>'delivery'`. `greatest(p_from, current_date)`. SECURITY DEFINER, GRANT authenticated+service_role. |
| `trg_gear_shortage_on_booking()` | Trigger fn (AFTER INSERT/UPDATE na bookings) → zavolá `detect_gear_shortages_for_window` pro pobočku motorky + termín. Celá v `BEGIN/EXCEPTION WHEN OTHERS→null` — **rezervaci NIKDY neblokuje** („vždy pustit + flagnout"). SECURITY DEFINER. |
| `detect_gear_shortages(p_horizon_days default 120)` | Plošný přepočet přes všechny pobočky (cron + backfill). SECURITY DEFINER, GRANT authenticated+service_role. |
| `get_branch_gear_calendar(p_branch_id, p_from, p_to)` | Kalendář dostupnosti per den: vrací `(shortage_date, accessory_type, size, is_consumable, stock, booked, free, deficit)`. Universe = **VŠECHNY aktivní `accessory_types`×velikosti** (∪ cokoliv reálně skladem) → ukáže i rukavice/kalhoty/boty s 0 ks; `is_consumable` rozděluje na spotřební/nespotřební (Velín záložka Dostupnost). STABLE SECURITY DEFINER, GRANT authenticated+service_role. **Pozn.:** signatura se měnila (přibyl `is_consumable`) → v migraci `drop function … first`. |
| `find_gear_surplus_branches(p_type, p_size, p_from, p_to, p_exclude_branch)` | Pro přesun: vrací pobočky s **volným kusem v celém termínu** (`free_qty = min(stock−booked)` přes okno). STABLE SECURITY DEFINER. |
| `transfer_branch_gear(p_from_branch, p_to_branch, p_type, p_size, p_qty, p_shortage_id default null)` | Atomický přesun výbavy mezi pobočkami (FOR UPDATE na zdroji, dekrement zdroj + upsert cíl `branch_accessories`), označí shortage `transfer_requested`, přepočítá obě pobočky (horizont 120 d). `is_admin()` guard. Vrací `{success}`/`{error:'not_authorized'/'bad_qty'/'insufficient_source'}`. SECURITY DEFINER. |
| `_create_gear_po(p_shortage_id, p_qty default null)` | Jádro tvorby draft `purchase_orders` + `purchase_order_items` z deficitu (skladová položka přes SKU `prislusenstvi-{type}-{size}`, dodavatel z `inventory.supplier_id`), označí shortage `order_created`. BEZ auth guardu — GRANT jen service_role. Vrací `{success, order_id, order_number}` nebo `{error:'no_inventory_item'/'no_supplier'/'no_deficit'/'not_found'}`. |
| `create_gear_purchase_order(p_shortage_id, p_qty default null)` | `is_admin()` wrapper nad `_create_gear_po` (ruční tlačítko „Vytvořit objednávku"). GRANT authenticated+service_role. |
| `auto_order_gear_shortages()` | Demand-driven: projde otevřené deficity, dedup na existující draft/sent PO (jen naváže), jinak `_create_gear_po`. Vrací `{created, skipped_dup, skipped_no_item, skipped_no_supplier}`. Tlačítko „🔁 Objednat automaticky vše" + volitelný cron. SECURITY DEFINER, GRANT authenticated+service_role. |
| `receive_stock_from_document(p_lines jsonb, p_note default null)` | Naskladnění z dokladu (faktura/DL/ručně): `p_lines=[{sku,name,qty,unit_price,category}]` → upsert `inventory` (navýší stock, doplní unit_price když 0; nový řádek když SKU chybí) + pohyb přes `log_stock_movement` (receipt). `is_admin()`. Vrací `{success, stocked}`. Volá Velín záložka Naskladnění. **2026-06-24 přepsáno** z přímého `insert into inventory_movements(item_id,...)` (padalo „column item_id does not exist") na `log_stock_movement`. Migrace `20260624_log_stock_movement.sql`. SECURITY DEFINER, GRANT authenticated+service_role. |
| `log_stock_movement(p_item uuid, p_type text, p_qty int, p_note default null)` | **NEW 2026-06-24 (`20260624_log_stock_movement.sql`)** — Odolný zápis skladového pohybu. Cílový FK sloupec (`item_id`/`inventory_id`/`inventory_item_id`) i note sloupec (`note`/`notes`) detekuje přes `information_schema`, insert obalen do `EXCEPTION WHEN OTHERS` (fallback bez performed_by/note, jinak no-op) → **nikdy neshodí** volající tok. Používá Velín (Inventory výdej/příjem, BranchHelpers deduct/return, IssueToBranch) + `receive_stock_from_document`. SECURITY DEFINER, GRANT authenticated+service_role. |
| `learn_sku(p_sku, p_name, p_category, p_type, p_size, p_alias)` | **NEW 2026-06-24 (`20260624_sku_catalog.sql`)** — Učení číselníku `sku_catalog` z ručních korekcí po OCR. UPSERT řádku dle `sku`, přidá alias (název dokladu) do `aliases[]` → příště se položka zařadí sama. Volá `Logistika` Naskladnění po Uložit. SECURITY DEFINER, GRANT authenticated+service_role. |
| `_norm_supplier(p text)` | **NEW 2026-06-25 (`20260625_stock_receipts_pairing.sql`)** — Normalizace názvu dodavatele (bez diakritiky, malá písmena) pro fuzzy párování. IMMUTABLE. |
| `record_stock_receipt(p_doc_type, p_doc_number, p_doc_date, p_variable_symbol, p_supplier_ico, p_supplier_name, p_amount, p_financial_event_id, p_will_stock)` | **NEW 2026-06-25 (`20260625_stock_receipts_pairing.sql`)** — Fáze 6 párování DL⇄faktura + ochrana proti duplicitě. Zaeviduje doklad do `stock_receipts` (STATE_1, vč. `doc_date`), najde nespárovaný protějšek OPAČNÉHO typu od stejného dodavatele (IČO nebo `_norm_supplier` názvu) s částkou ±2 % (posl. 120 dní). Pokud protějšek už zboží naskladnil → `duplicate=true` (caller NEnaskladní znovu) + spáruje (`matched_receipt_id` obousměrně). Faktura bez nalezeného DL → `needs_dl=true` (banner „chybí DL"); příchozí DL k faktuře protějšku `needs_dl` zruší. `p_will_stock=false` při dílčím doplnění jen financí. Vrací `{receipt_id, duplicate, will_stock, matched_receipt_id, needs_dl}`. Volá `Logistika` Naskladnění (`stockAll`). SECURITY DEFINER, GRANT authenticated+service_role. |
| `check_asset_status(p_asset_type, p_vin, p_spz, p_name, p_amount)` | **NEW 2026-06-25 (`20260625_check_asset_status.sql`)** — Kontrola duplicity MAJETKU (ne dokladu) při scanu faktury (read-only). Vozidlo/motorka se páruje nezaměnitelně dle **VIN nebo SPZ** (i když byla zanesena přes Flotilu → `motorcycles`), ostatní dlouhodobý/krátkodobý majetek dle názvu (`_norm_supplier`). Vrací `status`: `new` (založit) / `asset_exists_needs_doc` (majetek existuje, ale chybí doklad/cena → jen DOPLNIT, nevytvářet duplicitu — vč. motorky ve flotile bez `acc_long_term_assets` řádku nebo s `missing_purchase_doc`) / `duplicate_full` (kompletní → varovat), + `{kind (motorcycle/long_term/short_term), motorcycle_id, asset_id, label}`. Volá `Logistika` Naskladnění (OCR extract → banner); skutečnou deduplikaci dělá `createAssetFromEvent` (Velín) při schválení události. SECURITY DEFINER, GRANT authenticated+service_role. |
| `check_document_status(p_doc_type, p_doc_number, p_doc_date, p_variable_symbol, p_supplier_ico, p_supplier_name, p_amount)` | **NEW 2026-06-25 (`20260625_check_document_status.sql`)** — Kontrola duplicity dokladu + dílčí doplnění (read-only). Klíč duplicity = **`doc_number` + `doc_date`** (100% jistota); bez č. dokladu fallback dodavatel + částka ±2 % + datum ±3 dny. Zjistí `in_finance` / `in_stock` (vč. zboží naskladněného protějškem DL↔faktura a faktur ve `financial_events` dle č. faktury+`duzp`). Vrací `status`: `new` (zavést) / `duplicate_full` (faktura: sklad+finance, DL: sklad → caller se zeptá) / `need_stock` (faktura ve financích, chybí naskladnit → doplnit jen sklad) / `need_finance` (zboží naskladněno, chybí finance → doplnit jen finance), + `{found, in_finance, in_stock, match}`. Volá `Logistika` Naskladnění (při OCR extractu i Uložit). SECURITY DEFINER, GRANT authenticated+service_role. |
| `ai_lookup_bookings_by_contact(p_contact text)` | **NEW 2026-06-26 (`20260626_ai_public_readonly_verify.sql` — PŘEDLOŽENO JAKO TEXT, nutno aplikovat před deployem `ai-public-agent`).** READ-ONLY ověření rezervací pro veřejného AI agenta podle **e-mailu NEBO telefonu** (bez hesla). Auto-detekce: `@` → email (`lower(p.email)=lower(contact)`), jinak telefon (porovnání **posledních 9 číslic** `regexp_replace(phone,'\D','','g')`, shoda s 3FA helperem). JOIN `bookings b` → `profiles p` (b.user_id), LEFT JOIN `motorcycles m`. Vrací `{success, count, matched_by, bookings[]}`; každá rezervace: `booking_number` (`#`+posl. 8 z UUID), `booking_id` (plné UUID pro další tooly), `status`, `payment_status`, `booking_source`, `start_date`, `end_date`, `total_price`, `pickup_method`, `created_at`, `confirmed_at`, `abandoned_email_sent`/`reserved_email_sent` (bool z markerů) a `emails[]` = přehled `sent_emails` k té rezervaci (`template_slug, subject, status, sent_at`, bez `recipient_email`). **MINIMALIZACE PII:** NIKDY nevrací číslo dokladu, číslo/skupinu ŘP, heslo, datum narození ani celé bydliště. **Striktní scoping:** filtruje VÝHRADNĚ na zadaný kontakt → nikdy nevrátí rezervaci jiného kontaktu. Vylučuje `is_test`. Limit 15, ORDER created_at DESC. SLOUŽÍ JEN KE ČTENÍ — úprava dál přes 3FA (`apply_booking_changes_anon`). SECURITY DEFINER, STABLE, `SET search_path=public`, `REVOKE ALL FROM public`, GRANT anon+authenticated+service_role. |
| `ai_get_booking_emails(p_ref text)` | **NEW 2026-06-26 (`20260626_ai_public_readonly_verify.sql` — PŘEDLOŽENO JAKO TEXT).** READ-ONLY — vrátí odeslané maily k jedné rezervaci pro AI agenta (bez hesla). Vstup: plné UUID (i z odkazu) NEBO 8znaková ref (`right(replace(id,'-',''),8)`, case-insensitive, `ambiguous` při shodě na >1). Vrací `{success, booking_number, status, payment_status, email_count, emails[]}` (`template_slug, subject, status, sent_at` z `sent_emails`, ORDER sent_at DESC, BEZ `recipient_email`/`body_html`). Účel: ověřit „přišel mi mail / je zaplaceno" — `booking_reserved` = po platbě; jen `booking_abandoned` = nezaplaceno; `booking_missing_docs` = zaplaceno, chybí doklady. Error: `missing_inputs`/`bad_ref`/`ambiguous`/`not_found`. SECURITY DEFINER, STABLE, `SET search_path=public`, `REVOKE ALL FROM public`, GRANT anon+authenticated+service_role. |
| `ai_booking_readiness(p_ref text)` | **NEW 2026-06-27 (`20260627_ai_public_readonly_ops.sql` — PŘEDLOŽENO JAKO TEXT, nutno aplikovat před deployem `ai-public-agent`).** READ-ONLY připravenost rezervace k VYZVEDNUTÍ pro AI agenta (bez hesla, **PII-minimal — NIKDY nevrací samotný přístupový kód ani čísla dokladů**). Vstup: plné UUID nebo 8znaková ref (`ambiguous` při shodě >1). Načte booking (user_id, end_date, moto_id), zavolá `check_booking_docs_status(user_id, end_date::date, moto_id)` a přečte `branch_door_codes`. Vrací `{success, booking_number, status, payment_status, booking_source, docs_ok (bool), docs_missing_reason (text|null), codes_issued (=active∧sent), codes_active, codes_sent, codes_withheld_reason}`. Účel: dotazy „jak se dostanu k motorce / nepřišly mi kódy / ověřili jste doklady / co mi chybí". Error: `missing_inputs`/`bad_ref`/`ambiguous`/`not_found`. SECURITY DEFINER, STABLE, `SET search_path=public`, `REVOKE ALL FROM public`, GRANT anon+authenticated+service_role. |
| `ai_get_order_status(p_contact text)` | **NEW 2026-06-27 (`20260627_ai_public_readonly_ops.sql` — PŘEDLOŽENO JAKO TEXT).** READ-ONLY stav E-SHOP objednávky / POUKAZU pro AI agenta podle e-mailu NEBO čísla objednávky/kódu (bez hesla, PII-minimal). `shop_orders` dle `customer_email` nebo `order_number` → `{order_number, status, payment_status, total, tracking_number, created_at}`. `vouchers` dle `buyer_email`/`order_id`/`code` → `{code_masked` (`left(code,2)||'****'` — **NIKDY celý kód**), `status, amount, valid_until, source}`. Vrací `{success, matched_by, orders[], vouchers[]}`. NEvrací adresy ani celé kódy. SECURITY DEFINER, STABLE, `SET search_path=public`, `REVOKE ALL FROM public`, GRANT anon+authenticated+service_role. |

---

## `get_app_install_timeseries()` — NEW 2026-07-28 (`20260728b_app_install_timeseries.sql`, APLIKUJE AUTO-DEPLOY po merge)

SECURITY DEFINER, STABLE, `set search_path = public`, gate `is_admin()` (jinak `raise exception 'admin only'`), REVOKE public/anon + GRANT authenticated/service_role. Vrací jeden JSON pro **graf instalací appky v čase** (Velín → Analýza → Aplikace, komponenta `analyza/InstallsChart.jsx`):
- `installs` = `[{d 'YYYY-MM-DD', android, ios, other}]` — NOVÉ instalace per den dle `app_installations.first_seen_at` × `lower(platform)`. Přesné od 2026-06-28 (build s `InstallationService`); starší instalace se objeví v den prvního heartbeatu (hlubší per-platform historie z Play/App Store v DB není).
- `registrations` = `[{d, n}]` — registrace účtů per den (`profiles.created_at`) — kompletní historie růstu uživatelské základny (historická křivka, bez platform rozpadu).
- `tracking_since` = první den evidence instalací.
Agregaci (den/týden/měsíc), časové úseky (14 d–Vše), kumulativní režim a Android/iOS/celkem křivky dělá frontend nad denními řádky (řádků jsou max stovky).

## `get_app_install_stats()` — NEW 2026-06-27, dedup 2026-06-27 B, **PŘEPIS na app_installations 2026-06-28**

SECURITY DEFINER, `set search_path = public`, gate `is_admin()` (jinak `raise exception 'admin only'`). `grant execute … to authenticated`. Vrací jeden JSON s agregáty pro **Velín → Analýza → Aplikace**.

**2026-06-28 — PŘEPIS na `app_installations` (build-based, přesné):** Hlavní metriky se nově počítají z tabulky `app_installations` (heartbeat z appky se stabilním device UUID), nezávisle na souhlasu s push:
- `active_devices` = `count(*) from app_installations where last_seen_at > now()-interval '30 days'` (= **MAU**)
- `mau` / `wau` / `dau` = aktivní za 30 / 7 / 1 den (z `last_seen_at`)
- `installs_total` / `total_devices` = `count(*) from app_installations` (všechny evidované instalace)
- `app_users` = `count(distinct user_id) from app_installations`
- `android` / `ios` = aktivní zařízení (30 dní) dle `lower(platform)` (platí `active_devices = android + ios`)
- `push_enabled_devices` = aktivní zařízení (30 dní) s `push_enabled is true`
- `app_bookings` = `count(*) from bookings where booking_source = 'app'`
- `push_active_devices` = **legacy proxy** `count(distinct (user_id, lower(platform)))` z aktivních `push_tokens` — přechodná reference, než se rozšíří nový build appky (pak ji `app_installations` plně nahradí)

**Proč přepis:** push_tokens trpí (a) nadhodnocením — rotace FCM tokenu (`onTokenRefresh`, `upsert(onConflict:'token')`) zakládá nové aktivní řádky, starý zůstává `active=true`; (b) podhodnocením — `PushService.initialize()` se ukončí, když uživatel odmítl notifikace, takže instalace bez pushů v `push_tokens` VŮBEC nejsou. `app_installations` (vlastní device UUID + heartbeat) řeší obojí a funguje i na iOS, kam Play Developer API nedosáhne. Dedup verze 2026-06-27 B (`count(distinct (user_id, platform))` nad push_tokens) zůstává zachovaná jen jako `push_active_devices`.

Tabulka `app_installations` + tato RPC spuštěny ručně v SQL editoru, ověřeno 2026-06-28. Plní appka přes `InstallationService` (build nutný — nasadit SQL PŘED rozšířením buildu). Není to oficiální počet instalací z Google Play (ten by vyžadoval Play Developer API).

### Samoobslužná pobočka (kiosk) — NEW 2026-06-29 (`20260628_selfservice_kiosk.sql` + `20260629_kiosk_cameras_power.sql`, APLIKOVÁNO + OVĚŘENO)
Všechny SECURITY DEFINER, `SET search_path=public`. Autentizace kiosku přes (device_id + device_token) přes helper níže; kiosk běží pod anon klíčem a k tabulkám se NEdostane přímo (admin-only RLS), jen přes tyto RPC.

| Funkce | Popis |
|--------|-------|
| `kiosk_device_branch(p_device_id, p_device_token, p_touch)` | **INTERNÍ helper** (REVOKE FROM public — volá se jen z ostatních kiosk RPC). Ověří zařízení (`is_active`, token sedí), vrátí `branch_id` nebo NULL; při `p_touch=true` bumpne `last_seen_at`. |
| `kiosk_heartbeat(p_device_id, p_device_token, p_app_version, p_platform)` | Online stav (update last_seen + verze/platforma) + vrací konfiguraci pobočky: branch_name, music URL, časování, `power_status_url`, `power_poll_seconds`. GRANT anon/authenticated/service_role. |
| `kiosk_resolve_code(p_device_id, p_device_token, p_code)` | Ověří zadaný kód. Pořadí: **servisní heslo** (`branch_service_codes`) → vrátí `kind:'service'` + seznam VŠECH aktivních dveří; jinak **zákaznický kód** (`branch_door_codes` aktivní/vydaný/v platnosti/této pobočky) → vrátí `kind` (motorcycle/accessories) + konkrétní dveře (accessories door, nebo motorcycle dveře dle `motorcycles.box_number`) vč. relay_url/light_url + music URL; jinak `{ok:false,error:invalid_code}`. Bumpne last_seen. GRANT anon. |
| `kiosk_log_open(p_device_id, p_device_token, p_door_id, p_kind, p_booking_id, p_success, p_detail)` | Audit otevření → `branch_door_events`. GRANT anon. |
| `kiosk_fetch_commands(p_device_id, p_device_token)` | Vrátí čekající (`status=pending`) příkazy pro zařízení (id/command/params) — kiosk je stahuje po broadcast probuzení i při heartbeatu (pojistka). GRANT anon. |
| `kiosk_complete_command(p_device_id, p_device_token, p_command_id, p_success, p_result)` | Označí příkaz done/failed + `executed_at`. GRANT anon. |
| `kiosk_report_power(p_device_id, p_device_token, p_payload)` | Upsert `branch_power_status` pro pobočku zařízení. Mapuje běžné klíče měničů přes `kiosk_jnum`/`kiosk_jbool` (soc/battery_soc, pvPower/pv_power_w, loadPower…, grid/gen) + ukládá celý `raw`. GRANT anon. |
| `kiosk_jnum(p jsonb, VARIADIC keys)` / `kiosk_jbool(p jsonb, VARIADIC keys)` | IMMUTABLE helpery — bezpečné čtení numeric/bool z jsonb dle prvního přítomného klíče (try/cast). |
| `touch_updated_at()` | Generický `RETURNS trigger` — `NEW.updated_at := now()` (sdílený pro kiosk tabulky; viz STATE_4). |
| `kiosk_command_broadcast()` | `RETURNS trigger` (AFTER INSERT na kiosk_commands WHERE pending) — pošle `realtime.send({id}, 'cmd', 'kiosk:<device_id>', false)`. Obaleno EXCEPTION (insert nikdy neshodí, i kdyby `realtime.send` chyběl → fallback poll). |
| `mirror_route_images_tick()` | **NEW 2026-07-03:** SECURITY DEFINER void — přes `net.http_post` zavolá edge fn `mirror-route-images` (Authorization: service_role_key z `app_settings`). Volá ji cron job `mirror-route-images` (id 18, */6 min). Bez klíče v app_settings tiše no-op. |
| `get_visitor_stats(p_from, p_to, p_host, p_granularity)` | Server-side agregace reálné lidské návštěvnosti webu (`visitor_log`) pro Velín → Analýza → Návštěvnost. `is_admin()` guard, SECURITY DEFINER, `SET statement_timeout='20s'`, štíhlé CTE (jen potřebné sloupce, index `idx_visitor_log_host_ts`). Vrací jeden JSON: `total_views`, `unique_visitors`, `by_host`, `by_referrer_type`, `by_referrer_domain`, `top_paths`, `top_referrers`, `by_device`, `by_country`, `timeline` (dle p_granularity day/week/month/year). **UPDATE 2026-07-09 (`20260709_visitor_stats_hourly_dow.sql`):** přidány 3 agregáty pro vytíženost dne/týdne — `by_hour` `[{hour 0–23, views, visitors}]` (napříč všemi dny rozsahu), `by_dow` `[{dow 1–7 = Po–Ne, views, visitors}]`, `by_dow_hour` `[{dow 1–7, hour 0–23, views}]` (heatmapa den×hodina). Hodina i den v LOKÁLNÍM čase `Europe/Prague` (`ts at time zone 'Europe/Prague'`, ISODOW 1=Po…7=Ne). JSON zpětně kompatibilní (jen přibyly klíče), signatura/timeout/security beze změny. Čte Velín `Navstevnost.jsx` (heatmapa + bar po hodinách + bar po dnech, špička zvýrazněna). |
